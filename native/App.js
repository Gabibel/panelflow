// The entry point Expo looks for. Everything is in src/ — including every
// sentence, which is what lets `backend/test/i18n.test.js` scan one directory
// and know it has seen all of them.
export { default } from './src/Shell.js';
