// Generated by scripts/build-native-inject.mjs — do not edit.
// The extension's content scripts, baked in for the React Native shell.
export const early = "window.PanelFlowBlockedHosts=[\"popads.net\",\"popcash.net\",\"onclickads.net\",\"clickadu.com\",\"adcash.com\",\"zeropark.com\",\"hilltopads.net\",\"adskeeper.com\",\"galaksion.com\",\"ad-maven.com\",\"adsupply.com\",\"yllix.com\",\"adsco.re\",\"adnium.com\",\"onclickalgo.com\",\"poweradnetwork.com\",\"profitableratecpm.com\",\"highperformanceformat.com\",\"sunmedia.tv\",\"adsterra.com\",\"monetag.com\",\"propellerads.com\",\"exoclick.com\",\"juicyads.com\",\"mgid.com\",\"trafficjunky.net\",\"trafficfactory.biz\",\"trafficstars.com\",\"plugrush.com\",\"eroadvertising.com\",\"revcontent.com\",\"infolinks.com\",\"bidvertiser.com\",\"a-ads.com\",\"vidoomy.com\",\"servedbyadbutler.com\",\"doubleclick.net\",\"googlesyndication.com\",\"googletagservices.com\",\"adnxs.com\",\"amazon-adsystem.com\",\"pubmatic.com\",\"rubiconproject.com\",\"openx.net\",\"casalemedia.com\",\"smartadserver.com\",\"criteo.com\",\"criteo.net\",\"adform.net\",\"33across.com\",\"sharethrough.com\",\"teads.tv\",\"yieldmo.com\",\"media.net\",\"adroll.com\",\"taboola.com\",\"outbrain.com\",\"google-analytics.com\",\"googletagmanager.com\",\"moatads.com\",\"scorecardresearch.com\",\"quantserve.com\",\"histats.com\",\"statcounter.com\",\"hotjar.com\",\"imasdk.googleapis.com\",\"pubads.g.doubleclick.net\",\"securepubads.g.doubleclick.net\",\"innovid.com\",\"spotxchange.com\",\"springserve.com\",\"aniview.com\"];\nwindow.PanelFlowBlockedImageHosts=[\"adsterra.com\",\"monetag.com\",\"propellerads.com\",\"exoclick.com\",\"juicyads.com\",\"mgid.com\",\"trafficjunky.net\",\"trafficfactory.biz\",\"trafficstars.com\",\"plugrush.com\",\"eroadvertising.com\",\"revcontent.com\",\"infolinks.com\",\"bidvertiser.com\",\"a-ads.com\",\"vidoomy.com\",\"servedbyadbutler.com\",\"google-analytics.com\",\"googletagmanager.com\",\"moatads.com\",\"scorecardresearch.com\",\"quantserve.com\",\"histats.com\",\"statcounter.com\",\"hotjar.com\"];\ntry{\n// The transport the other injected scripts expect, spelt for react-native-webview.\n//\n// `chrome-shim.js` and `report-failure.js` look for one of two things on the\n// page: Android's `PanelFlowNative.post` (an `@JavascriptInterface`) or iOS's\n// `webkit.messageHandlers.panelflow` (a `WKScriptMessageHandler`). A WebView\n// driven from React Native offers neither — it offers\n// `ReactNativeWebView.postMessage`, on both platforms. So this file provides the\n// Android-shaped name over that one channel, and every script after it runs\n// unchanged, exactly as it does in the extension and in the two native shells.\n//\n// First in the injection set, before anything that might want to speak.\n(function () {\n  'use strict';\n  if (window.PanelFlowNative && window.PanelFlowNative.post) return;\n\n  // Messages sent before the host is ready would otherwise be dropped in\n  // silence. It is a small window — `ReactNativeWebView` is installed with the\n  // page — but a lost `pageDetected` is a reader pill that never appears, and\n  // the failure would look like a detection bug rather than a timing one.\n  const queued = [];\n\n  function flush() {\n    if (!window.ReactNativeWebView) return false;\n    while (queued.length) window.ReactNativeWebView.postMessage(queued.shift());\n    return true;\n  }\n\n  window.PanelFlowNative = {\n    post(payload) {\n      queued.push(String(payload));\n      if (flush()) return;\n      // Poll rather than wait on an event: there is no event for this, and the\n      // alternative — losing the message — is worse than a handful of ticks.\n      let tries = 0;\n      const timer = setInterval(() => {\n        if (flush() || ++tries > 100) clearInterval(timer);\n      }, 50);\n    },\n  };\n})();\n\n}catch(e){if(window.PanelFlowFailed)window.PanelFlowFailed(\"rn-bridge.js\",e);else console.warn('panelflow: rn-bridge.js failed',e)}\ntry{\n// Ad blocking, from inside the page, because this shell has no way to do it\n// from outside.\n//\n// The other three surfaces block requests before they are made: Chrome compiles\n// `shared/adblock-list.json` into a declarativeNetRequest ruleset, iOS into a\n// WKContentRuleList, Android matches hostnames in `shouldInterceptRequest`.\n// React Native's WebView exposes no subresource hook at all —\n// `onShouldStartLoadWithRequest` only sees main-frame navigations — so the only\n// place left to stand is the page itself.\n//\n// That is a weaker position and it is worth being honest about which parts it\n// costs: a request this file cannot see is a request it cannot stop. What it\n// can do is refuse the three ways a scan site actually loads its advertising —\n// a <script> or <iframe> put into the document, a fetch, an XMLHttpRequest —\n// and that is most of the weight and nearly all of the latency.\n//\n// The list is `shared/adblock-list.json`, the same file the other three read,\n// baked in by scripts/build-native-inject.mjs. There is no second list here.\n(function () {\n  'use strict';\n  if (window.__panelflowAdblock) return;\n  window.__panelflowAdblock = true;\n\n  const HOSTS = new Set(window.PanelFlowBlockedHosts || []);\n\n  /**\n   * The hosts whose *images* may also be refused, and why that is a separate\n   * list rather than a rule.\n   *\n   * 47 of the 72 entries carry `images: false`, which the Chrome ruleset spells\n   * as a shorter `resourceTypes` and Safari as `load-type`. It means: block this\n   * host's scripts and frames, never its pictures. A blocker that ignores it\n   * takes the panels out of chapters served from a CDN that happens to sit\n   * under a domain on the list — the reader then finds no strip, decides the\n   * page must be prose, and opens a manga chapter as text. That is not a\n   * hypothetical; it is what this file did before it read this column.\n   */\n  const IMAGE_HOSTS = new Set(window.PanelFlowBlockedImageHosts || []);\n\n  /**\n   * A site the reader asked us to leave alone.\n   *\n   * Whitelisting is an account setting (`shared/prefs.js`), and it means \"stop\n   * blocking here\" — the whole page, not just its own assets. So this file\n   * stands down entirely rather than filtering what it stands down on.\n   */\n  const whitelisted = (window.PanelFlowAdblockWhitelist || []).some((host) => {\n    const here = location.hostname.toLowerCase();\n    return here === host || here.endsWith('.' + host);\n  });\n\n  if (!HOSTS.size || whitelisted) return;\n\n  /**\n   * Whether a URL belongs to a blocked host, for a given kind of request.\n   *\n   * Subdomains count — an entry is a site, and `a.ads.example.com` is that\n   * site — but a suffix match alone would also block `notexample.com`, so the\n   * dot is required. Anything unparseable (a data: URI, a relative path) is not\n   * a third-party request and is left alone.\n   */\n  function blocked(url, images) {\n    let host;\n    try {\n      host = new URL(String(url), location.href).hostname.toLowerCase();\n    } catch {\n      return false;\n    }\n    const list = images ? IMAGE_HOSTS : HOSTS;\n    if (list.has(host)) return true;\n    for (const h of list) if (host.endsWith('.' + h)) return true;\n    return false;\n  }\n\n  // --- what the page asks for itself ----------------------------------------\n\n  const nativeFetch = window.fetch;\n  if (typeof nativeFetch === 'function') {\n    window.fetch = function (input, init) {\n      const url = (input && input.url) || input;\n      if (blocked(url, false)) {\n        // A rejected promise, not a hang: a tracker whose fetch never settles\n        // can hold a page's own `Promise.all` open forever, and the site is not\n        // ours to freeze.\n        return Promise.reject(new TypeError('blocked by PanelFlow'));\n      }\n      return nativeFetch.call(this, input, init);\n    };\n  }\n\n  const open = XMLHttpRequest.prototype.open;\n  XMLHttpRequest.prototype.open = function (method, url, ...rest) {\n    if (blocked(url, false)) {\n      // Pointed at nothing rather than thrown: a synchronous throw from `open`\n      // lands in the page's own code, and some ad loaders take an exception\n      // there as a reason to retry in a loop.\n      return open.call(this, method, 'about:blank', ...rest);\n    }\n    return open.call(this, method, url, ...rest);\n  };\n\n  // --- what the page puts into the document ---------------------------------\n\n  const SRC = ['src', 'href', 'data-src'];\n\n  /** The address an element would load, whichever attribute it keeps it in. */\n  function addressOf(el) {\n    for (const attr of SRC) {\n      const value = el.getAttribute && el.getAttribute(attr);\n      if (value) return value;\n    }\n    return null;\n  }\n\n  function inspect(node) {\n    if (!node || node.nodeType !== 1) return;\n    const tag = node.tagName;\n    if (tag !== 'SCRIPT' && tag !== 'IFRAME' && tag !== 'IMG' && tag !== 'LINK') return;\n    const url = addressOf(node);\n    // An <img> is judged against the shorter list, and a chapter's panels are\n    // why. Everything else is judged against the whole one.\n    if (url && blocked(url, tag === 'IMG')) node.remove();\n  }\n\n  // A <script> or <iframe> starts loading when it is inserted, so catching the\n  // insertion is catching it before the request.\n  const observer = new MutationObserver((records) => {\n    for (const record of records) {\n      for (const node of record.addedNodes) {\n        inspect(node);\n        if (node.querySelectorAll) {\n          for (const child of node.querySelectorAll('script,iframe,img,link')) inspect(child);\n        }\n      }\n    }\n  });\n\n  observer.observe(document.documentElement || document, { childList: true, subtree: true });\n})();\n\n}catch(e){if(window.PanelFlowFailed)window.PanelFlowFailed(\"rn-adblock.js\",e);else console.warn('panelflow: rn-adblock.js failed',e)}\ntry{\n// What a broken injected script looks like on a phone.\n//\n// The native shells wrap each injected file in its own try/catch so one bad\n// file cannot cost the user the rest — the right call, except that the catch\n// used to end in `console.warn`. Nobody reads a WebView console on a phone.\n// The observable result of detect.js dying, or of an asset missing from the\n// build entirely, was a page where the Reader Mode pill simply never appeared,\n// with nothing anywhere saying why. \"It doesn't work on this site\" is what the\n// user reports, and it is not enough to act on.\n//\n// So the catch calls in here instead, and this file puts a line on the screen.\n// It is injected first, before every other file, and it deliberately depends on\n// nothing: not chrome-shim.js (which is itself one of the files that can fail),\n// not the reader's stylesheet, not the page's own scripts. Two consequences —\n// the eight lines of bridge transport below are a copy of the shim's, and every\n// style is inline and `!important`, because the page is a scan site and its CSS\n// is hostile by accident if not by design.\n(function () {\n  'use strict';\n  // User scripts run again on every SPA navigation on both platforms.\n  if (window.PanelFlowFailed && window.PanelFlowFailed.__panelflow) return;\n\n  const BOX = 'panelflow-load-failure';\n  const failures = [];\n\n  // Fire-and-forget, so it needs no reply and holds no state open. Both shells\n  // route `event` envelopes through a `when`/`switch` that ignores what it does\n  // not know, so this is already safe to send to today's builds.\n  const transport = (() => {\n    if (window.PanelFlowNative && window.PanelFlowNative.post) {\n      return (s) => window.PanelFlowNative.post(s);\n    }\n    if (window.webkit && window.webkit.messageHandlers &&\n        window.webkit.messageHandlers.panelflow) {\n      return (s) => window.webkit.messageHandlers.panelflow.postMessage(s);\n    }\n    return null;\n  })();\n\n  const style = (el, css) => el.setAttribute('style', css);\n\n  function paint() {\n    const root = document.body || document.documentElement;\n    if (!root) return;\n\n    let box = document.getElementById(BOX);\n    if (!box || !box.isConnected) {\n      box = document.createElement('div');\n      box.id = BOX;\n      style(box, [\n        'position:fixed!important', 'left:12px!important', 'right:12px!important',\n        'bottom:12px!important', 'z-index:2147483647!important',\n        'box-sizing:border-box!important', 'padding:12px 14px!important',\n        'border-radius:12px!important', 'border:1px solid #f87171!important',\n        'background:#1f1113!important', 'color:#fde8e8!important',\n        'font:13px/1.45 system-ui,-apple-system,sans-serif!important',\n        'text-align:left!important', 'box-shadow:0 6px 24px rgba(0,0,0,.4)!important',\n      ].join(';'));\n\n      const text = document.createElement('div');\n      text.id = BOX + '-text';\n      box.appendChild(text);\n\n      const row = document.createElement('div');\n      style(row, 'margin-top:10px!important;display:flex!important;gap:8px!important');\n      const button = (label, onClick) => {\n        const b = document.createElement('button');\n        b.type = 'button';\n        b.textContent = label;\n        style(b, [\n          'all:unset!important', 'cursor:pointer!important',\n          'padding:6px 12px!important', 'border-radius:8px!important',\n          'border:1px solid #f87171!important', 'color:#fde8e8!important',\n          'font:600 13px/1 system-ui,-apple-system,sans-serif!important',\n        ].join(';'));\n        b.addEventListener('click', onClick);\n        row.appendChild(b);\n        return b;\n      };\n      // Reload first because it is the one that sometimes works: a script the\n      // page clobbered before we ran may well survive the next load. A file\n      // missing from the build will not, which is why the text says which.\n      button('Reload', () => location.reload());\n      button('Hide', () => box.remove());\n      box.appendChild(row);\n\n      root.appendChild(box);\n    }\n\n    const lines = failures.map((f) => `${f.file} — ${f.error}`).join('\\n');\n    document.getElementById(BOX + '-text').textContent =\n      'PanelFlow could not fully start on this page. Reader Mode and your ' +\n      'library may not work here.\\n' + lines;\n  }\n\n  function show() {\n    if (document.body) return paint();\n    // At document-start there is no body yet, and appending to a bare\n    // <html> is how you get an element the parser then throws away.\n    document.addEventListener('DOMContentLoaded', paint, { once: true });\n  }\n\n  /**\n   * One injected file did not survive. Called by the native wrappers, by file\n   * name, including for a file the build did not ship at all.\n   */\n  function report(file, error) {\n    const detail = { file: String(file), error: String((error && error.message) || error || 'unknown') };\n    failures.push(detail);\n    // Kept, because it is what a developer with a cable attached reads, and it\n    // is the only trace left if the DOM part of this file is what broke.\n    console.warn('panelflow: ' + detail.file + ' failed', error);\n    try {\n      if (transport) transport(JSON.stringify({ event: 'scriptFailed', ...detail }));\n    } catch (e) { /* the bridge is the thing that is broken; nothing else to try */ }\n    try {\n      show();\n    } catch (e) {\n      console.warn('panelflow: could not show the failure', e);\n    }\n  }\n\n  report.__panelflow = true;\n  report.list = () => failures.slice();\n  window.PanelFlowFailed = report;\n})();\n\n}catch(e){if(window.PanelFlowFailed)window.PanelFlowFailed(\"report-failure.js\",e);else console.warn('panelflow: report-failure.js failed',e)}\ntry{\n// Runs at document_start. Blocks the popup/redirect hijacks common on manga\n// aggregator sites: window.open calls not triggered by a real user gesture,\n// window.open calls that spend a real gesture on somebody else's domain, and\n// programmatic clicks on injected anchors targeting new windows.\n//\n// The manifest puts this one file in the MAIN world, and it has to stay there.\n// A content script's default isolated world has its own `window`, so the\n// reassignment below used to replace a `window.open` no page script could ever\n// reach: every ad tab still opened, and nothing about the code looked wrong.\n// The click listener worked all along — DOM events are shared between the\n// worlds — so exactly half of this file was doing anything.\n//\n// The price of the main world is no `chrome.*` here. Nothing in this file wants\n// it; if that changes, the new part belongs in an isolated script that talks to\n// this one through the DOM, not in a second copy of the guard.\n(() => {\n  'use strict';\n  let userGestureUntil = 0;\n  const GESTURE_WINDOW_MS = 1000;\n\n  // Clicks on PanelFlow's own overlay are not a gesture the page may spend.\n  // The reader and the library sheet are elements in the site's document, so a\n  // press on the close button or the chapter list looks to the page exactly\n  // like a press on the page — and the pop-under scripts these sites run are\n  // waiting for precisely that. Reading a chapter should not cost a tab of\n  // advertising per button pressed, so the window never opens for our own.\n  //\n  // The sheet is a closed shadow root and the reader is not, so `closest` from\n  // the event's target answers for both: inside the sheet every event is\n  // reported on its host, and inside the reader on the button itself.\n  const ours = (e) => {\n    const el = e.target;\n    return !!(el && el.closest\n      && el.closest('#panelflow-reader, #panelflow-libmodal, #panelflow-pill'));\n  };\n\n  const markGesture = (e) => {\n    if (ours(e)) return;\n    userGestureUntil = Date.now() + GESTURE_WINDOW_MS;\n  };\n  addEventListener('pointerdown', markGesture, true);\n  addEventListener('keydown', markGesture, true);\n\n  // The registrable domain, near enough for this one decision. The real answer\n  // needs the public suffix list, which is a megabyte we are not shipping into\n  // every page at document_start; the last two labels are right for the .com,\n  // .fr and .to domains these sites live on, and the exception list below\n  // covers the \".co.uk\" shape. Being wrong here means treating two domains as\n  // one site — which loses a block, never a page the reader wanted.\n  const TWO_PART = new Set(['co', 'com', 'net', 'org', 'gov', 'edu', 'ac']);\n  const site = (host) => {\n    const parts = String(host || '').toLowerCase().split('.').filter(Boolean);\n    if (parts.length < 3) return parts.join('.');\n    return parts.slice(TWO_PART.has(parts[parts.length - 2]) ? -3 : -2).join('.');\n  };\n\n  // A real click on the site's own \"next chapter\" control is a real gesture,\n  // so the gesture window above cannot tell it apart from a real click that\n  // the page answers by opening an advertiser. What tells them apart is where\n  // the tab is going: a scan site opens its own pages — the next chapter, the\n  // series index, its Discord invite is an <a> the reader can see — and never\n  // needs script to open somebody else's domain in a new tab.\n  //\n  // No URL at all is the pop-under proper: open a blank tab now, keep the\n  // handle, and navigate it once the gesture is spent and nobody is watching.\n  const sameSite = (url) => {\n    if (url === undefined || url === null || url === '') return false;\n    let u;\n    try { u = new URL(String(url), location.href); } catch (e) { return false; }\n    if (u.protocol !== 'http:' && u.protocol !== 'https:') return false;\n    return site(u.hostname) === site(location.hostname);\n  };\n\n  const nativeOpen = window.open;\n  window.open = function (...args) {\n    if (Date.now() > userGestureUntil) {\n      console.debug('[PanelFlow] blocked window.open without user gesture:', args[0]);\n      return null;\n    }\n    if (!sameSite(args[0])) {\n      console.debug('[PanelFlow] blocked window.open to another site:', args[0]);\n      return null;\n    }\n    return nativeOpen.apply(this, args);\n  };\n\n  // Neutralize \"click anywhere opens an ad tab\" overlays: synthetic clicks\n  // (isTrusted === false) on anchors with target=_blank are cancelled.\n  addEventListener('click', (e) => {\n    if (e.isTrusted) return;\n    const a = e.target.closest && e.target.closest('a[target=\"_blank\"]');\n    if (a) {\n      e.preventDefault();\n      e.stopImmediatePropagation();\n      console.debug('[PanelFlow] blocked synthetic click on', a.href);\n    }\n  }, true);\n})();\n\n}catch(e){if(window.PanelFlowFailed)window.PanelFlowFailed(\"popup-guard.js\",e);else console.warn('panelflow: popup-guard.js failed',e)}\ntry{\n// `chrome.*` for pages that are not in Chrome.\n//\n// The detection engine, the reader and the add-to-library modal are the same\n// files the extension ships (`extension/content/*.js`), injected verbatim into\n// the in-app browser. They touch exactly five Chrome APIs; this file provides\n// those five over the native bridge, and nothing else. Keeping the shim this\n// small is deliberate — the moment it starts emulating Chrome broadly, the\n// mobile behaviour and the extension behaviour drift.\n//\n// Must be injected BEFORE detect.js / reader.js / library-modal.js.\n(function () {\n  'use strict';\n  if (window.chrome && window.chrome.runtime && window.chrome.runtime.__panelflowShim) return;\n\n  let nextId = 1;\n  const pending = new Map();\n\n  const transport = (() => {\n    if (window.PanelFlowNative && window.PanelFlowNative.post) {\n      return (s) => window.PanelFlowNative.post(s);\n    }\n    if (window.webkit && window.webkit.messageHandlers &&\n        window.webkit.messageHandlers.panelflow) {\n      return (s) => window.webkit.messageHandlers.panelflow.postMessage(s);\n    }\n    return null;\n  })();\n\n  function request(msg) {\n    return new Promise((resolve) => {\n      if (!transport) return resolve(undefined);\n      const id = nextId++;\n      // Resolving with `undefined` on timeout rather than rejecting: that is\n      // what a Chrome content script sees when the worker is gone, and the\n      // callers already handle it (they check chrome.runtime.lastError).\n      const timer = setTimeout(() => {\n        if (pending.delete(id)) resolve(undefined);\n      }, 45000);\n      pending.set(id, { resolve, timer });\n      transport(JSON.stringify({ id, msg }));\n    });\n  }\n\n  const pageListeners = [];\n\n  const runtime = {\n    __panelflowShim: true,\n    // Chrome sets `lastError` only inside a failed callback. Nothing in the\n    // content scripts writes it, and they only ever read it to detect a dead\n    // worker, so a permanent null is honest here.\n    lastError: null,\n    id: 'panelflow-mobile',\n    sendMessage(msg, callback) {\n      const p = request(msg);\n      if (typeof callback === 'function') { p.then(callback); return undefined; }\n      return p;\n    },\n    onMessage: {\n      addListener: (fn) => pageListeners.push(fn),\n      removeListener: (fn) => {\n        const i = pageListeners.indexOf(fn);\n        if (i !== -1) pageListeners.splice(i, 1);\n      },\n    },\n  };\n\n  // The content scripts use chrome.storage.local only for reader preferences\n  // and the auto-open-per-site settings; it lands in the same store the rest of\n  // the app uses, so a preference set in the reader is visible to the shell.\n  const storage = {\n    local: {\n      get(keys, callback) {\n        const p = request({ type: 'storageGet', keys: keys ?? null })\n          .then((r) => (r && r.values) || {});\n        if (typeof callback === 'function') { p.then(callback); return undefined; }\n        return p;\n      },\n      set(obj, callback) {\n        const p = request({ type: 'storageSet', values: obj }).then(() => undefined);\n        if (typeof callback === 'function') { p.then(callback); return undefined; }\n        return p;\n      },\n      remove(keys, callback) {\n        const p = request({ type: 'storageRemove', keys }).then(() => undefined);\n        if (typeof callback === 'function') { p.then(callback); return undefined; }\n        return p;\n      },\n    },\n  };\n\n  window.chrome = Object.assign(window.chrome || {}, { runtime, storage });\n\n  /**\n   * Native's handle on this page. `deliver` completes a pending sendMessage;\n   * `dispatch` is how the browser toolbar's buttons reach the content scripts —\n   * it stands in for the extension popup sending `toggleReader`,\n   * `openLibraryModal` or `getSeriesMeta` to the active tab.\n   */\n  window.PanelFlowPage = {\n    deliver(id, body) {\n      const entry = pending.get(id);\n      if (!entry) return;\n      pending.delete(id);\n      clearTimeout(entry.timer);\n      entry.resolve(typeof body === 'string' ? safeParse(body) : body);\n    },\n    dispatch(msg, replyId) {\n      const parsed = typeof msg === 'string' ? safeParse(msg) : msg;\n      let answered = false;\n      const respond = (r) => {\n        if (answered) return;\n        answered = true;\n        if (replyId != null && transport) {\n          transport(JSON.stringify({ reply: { id: replyId, body: r ?? null } }));\n        }\n      };\n      for (const fn of pageListeners.slice()) {\n        try { fn(parsed, {}, respond); } catch (e) { console.warn('page listener failed', e); }\n      }\n      return answered;\n    },\n    // The in-app browser's \"is there a chapter here?\" indicator, and the source\n    // of the compatibility verdict once a page has actually loaded — which\n    // beats the markup-only guess the search list showed.\n    state() {\n      const d = window.__panelflowDetect;\n      return {\n        url: location.href,\n        title: document.title,\n        detected: !!(d && d.detection),\n        readerOpen: !!(window.PanelFlowReader && window.PanelFlowReader.isOpen &&\n          window.PanelFlowReader.isOpen()),\n        meta: d && d.detection ? d.seriesMeta() : null,\n      };\n    },\n  };\n\n  const safeParse = (s) => { try { return JSON.parse(s); } catch { return s; } };\n})();\n\n}catch(e){if(window.PanelFlowFailed)window.PanelFlowFailed(\"chrome-shim.js\",e);else console.warn('panelflow: chrome-shim.js failed',e)}";
export const late = "try{\n// Generated by scripts/build-messages.mjs from shared/_locales/.\n// Edit the locale files, not this one — `npm run sync:shared` rewrites it.\nglobalThis.PanelFlowMessages = {\n  \"en\": {\n    \"extName\": \"PanelFlow\",\n    \"extDescription\": \"Read manga on any site in a clean reader, and keep one library across your devices.\",\n    \"commandToggleReader\": \"Show or hide the reader on this page\",\n    \"actionBack\": \"Back\",\n    \"actionCancel\": \"Cancel\",\n    \"actionConnect\": \"Connect\",\n    \"actionCreateAccount\": \"Create account\",\n    \"actionDisconnect\": \"Disconnect\",\n    \"actionDone\": \"Done\",\n    \"actionGotIt\": \"Got it\",\n    \"actionNext\": \"Next\",\n    \"actionOpen\": \"Open\",\n    \"actionRead\": \"Read\",\n    \"actionRemove\": \"Remove\",\n    \"actionSave\": \"Save\",\n    \"actionSearch\": \"Search\",\n    \"actionSignIn\": \"Sign in\",\n    \"actionSignOut\": \"Sign out\",\n    \"actionUndo\": \"Undo\",\n    \"actionUse\": \"Use\",\n    \"fieldEmail\": \"Email\",\n    \"fieldPassword\": \"Password\",\n    \"fieldProgress\": \"Progress\",\n    \"fieldFolder\": \"Folder\",\n    \"fieldLanguage\": \"Language\",\n    \"fieldScore\": \"Score\",\n    \"fieldNote\": \"Note\",\n    \"fieldStartDate\": \"Start date\",\n    \"fieldFinishDate\": \"Finish date\",\n    \"fieldRereads\": \"Rereads\",\n    \"fieldTags\": \"Tags\",\n    \"placeholderNone\": \"none\",\n    \"chipNone\": \"None\",\n    \"untitled\": \"(untitled)\",\n    \"signedInAs\": \"Signed in as\",\n    \"statusSaved\": \"Saved ✓\",\n    \"statusSaving\": \"Saving…\",\n    \"statusConnected\": \"Connected ✓\",\n    \"statusSyncing\": \"Syncing…\",\n    \"statusSynced\": \"Synced ✓\",\n    \"statusChecking\": \"Checking…\",\n    \"statusSearching\": \"Searching…\",\n    \"folder_reading\": \"Reading\",\n    \"folder_paused\": \"Paused\",\n    \"folder_plan\": \"Plan\",\n    \"folder_completed\": \"Completed\",\n    \"folder_dropped\": \"Dropped\",\n    \"sort_updated\": \"Recently updated\",\n    \"sort_added\": \"Recently added\",\n    \"sort_title\": \"Title\",\n    \"sort_chapter\": \"Latest chapter\",\n    \"sort_behind\": \"Chapters behind\",\n    \"sort_score\": \"Score\",\n    \"sort_site\": \"Site\",\n    \"modeVertical\": \"Vertical scroll\",\n    \"modeLtr\": \"Pages, left to right\",\n    \"modeSpread\": \"Double page →\",\n    \"modeShortVertical\": \"Long strip\",\n    \"modeShortLtr\": \"Single page →\",\n    \"modeShortSpread\": \"Double page →\",\n    \"modeToastVertical\": \"Long strip — scroll down\",\n    \"modeToastLtr\": \"Single page — left to right →\",\n    \"modeToastSpread\": \"Double page — left to right →\",\n    \"modeToastNovel\": \"Text chapter — scroll down\",\n    \"optionsTitle\": \"PanelFlow settings\",\n    \"optionsAccountLegend\": \"Account & sync\",\n    \"actionForgotPassword\": \"Forgotten your password?\",\n    \"optionsAccountHint\": \"An account carries your library, your progress and your history between this computer, your phone and the web app. Without one, everything stays on this computer — which works, and is a choice.\",\n    \"actionSyncNow\": \"Sync now\",\n    \"optionsUiLanguage\": \"Interface language\",\n    \"optionsLanguageAuto\": \"Same as the browser\",\n    \"optionsLanguageHint\": \"Applies to the popup, the reader and this page. Pages already open keep the old language until they are reloaded.\",\n    \"optionsOpenInApp\": \"Open these settings in the PanelFlow app →\",\n    \"optionsAdvancedLegend\": \"Advanced\",\n    \"optionsApiUrl\": \"API URL\",\n    \"optionsBackendHint\": \"Detection rules, library sync and progress sync are served from here. Point it at <code>http://localhost:8787</code> to work against a backend of your own.\",\n    \"optionsAppearanceLegend\": \"Appearance\",\n    \"optionsTheme\": \"Theme\",\n    \"optionsThemeSystem\": \"Follow the system\",\n    \"optionsThemeLight\": \"Light\",\n    \"optionsThemeDark\": \"Dark\",\n    \"optionsThemeHint\": \"Follows your account: change it here and the website and the phone app change too. “Follow the system” is also an answer — pick it and each device asks the machine it is on. The reader has its own answer below.\",\n    \"optionsReaderLegend\": \"Reader\",\n    \"optionsDefaultMode\": \"Default reading mode\",\n    \"optionsReaderHint\": \"Alt+R shows or hides the reader on any detected page. Everything else — brightness, page gap, strip width — is set in the reader itself, on the chapter you are looking at.\",\n    \"optionsAutoShow\": \"Open the reader by itself on a chapter page\",\n    \"optionsAutoNext\": \"Go on to the next chapter at the end of one\",\n    \"optionsHideRead\": \"Hide chapters already read from the chapter list\",\n    \"optionsReaderDark\": \"Keep the reader dark, whatever the theme\",\n    \"optionsTapZones\": \"Tapping the page turns it\",\n    \"optionsTapSides\": \"On either third of the screen\",\n    \"optionsTapEdges\": \"Only near the edges\",\n    \"optionsTapOff\": \"Never — arrows and scrolling only\",\n    \"optionsTapHint\": \"Narrow edges are for reading with a finger on the screen, where a tap in the middle is usually meant for the panel and not for turning the page.\",\n    \"optionsUpdatesLegend\": \"New chapters\",\n    \"optionsCheckEvery\": \"Look for new chapters\",\n    \"optionsEvery1h\": \"Every hour\",\n    \"optionsEvery3h\": \"Every 3 hours\",\n    \"optionsEvery6h\": \"Every 6 hours\",\n    \"optionsEvery12h\": \"Every 12 hours\",\n    \"optionsEvery24h\": \"Once a day\",\n    \"optionsUpdatesHint\": \"Only while the browser is running. The server keeps watching the sites it can reach on its own, and hands over what it found the next time the extension asks — so checking rarely costs less than it looks.\",\n    \"optionsSitesLegend\": \"Sites\",\n    \"optionsAllSites\": \"Let PanelFlow run on every site\",\n    \"optionsAllSitesHint\": \"PanelFlow only runs on the sites it knows about, and asks for anything else from the toolbar, site by site. Turn this on for a site it has not learned yet, or if covers and chapter downloads fail on a site that otherwise works — those images usually come from a separate domain no site list mentions.\",\n    \"optionsAdblockLegend\": \"Ad blocking\",\n    \"optionsWhitelistLabel\": \"Whitelisted domains (one per line — adblock disabled there)\",\n    \"optionsAdblockHint\": \"A filter list ships with the extension and is replaced by a newer one from the backend when there is one — no extension update needed. Whitelisting a domain covers its subdomains and every request its pages make.\",\n    \"optionsReplayHint\": \"Lost? <a href=\\\"#\\\" id=\\\"replay\\\">Run the setup again</a> — it walks through pinning the button, when the reader opens, and whether to keep a library on this computer or on an account.\",\n    \"welcomeDocTitle\": \"Welcome to PanelFlow\",\n    \"welcomeSkip\": \"Continue without an account for now\",\n    \"welcomeStep1Title\": \"Pin PanelFlow to your toolbar\",\n    \"welcomeStep1Lede\": \"Chrome hides new extensions behind the puzzle-piece icon. Click it, then click the pin next to PanelFlow.\",\n    \"welcomeStep1Hint\": \"That button opens your library, your reading statistics and the list of sites. The reader also opens with <kbd>Alt</kbd> + <kbd>R</kbd> on any page.\",\n    \"welcomeStep1Next\": \"Done, next\",\n    \"welcomeThemeTitle\": \"How should it look?\",\n    \"welcomeThemeHint\": \"The page changes as you choose. Your account keeps this setting, so the website and the mobile app will look the same.\",\n    \"welcomeStep2Title\": \"When should the reader open?\",\n    \"welcomeStep2Lede\": \"PanelFlow works out whether the page you are on is a chapter. You decide what happens next, and you can change it site by site from the toolbar button.\",\n    \"welcomeAutoOnTitle\": \"Automatically\",\n    \"welcomeAutoOnBody\": \"You arrive on a chapter, the reader is already open. Recommended.\",\n    \"welcomeAutoOffTitle\": \"Only when I ask\",\n    \"welcomeAutoOffBody\": \"The page stays as it is until you press <kbd>Alt</kbd>+<kbd>R</kbd> or use the toolbar button.\",\n    \"welcomeDirectionLabel\": \"Default reading direction\",\n    \"welcomeModeVertical\": \"Long strip — scrolls down (webtoons)\",\n    \"welcomeModeLtr\": \"Single page →\",\n    \"welcomeModeSpread\": \"Double page →\",\n    \"welcomeDirectionHint\": \"A site that declares its own direction takes priority, and any change you make inside the reader is remembered.\",\n    \"welcomeStep3Title\": \"Create your account\",\n    \"welcomeStep3Lede1\": \"PanelFlow keeps your library, your progress and your saved chapters on your account. That is what lets you find them again in another browser and in the mobile app.\",\n    \"welcomeStep3Lede2\": \"It is also what lets our server watch your series for new chapters while Chrome is closed. Setup continues as soon as you are signed in.\",\n    \"welcomeHaveAccount\": \"I already have an account\",\n    \"welcomeLibraryFollows\": \". Your library now follows you.\",\n    \"welcomeStep4Title\": \"You are all set\",\n    \"welcomeStep4Lede\": \"PanelFlow has no catalogue of its own: it reads the site you are already on. Go to the scan sites you normally use and open a chapter.\",\n    \"welcomeStep4Lede2\": \"If it is recognised, the reader opens or the PanelFlow button appears on the page: you will know straight away whether a site works. <kbd>Alt</kbd>+<kbd>R</kbd> opens it at any time too.\",\n    \"welcomeStep4Hint\": \"Support is not a fixed list: PanelFlow recognises most sites on its own and ships tuned rules for the main ones. If a site is not detected, report it from the extension menu.\",\n    \"authContacting\": \"Contacting the server…\",\n    \"authNoAnswer\": \"No answer from the server — check the API URL in the options.\",\n    \"popupOpenApp\": \"Open PanelFlow\",\n    \"popupCompatibleSites\": \"Compatible sites\",\n    \"popupReadingStats\": \"Reading stats\",\n    \"navTrackers\": \"Trackers\",\n    \"popupSavedChapters\": \"Saved chapters\",\n    \"popupAddToLibrary\": \"Add to library\",\n    \"popupShowReader\": \"Show reader\",\n    \"popupHideReader\": \"Hide reader\",\n    \"popupReportIssue\": \"Report an issue\",\n    \"navLibrary\": \"Library\",\n    \"popupGroupRecent\": \"Recently read\",\n    \"popupGroupRecentWatched\": \"Recently watched\",\n    \"popupGroupNovels\": \"Light novels\",\n    \"popupGroupWebtoons\": \"Webtoons\",\n    \"popupGroupDefaults\": \"Default settings\",\n    \"popupPanelInfo\": \"Info\",\n    \"popupChapters\": \"Chapters\",\n    \"searchLibrary\": \"Search your library…\",\n    \"popupSortOrder\": \"Order\",\n    \"actionReverseOrder\": \"Reverse the order\",\n    \"popupOnlyOneTag\": \"Show only one tag\",\n    \"popupUnread\": \"Unread\",\n    \"popupUnreadTitle\": \"Only series with a chapter you have not read\",\n    \"popupAllTags\": \"All tags\",\n    \"popupLibraryEmpty\": \"Nothing yet. Add entries to your library from any compatible site.\",\n    \"statsNothingRead\": \"Nothing read yet.\",\n    \"popupAutoShowReader\": \"Auto show reader\",\n    \"switchOn\": \"ON\",\n    \"switchOff\": \"OFF\",\n    \"switchDefault\": \"DEFAULT\",\n    \"actionCheckNow\": \"Check for new chapters\",\n    \"navSettings\": \"Settings\",\n    \"navSites\": \"Sites\",\n    \"webSitesTitle\": \"Where you read\",\n    \"webSitesLede\": \"PanelFlow has no catalogue of its own: it reads the site you are already on. These are the sites it ships tuned rules for, and it detects most others by itself. Star the ones you use and they come first here and in the extension.\",\n    \"webSitesYours\": \"Your sites\",\n    \"webSitesAll\": \"All sites\",\n    \"webSitesPin\": \"Add to your sites\",\n    \"webSitesUnpin\": \"Remove from your sites\",\n    \"webSitesUnavailable\": \"The list could not be loaded. PanelFlow still recognises chapters on its own.\",\n    \"popupSearchSites\": \"Search sites…\",\n    \"popupSitesLede\": \"PanelFlow has no fixed list — any page that looks like a chapter is detected automatically, so a site does not need to appear here to work. These are the ones it already knows about.\",\n    \"popupNoMatch\": \"No match.\",\n    \"statsLast30Days\": \"Last 30 days\",\n    \"statsMostRead\": \"Most read\",\n    \"statsReadingLog\": \"Reading log\",\n    \"trackerLinkedSeries\": \"Linked series\",\n    \"trackerFindOnTracker\": \"Find it on the tracker\",\n    \"popupSearchTracker\": \"Search the tracker…\",\n    \"trackerNeverSend\": \"Never send this one\",\n    \"trackerNoResults\": \"Nothing came back for that.\",\n    \"popupLocalOnly\": \"no account — sign in\",\n    \"popupIntroWhat\": \"PanelFlow spots chapters on scan sites and opens them in a reader of its own — one set of controls, whatever the site looks like.\",\n    \"popupIntroWhere\": \"On a chapter page the reader button appears on the page itself, and <kbd>Alt+R</kbd> opens it from anywhere. This window is the PanelFlow icon in the toolbar — pin it and it stays there.\",\n    \"popupIntroAccount\": \"Your library, your place in each chapter and your statistics are kept on your account, so you find them again on another computer and in the mobile app. <a href=\\\"#\\\" id=\\\"intro-account\\\">Create an account</a> to get started.\",\n    \"popupIntroDismiss\": \"Got it\",\n    \"actionRemoveFromLibrary\": \"Remove from library\",\n    \"popupRemoveFromHistory\": \"Remove from history\",\n    \"popupBadgeTuned\": \"tuned rules\",\n    \"popupBadgeInLibrary\": \"in library\",\n    \"popupNoAccountBody\": \"Your library is on this computer only, and new chapters are not being watched for while Chrome is closed. An account fixes both.\",\n    \"popupNoAccountAction\": \"Create one\",\n    \"popupBadgeFavourite\": \"favourite\",\n    \"standUnread\": \"Not caught up — there is something here to read\",\n    \"standReading\": \"Part-way through a chapter\",\n    \"standRead\": \"Caught up\",\n    \"pageStateNoTab\": \"No page in front — open a chapter first.\",\n    \"pageStateScheme\": \"PanelFlow does not run on browser pages.\",\n    \"pageStateUnreachable\": \"PanelFlow has not loaded on this tab — reload it.\",\n    \"pageStateUndetected\": \"No chapter found on this page.\",\n    \"pageStateUngranted\": \"PanelFlow does not run on this site yet — turn it on here.\",\n    \"chapterBadge\": \"Ch.$1\",\n    \"chapterN\": \"Ch. $1\",\n    \"chaptersShort\": \"$1 ch.\",\n    \"sinceDate\": \"since $1\",\n    \"actionReadChapter\": \"Read $1\",\n    \"actionContinueChapter\": \"Continue — $1\",\n    \"actionOpenSeriesPage\": \"Open the series page\",\n    \"badgeNewChapter\": \"New · $1\",\n    \"badgeNew\": \"New\",\n    \"agoNow\": \"now\",\n    \"agoMinutes\": \"$1m ago\",\n    \"agoHours\": \"$1h ago\",\n    \"agoDays\": \"$1d ago\",\n    \"agoMonths\": \"$1mo ago\",\n    \"dayToday\": \"Today\",\n    \"dayYesterday\": \"Yesterday\",\n    \"durationMinutes\": \"$1 min\",\n    \"durationHours\": \"$1h $2\",\n    \"toastReadingPage\": \"Reading page…\",\n    \"toastNotReadable\": \"Not a readable page — reload it and retry.\",\n    \"toastCouldNotRead\": \"Could not read this page.\",\n    \"toastWakingUp\": \"The extension is still waking up — try again.\",\n    \"toastConnecting\": \"Connecting…\",\n    \"toastBackendDown\": \"$1 is not responding — check the API URL in the options.\",\n    \"trackerAsking\": \"Asking your account…\",\n    \"trackerUnreachable\": \"Could not reach your account.\",\n    \"trackerConnectedAs\": \"Connected as $1\",\n    \"trackerConnected\": \"Connected\",\n    \"trackerNotConnected\": \"Not connected\",\n    \"trackerNoCredentials\": \"No credentials on the server\",\n    \"trackerPasswordAuth\": \"Asks for a password rather than a permission page — not supported\",\n    \"trackerSendAll\": \"Send all\",\n    \"trackerImportPending\": \"Import $1 + $2\",\n    \"trackerImportList\": \"Import list\",\n    \"trackerUpToChapter\": \" · up to ch. $1\",\n    \"trackerSending\": \"Sending your library to $1…\",\n    \"trackerSent\": \"$1 sent\",\n    \"trackerSkipped\": \"$1 skipped\",\n    \"trackerFailed\": \"$1 failed\",\n    \"trackerRemaining\": \"$1 left — press again\",\n    \"trackerNothingMissing\": \"$1 has nothing this library is missing\",\n    \"trackerImportPreview\": \"$1 to add · $2 to fill in — press again\",\n    \"trackerImportDone\": \"$1 added · $2 filled in\",\n    \"trackerNotListening\": \" — nothing is sent to it yet\",\n    \"trackerRefusing\": \"Refusing what PanelFlow sends: $1\",\n    \"trackerFetch\": \"Fetch\",\n    \"trackerFetching\": \"Reading what $1 already has…\",\n    \"trackerFetched\": \"$1 series brought up to date\",\n    \"trackerFetchedAhead\": \"$1 brought up to date · $2 further along there than here\",\n    \"statsLoadError\": \"Statistics could not be loaded: $1. What you read on this device is kept below.\",\n    \"statsLocalOnly\": \"Counted on this device. An account adds up every device you read on — and keeps more than the last few thousand chapters.\",\n    \"statChaptersRead\": \"Chapters read\",\n    \"statTimeRead\": \"Time read\",\n    \"statSeriesRead\": \"Series read\",\n    \"statPerReadingDay\": \"Per reading day\",\n    \"statCurrentStreak\": \"Current streak\",\n    \"statLongestStreak\": \"Longest streak\",\n    \"statInLibrary\": \"In library\",\n    \"statAverageOfN\": \"Average of $1 scores\",\n    \"statAverageScore\": \"Average score\",\n    \"statDays\": \"$1 d\",\n    \"reportWhatWentWrong\": \"What went wrong:\",\n    \"readerClose\": \"Close (Esc)\",\n    \"readerPrevChapter\": \"Previous chapter\",\n    \"readerNextChapter\": \"Next chapter\",\n    \"readerChapters\": \"Chapters\",\n    \"readerChaptersTitle\": \"Chapters (C)\",\n    \"readerDownloadCbz\": \"Download chapter (.cbz)\",\n    \"readerDownloadTxt\": \"Download chapter (.txt)\",\n    \"readerSaveOffline\": \"Save for offline reading\",\n    \"readerSavedOffline\": \"Saved offline — click to remove\",\n    \"readerNotSaved\": \"Not saved — $1\",\n    \"readerPrefs\": \"Reader preferences (S)\",\n    \"readerNeedsAccess\": \"PanelFlow is not allowed to read the pages from $1. Grant it in the settings to download this chapter.\",\n    \"readerGrantAccess\": \"Open settings\",\n    \"readerNotDownloaded\": \"Could not download — $1\",\n    \"readerResetZoom\": \"Reset zoom (0)\",\n    \"readerFullscreen\": \"Full screen (F)\",\n    \"readerHelpTitle\": \"Keys and gestures (?)\",\n    \"readerHideControls\": \"Hide controls (H)\",\n    \"readerCurrentPage\": \"Current page\",\n    \"readerBreakFirst\": \"Break 1st page (B)\",\n    \"readerAutoPlay\": \"Auto play (long strip)\",\n    \"readerReadingMode\": \"Reading mode\",\n    \"readerBrightness\": \"Brightness\",\n    \"readerContrast\": \"Contrast\",\n    \"readerGapSize\": \"Gap size\",\n    \"readerStripWidth\": \"Strip width\",\n    \"readerTextSize\": \"Text size\",\n    \"readerLineSpacing\": \"Line spacing\",\n    \"readerTextWidth\": \"Text width\",\n    \"readerPlaySpeed\": \"Play speed\",\n    \"readerProgressSize\": \"Progress size\",\n    \"readerTapZones\": \"Tap zones\",\n    \"readerTapSides\": \"Left / right thirds\",\n    \"readerTapEdges\": \"Narrow edges\",\n    \"readerTapOff\": \"Off (keys only)\",\n    \"readerSwapTap\": \"Swap tap sides\",\n    \"readerAutoNext\": \"Auto next chapter\",\n    \"readerHideRead\": \"Hide chapters I've read\",\n    \"readerHiddenOne\": \"— $1 read chapter hidden —\",\n    \"readerHiddenMany\": \"— $1 read chapters hidden —\",\n    \"readerHelpHead\": \"Keys and gestures\",\n    \"readerHelpTap\": \"<b>Tap</b> a side to turn the page, the middle for the controls — long strip scrolls instead\",\n    \"readerHelpPinch\": \"<b>Pinch</b> or <b>ctrl+wheel</b> to zoom, <b>drag</b> to move — the view never snaps back\",\n    \"readerHelpDoubleTap\": \"<b>Double tap</b> to zoom in on a panel, again to fit the page\",\n    \"readerHelpArrows\": \"<b>←</b> <b>→</b> <b>space</b> turn the page · <b>↑</b> <b>↓</b> scroll the long strip\",\n    \"readerHelpWheel\": \"<b>C</b> the chapter wheel — <b>scroll</b> or <b>↑</b> <b>↓</b> to find one, <b>enter</b> to open it\",\n    \"readerHelpKeys1\": \"<b>S</b> preferences · <b>B</b> break the first page · <b>0</b> reset zoom\",\n    \"readerHelpKeys2\": \"<b>F</b> full screen · <b>H</b> hide the controls · <b>?</b> this list · <b>Esc</b> close\",\n    \"readerHelpNote\": \"Reading direction, tap zones and everything else live under ⚙.\",\n    \"readerResetDefaults\": \"Back to default settings\",\n    \"readerHelpToggleChrome\": \"Tap the middle of the page to hide the controls — and again to bring them back.\",\n    \"readerSeriesPrefs\": \"Keep these for this series\",\n    \"readerSeriesOn\": \"These settings now follow this series\",\n    \"readerSeriesOff\": \"Back to your usual settings\",\n    \"readerEndTitle\": \"End of chapter\",\n    \"readerEndOf\": \"End of $1\",\n    \"readerEndThis\": \"this chapter\",\n    \"readerEndNext\": \"Next chapter\",\n    \"readerEndStay\": \"Stay here\",\n    \"readerEndMarkRead\": \"Mark as read\",\n    \"readerEndMarked\": \"Marked as read\",\n    \"readerEndCaughtUp\": \"That is the newest chapter here.\",\n    \"readerEndLeftOne\": \"1 more chapter in this list.\",\n    \"readerEndLeftMany\": \"$1 more chapters in this list.\",\n    \"zoneBack\": \"Back\",\n    \"zoneNext\": \"Next\",\n    \"zoneControls\": \"Controls\",\n    \"zoneControlsKeys\": \"Controls — keys turn the page\",\n    \"pillReaderMode\": \"Reader Mode\",\n    \"pillReaderModeTitle\": \"PanelFlow: open this chapter in Reader Mode\",\n    \"pillAddAnime\": \"Add to library\",\n    \"pillAddAnimeTitle\": \"Track this anime in PanelFlow\",\n    \"pillHideControls\": \"Hide these controls\",\n    \"pillShowControls\": \"PanelFlow controls\",\n    \"modalAlreadyInLibrary\": \"Already in your library\",\n    \"modalSameTitleLead\": \"You already follow this series on another site.\",\n    \"modalLikelyLead\": \"This looks like a series you already follow.\",\n    \"modalInYourLibrary\": \"In your library\",\n    \"modalThisPage\": \"This page\",\n    \"modalCloseTitlesWarning\": \"The titles are close but not identical — check this is really the same series before migrating.\",\n    \"modalKeepsProgress\": \"Your progress, score, notes and tags\",\n    \"modalKeepsDate\": \"The date you added it\",\n    \"modalKeepsOldSite\": \"The old site, kept in the entry’s history\",\n    \"modalMigrating\": \"Migrating…\",\n    \"modalMigrationFailed\": \"Migration failed.\",\n    \"modalAddSeparately\": \"Add as a separate series\",\n    \"modalEditEntry\": \"Edit library entry\",\n    \"modalTranslatedLanguage\": \"Translated language\",\n    \"modalCurrentProgress\": \"Current progress\",\n    \"modalAddTagHint\": \"Add a tag and press Enter\",\n    \"modalLocalOnlyHint\": \"Saved on this device only — sign in from the PanelFlow extension settings to see it in the web app.\",\n    \"modalConnectTrackerLead\": \"Connect a tracker to fill this in from your own list:\",\n    \"modalConnectTrackerHint\": \"Connect AniList or MyAnimeList in the PanelFlow web app to fill this in from your own list.\",\n    \"modalTrackerConnect\": \"Connect $1\",\n    \"modalTrackerMatchedAs\": \"matched as “$1”\",\n    \"modalTrackerUnreachable\": \"$1 could not be reached — $2\",\n    \"modalTrackerNotListed\": \"Not on your $1 list yet.\",\n    \"modalTrackerAdd\": \"Add\",\n    \"modalTrackerAdding\": \"Adding…\",\n    \"modalTrackerAdded\": \"Added to $1 at chapter $2.\",\n    \"modalTrackerAddedPlain\": \"Added to $1.\",\n    \"modalTrackerAhead\": \"$1 is already at this chapter or further.\",\n    \"modalTrackerPickSeries\": \"$1 does not know this title. Pick the right series:\",\n    \"modalTrackerNoHits\": \"$1 found nothing for “$2”.\",\n    \"modalTrackerFailed\": \"$1 refused it — $2\",\n    \"modalTrackerNoAnswer\": \"no answer\",\n    \"modalNoTitle\": \"Could not read a title on this page.\",\n    \"offlineDocTitle\": \"PanelFlow — Saved chapters\",\n    \"offlineEmpty\": \"Nothing saved yet. Open a chapter, then press <code>📥</code> in the reader's side bar to keep it on this device.\",\n    \"offlineChapterOne\": \"$1 chapter\",\n    \"offlineChapterMany\": \"$1 chapters\",\n    \"offlinePageOne\": \"$1 page\",\n    \"offlinePageMany\": \"$1 pages\",\n    \"offlineParagraphOne\": \"$1 paragraph\",\n    \"offlineParagraphMany\": \"$1 paragraphs\",\n    \"offlineUsage\": \"$1 · $2 on this device · kept for $3 days\",\n    \"offlineExpiresToday\": \"expires today\",\n    \"offlineExpiresIn\": \"expires in $1 days\",\n    \"offlineUnknownSeries\": \"Unknown series\",\n    \"notifyNewChapterTitle\": \"New chapter!\",\n    \"notifyNewChapterBody\": \"$1 — chapter $2 is out on $3\",\n    \"navUpdates\": \"Updates\",\n    \"navStatistics\": \"Statistics\",\n    \"navHistory\": \"History\",\n    \"webTagline\": \"Your manga library, everywhere.\",\n    \"webForgotLede\": \"We'll email you a link to choose a new password.\",\n    \"webSendTheLink\": \"Send the link\",\n    \"webBackToSignIn\": \"Back to sign in\",\n    \"webResetLede\": \"Choose a new password.\",\n    \"webNewPassword\": \"New password\",\n    \"webChangePassword\": \"Change my password\",\n    \"webPushTest\": \"Send yourself a test notification\",\n    \"webTryAgain\": \"Try again\",\n    \"libraryContinueReading\": \"Continue reading\",\n    \"webImport\": \"Import\",\n    \"webImportTitle\": \"Import a list from AniList or MyAnimeList\",\n    \"webExport\": \"Export\",\n    \"webExportTitle\": \"Download a backup, or restore one\",\n    \"webMoveSite\": \"Move site\",\n    \"webMoveSiteTitle\": \"Move every series off a dead site\",\n    \"webAddSeries\": \"Add series\",\n    \"webSort\": \"Sort\",\n    \"webUnreadOnly\": \"Unread only\",\n    \"webLibraryEmpty\": \"Nothing here yet. Add a series, or pin one from the PanelFlow extension while browsing your favorite manga site.\",\n    \"webNewChapters\": \"New chapters\",\n    \"webCheckUpdatesTitle\": \"Re-scan every series for new chapters\",\n    \"webUpdatesEmpty\": \"Nothing new. Every series you are following is read up to its latest chapter.\",\n    \"webReadingStatistics\": \"Reading statistics\",\n    \"webStatsEmpty\": \"Nothing read yet. Reader Mode in the extension records reading time as you go.\",\n    \"webLibraryBreakdown\": \"Library breakdown\",\n    \"webReadingHistory\": \"Reading history\",\n    \"webClearHistory\": \"Clear history\",\n    \"webClearHistoryTitle\": \"Delete every recorded read\",\n    \"webConnectedAccounts\": \"Connected accounts\",\n    \"webTrackersLede\": \"Every chapter you finish is sent to the accounts below. PanelFlow only ever writes the chapter count, and only ever forwards.\",\n    \"webNoLinksYet\": \"Nothing linked yet. A series finds itself on the tracker the first time you read a chapter of it.\",\n    \"webAppearance\": \"Appearance\",\n    \"webThemeHint\": \"Follows your account: change it here and the extension and the phone app change too. If the desk you read at and the phone in your pocket do not want the same answer at the same hour, that is what “Follow the system” is for — it is a choice, and each device asks the machine it is on.\",\n    \"webLanguageHint\": \"Follows your account, and applies here as much as in the extension and the phone app.\",\n    \"navAccount\": \"Account\",\n    \"webAccountHint\": \"Your library, your progress and your trackers follow this address.\",\n    \"webPasswordHint\": \"We send a link to your address, and the link is what changes the password — so a browser someone else left signed in cannot change it.\",\n    \"webEmailMeALink\": \"Email me a link\",\n    \"webReading\": \"Reading\",\n    \"webNoExtension\": \"These settings live in the PanelFlow extension, which this browser does not have. Install it and this section fills in.\",\n    \"webReadingDirectionHint\": \"How a chapter opens, unless the site says otherwise.\",\n    \"webCheckEveryHint\": \"How often the extension looks, in the background.\",\n    \"webWhitelist\": \"Sites to leave alone\",\n    \"webWhitelistHint\": \"One domain per line. Ads are not blocked on these — for a site that breaks when they are.\",\n    \"webFieldTitle\": \"Title\",\n    \"webFieldSeriesUrl\": \"Series URL\",\n    \"webFieldCoverUrl\": \"Cover image URL\",\n    \"webFieldCoverHint\": \"(auto-filled from the page)\",\n    \"webFieldStatus\": \"Status\",\n    \"webFieldPublication\": \"Publication\",\n    \"webOngoing\": \"Ongoing\",\n    \"webFinished\": \"Finished\",\n    \"webLangJa\": \"Japanese (manga)\",\n    \"webLangKo\": \"Korean (manhwa)\",\n    \"webLangZh\": \"Chinese (manhua)\",\n    \"webLangEn\": \"English\",\n    \"webLangFr\": \"French\",\n    \"webLangEs\": \"Spanish\",\n    \"webStarted\": \"Started\",\n    \"webCommaSeparated\": \"(comma separated)\",\n    \"webTagsPlaceholder\": \"romance, school\",\n    \"webNotePlaceholder\": \"Anything you want to remember\",\n    \"webReadingProgress\": \"Reading progress\",\n    \"webFieldChapter\": \"Chapter\",\n    \"webChapterPlaceholder\": \"Chapter 42\",\n    \"webFieldPage\": \"Page\",\n    \"webFieldChapterUrl\": \"Chapter URL\",\n    \"webImportList\": \"Import a list\",\n    \"webImportLede\": \"Adds what is missing and fills in blanks. Nothing already in your library is overwritten.\",\n    \"webFromConnected\": \"From an account you have connected:\",\n    \"webOr\": \"or\",\n    \"webAnilistUsername\": \"AniList username\",\n    \"webYourName\": \"yourname\",\n    \"webMalExport\": \"MyAnimeList export\",\n    \"webMalExportHint\": \"(the .xml from Export)\",\n    \"webClose\": \"Close\",\n    \"webPreview\": \"Preview\",\n    \"webExportLede\": \"The backup holds everything — shelf, bookmarks and reading history — and is the only format that can be restored here.\",\n    \"webBackupJson\": \"Backup (.json)\",\n    \"webSpreadsheetCsv\": \"Spreadsheet (.csv)\",\n    \"webRestoreBackup\": \"Restore a backup\",\n    \"webRestoreLede\": \"Fills in what is missing. Nothing already in your library is overwritten.\",\n    \"webPanelflowBackup\": \"PanelFlow backup\",\n    \"webPanelflowBackupHint\": \"(the .json above)\",\n    \"webPreviewRestore\": \"Preview restore\",\n    \"webRestore\": \"Restore\",\n    \"webMoveWholeSite\": \"Move a whole site\",\n    \"webLeaving\": \"Leaving\",\n    \"webGoingTo\": \"Going to\",\n    \"webGoingToHint\": \"(domain of the new site)\",\n    \"webFindMatches\": \"Find matches\",\n    \"webMoveSelected\": \"Move selected\",\n    \"webYourShelves\": \"Your shelves\",\n    \"webShelvesLede\": \"A shelf is a tab of your own. It still counts as one of the five reading statuses — that is what decides whether PanelFlow watches those series for new chapters, and what they export as.\",\n    \"webNewShelfPlaceholder\": \"New shelf — “Weekly”, “With my brother”…\",\n    \"webAdd\": \"Add\",\n    \"webAlreadyRegistered\": \"Already registered?\",\n    \"webNoAccountYet\": \"No account yet?\",\n    \"webCreateOne\": \"Create one\",\n    \"webForgotSent\": \"If that address has an account, a link is on its way.\",\n    \"webLinkOnItsWay\": \"A link is on its way to your inbox.\",\n    \"webPasswordChanged\": \"Password changed — sign in with your new one.\",\n    \"webLoading\": \"Loading…\",\n    \"webLoadFailed\": \"Could not load your library\",\n    \"actionResume\": \"Resume\",\n    \"webNotStarted\": \"Not started\",\n    \"webEditDetails\": \"Edit details\",\n    \"webEditSeries\": \"Edit series\",\n    \"webSetProgress\": \"Set reading progress\",\n    \"webCouldNotRemove\": \"Could not remove $1\",\n    \"webCouldNotMove\": \"Could not move $1\",\n    \"webChaptersAhead\": \"The latest chapter is $1 ahead of your bookmark\",\n    \"webYourScore\": \"Your score: $1/10\",\n    \"webReadThrough\": \"Read through $1 times\",\n    \"webNewChapter\": \"New chapter\",\n    \"webNewChapterOut\": \"A chapter you have not read yet is out\",\n    \"webOneNewChapter\": \"$1 new chapter\",\n    \"webNNewChapters\": \"$1 new chapters\",\n    \"webCheckingAll\": \"Checking every series…\",\n    \"webNoNewChapters\": \"No new chapters ($1 series checked)\",\n    \"webOneHasNew\": \"$1 series has new chapters!\",\n    \"webNHaveNew\": \"$1 series have new chapters!\",\n    \"webSortAscending\": \"Ascending — click for descending\",\n    \"webSortDescending\": \"Descending — click for ascending\",\n    \"webOnlyTagged\": \"Show only series tagged $1\",\n    \"webShownOfTotal\": \"$1 of $2\",\n    \"webMakeShelf\": \"Make a shelf of your own\",\n    \"webNoShelves\": \"No shelves yet.\",\n    \"webMoveUp\": \"Move up\",\n    \"webShelfCountsAs\": \"Your shelf — counts as $1\",\n    \"webShelfStatusHint\": \"What this shelf counts as: whether its series are watched for new chapters, and what they export as\",\n    \"webShelfRemoveHint\": \"Remove this shelf — its series move to $1\",\n    \"webShelfMovesOne\": \"$1 series moves to $2.\",\n    \"webShelfMovesN\": \"$1 series move to $2.\",\n    \"webRemoveShelf\": \"Remove the shelf “$1”?\",\n    \"webFetchingPage\": \"Fetching page info…\",\n    \"webPageUnreadable\": \"Could not read the page ($1) — fill in manually.\",\n    \"webClearHistoryConfirm\": \"Delete every recorded read? Your library and bookmarks are not touched.\",\n    \"webClearHistoryFailed\": \"Could not clear your history\",\n    \"webReadingList\": \"Reading the list…\",\n    \"webNothingWritten\": \"Nothing has been written yet.\",\n    \"webImporting\": \"Importing…\",\n    \"webReadingBackup\": \"Reading the backup…\",\n    \"webRestoring\": \"Restoring…\",\n    \"webMigrateSearching\": \"Searching the new site for each series — this takes a while…\",\n    \"webSendingTest\": \"Sending a test notification…\",\n    \"webExtensionSilent\": \"The extension did not answer — try again.\",\n    \"webSavedHereOnly\": \"Saved here, but the account did not hear about it.\",\n    \"webTrackerRefusingFix\": \". Disconnecting and connecting again usually fixes it.\",\n    \"webEverySite\": \"Every site ($1 series)\",\n    \"badgeNewChapterNo\": \"New · ch. $1\",\n    \"folder_all\": \"All\",\n    \"agoWeeks\": \"$1w ago\",\n    \"agoSomeTime\": \"at some point\",\n    \"durationSeconds\": \"$1s\",\n    \"statReadingSince\": \"Reading since\",\n    \"webSessionExpired\": \"session expired\",\n    \"webRequestFailed\": \"request failed ($1)\",\n    \"webOneBehind\": \"$1 chapter behind\",\n    \"webNBehind\": \"$1 chapters behind\",\n    \"webFinishedHint\": \"The series itself is finished\",\n    \"webOngoingHint\": \"The series is still being published\",\n    \"webCoverFound\": \"cover found\",\n    \"webNoCoverFound\": \"no cover found\",\n    \"webLatestChapter\": \"latest chapter: $1\",\n    \"webChartDay\": \"$1 · $2 ch · $3\",\n    \"webChartNothing\": \"$1 · nothing read\",\n    \"statChaptersAndTime\": \"$1 chapters · $2\",\n    \"webShelves\": \"Shelves\",\n    \"trackerSendMyLibrary\": \"Send my library now\",\n    \"trackerSendMyLibraryHint\": \"Bring the tracker up to date with every bookmark you already have\",\n    \"trackerFetchAll\": \"Fetch what it already has\",\n    \"trackerFetchAllHint\": \"Read your chapter counts back from the tracker, so PanelFlow stops below what you have already read there. Your bookmarks are not touched.\",\n    \"trackerLastUpdate\": \" — last update $1\",\n    \"trackerWaitingFor\": \"Waiting for $1…\",\n    \"trackerDisconnectConfirm\": \"Disconnect $1?\\n\\nNothing is removed from your tracker. PanelFlow forgets which series matched which, so reconnecting starts the matching over.\",\n    \"trackerNoMatch\": \"$1 · no match found — pick it yourself\",\n    \"trackerRematchHint\": \"Match this one again from scratch on the next chapter you read\",\n    \"webPopupBlocked\": \"the browser blocked the window — allow popups for this site\",\n    \"webNoGunzip\": \"this browser cannot unzip .gz — unzip the export first\",\n    \"webNotJson\": \"that file is not JSON\",\n    \"webImportNeedsSource\": \"give an AniList username or pick a MyAnimeList export\",\n    \"webExportFailed\": \"export failed ($1)\",\n    \"webEntriesInList\": \"$1 entries in the list\",\n    \"webPlanCounts\": \"$1 to add · $2 to fill in · $3 already up to date\",\n    \"webPlanBookmarks\": \"$1 will get a chapter bookmark\",\n    \"webPlanHistory\": \"$1 bookmarks · $2 reads in the history\",\n    \"webFilledIn\": \"Filled in\",\n    \"webImportDone\": \"Done — $1 added, $2 updated.\",\n    \"webMigrateFound\": \"$1 of $2 found on $3\",\n    \"webMigrateTruncated\": \"the search ran out of time — run it again for the rest\",\n    \"webMigrateSkipped\": \"$1 not searched\",\n    \"webMoving\": \"Moving $1 series…\",\n    \"webMoved\": \"$1 moved\",\n    \"webMoveRefused\": \" · $1 refused: $2\",\n    \"webPushBlocked\": \"This browser is blocking notifications for PanelFlow — allow them in the site settings\",\n    \"webPushOnHint\": \"New chapters are announced even when PanelFlow is closed. Click to stop.\",\n    \"webPushOffHint\": \"Be told about new chapters even when PanelFlow is closed\",\n    \"webPushTookOne\": \"$1 browser took it\",\n    \"webPushTookN\": \"$1 browsers took it\",\n    \"webPushExpiredOne\": \"$1 subscription had expired and was removed\",\n    \"webPushExpiredN\": \"$1 subscriptions had expired and were removed\",\n    \"webPushUnreachableOne\": \"$1 browser could not be reached — try again later\",\n    \"webPushUnreachableN\": \"$1 browsers could not be reached — try again later\",\n    \"webPushKeysHint\": \". If no notification appears, the server’s keys are wrong.\",\n    \"navSearch\": \"Search\",\n    \"navStats\": \"Stats\",\n    \"badgeNNew\": \"$1 new\",\n    \"searchNoResults\": \"No results.\",\n    \"statusRemoved\": \"Removed\",\n    \"statusSignedIn\": \"Signed in\",\n    \"statusUpToDate\": \"Up to date\",\n    \"mobileLibraryEmpty\": \"Nothing here yet. Search for a series, open it, and add it from the reader.\",\n    \"mobileNothingFiled\": \"Nothing filed under “$1” yet.\",\n    \"mobileSearchPlaceholder\": \"Search the web for a series\",\n    \"mobileGo\": \"Go\",\n    \"mobileScansOnly\": \"Bias toward reading sites\",\n    \"mobileSearchKind\": \"Prefer sites for\",\n    \"mobileSearchHint\": \"PanelFlow does not host or index anything. This is a plain web search over your own words — each result is checked to tell you whether Reader Mode will work there before you open it.\",\n    \"mobileSearchWebHint\": \"PanelFlow hosts and indexes nothing. This runs your own words as an ordinary web search, in the app’s own browser — a result that turns out to be a chapter is recognised the moment it opens.\",\n    \"mobileMediumManga\": \"Manga\",\n    \"mobileMediumAnime\": \"Anime\",\n    \"mobileLibraryCount\": \"$1 series in your library\",\n    \"mobileSearchFailed\": \"Search failed: $1\",\n    \"mobileSearchNeedsAccount\": \"Search runs on the PanelFlow backend — sign in on the Account tab first.\",\n    \"mobileVerdictReady\": \"Reader Mode works here\",\n    \"mobileVerdictLikely\": \"Reader Mode will probably work\",\n    \"mobileVerdictUnknown\": \"Not sure until it loads\",\n    \"mobileVerdictUnlikely\": \"Reader Mode probably will not work\",\n    \"mobileLatestCh\": \"latest Ch. $1\",\n    \"mobileYouAreOn\": \"you are on $1\",\n    \"mobileNotStarted\": \"not started\",\n    \"mobileReadNew\": \"Read $1 — new\",\n    \"mobileFindElsewhere\": \"Find it on another site\",\n    \"mobileChartDay\": \"$1 — $2\",\n    \"mobileStatsSignedOut\": \"Statistics live on your account — sign in on the Account tab to see them. What you read on this phone is kept below either way.\",\n    \"mobileSignedInAs\": \"Signed in as \",\n    \"mobileMergeDuplicates\": \"Merge duplicate entries\",\n    \"mobileMerged\": \"Merged $1 duplicate(s)\",\n    \"mobileNoDuplicates\": \"No duplicates found\",\n    \"mobileAccountHint\": \"Your library works fully offline and signed out. An account only adds sync between your phone and your browser, and powers search.\",\n    \"mobileNoBridge\": \"Opened outside the app: there is no native bridge here, so there is no library to show.\",\n  },\n  \"fr\": {\n    \"extName\": \"PanelFlow\",\n    \"extDescription\": \"Lisez vos mangas sur n’importe quel site dans un lecteur épuré, avec une seule bibliothèque sur tous vos appareils.\",\n    \"commandToggleReader\": \"Afficher ou masquer le lecteur sur cette page\",\n    \"actionBack\": \"Retour\",\n    \"actionCancel\": \"Annuler\",\n    \"actionConnect\": \"Connecter\",\n    \"actionCreateAccount\": \"Créer un compte\",\n    \"actionDisconnect\": \"Déconnecter\",\n    \"actionDone\": \"Terminé\",\n    \"actionGotIt\": \"C’est compris\",\n    \"actionNext\": \"Suivant\",\n    \"actionOpen\": \"Ouvrir\",\n    \"actionRead\": \"Lire\",\n    \"actionRemove\": \"Supprimer\",\n    \"actionSave\": \"Enregistrer\",\n    \"actionSearch\": \"Rechercher\",\n    \"actionSignIn\": \"Se connecter\",\n    \"actionSignOut\": \"Se déconnecter\",\n    \"actionUndo\": \"Annuler\",\n    \"actionUse\": \"Utiliser\",\n    \"fieldEmail\": \"E-mail\",\n    \"fieldPassword\": \"Mot de passe\",\n    \"fieldProgress\": \"Progression\",\n    \"fieldFolder\": \"Dossier\",\n    \"fieldLanguage\": \"Langue\",\n    \"fieldScore\": \"Note\",\n    \"fieldNote\": \"Commentaire\",\n    \"fieldStartDate\": \"Date de début\",\n    \"fieldFinishDate\": \"Date de fin\",\n    \"fieldRereads\": \"Relectures\",\n    \"fieldTags\": \"Tags\",\n    \"placeholderNone\": \"aucun\",\n    \"chipNone\": \"Aucun\",\n    \"untitled\": \"(sans titre)\",\n    \"signedInAs\": \"Connecté en tant que\",\n    \"statusSaved\": \"Enregistré ✓\",\n    \"statusSaving\": \"Enregistrement…\",\n    \"statusConnected\": \"Connecté ✓\",\n    \"statusSyncing\": \"Synchronisation…\",\n    \"statusSynced\": \"Synchronisé ✓\",\n    \"statusChecking\": \"Vérification…\",\n    \"statusSearching\": \"Recherche…\",\n    \"folder_reading\": \"En cours\",\n    \"folder_paused\": \"En pause\",\n    \"folder_plan\": \"À lire\",\n    \"folder_completed\": \"Terminés\",\n    \"folder_dropped\": \"Abandonnés\",\n    \"sort_updated\": \"Mis à jour récemment\",\n    \"sort_added\": \"Ajoutés récemment\",\n    \"sort_title\": \"Titre\",\n    \"sort_chapter\": \"Dernier chapitre\",\n    \"sort_behind\": \"Chapitres de retard\",\n    \"sort_score\": \"Note\",\n    \"sort_site\": \"Site\",\n    \"modeVertical\": \"Défilement vertical\",\n    \"modeLtr\": \"Pages, de gauche à droite\",\n    \"modeSpread\": \"Double page →\",\n    \"modeShortVertical\": \"Bande verticale\",\n    \"modeShortLtr\": \"Page simple →\",\n    \"modeShortSpread\": \"Double page →\",\n    \"modeToastVertical\": \"Bande verticale — faites défiler vers le bas\",\n    \"modeToastLtr\": \"Page simple — de gauche à droite →\",\n    \"modeToastSpread\": \"Double page — de gauche à droite →\",\n    \"modeToastNovel\": \"Chapitre texte — faites défiler vers le bas\",\n    \"optionsTitle\": \"Réglages de PanelFlow\",\n    \"optionsAccountLegend\": \"Compte et synchronisation\",\n    \"actionForgotPassword\": \"Mot de passe oublié ?\",\n    \"optionsAccountHint\": \"Un compte emporte votre bibliothèque, votre progression et votre historique d’un appareil à l’autre : cet ordinateur, votre téléphone, l’appli web. Sans compte, tout reste sur cet ordinateur — ça marche aussi, et c’est un choix.\",\n    \"actionSyncNow\": \"Synchroniser maintenant\",\n    \"optionsUiLanguage\": \"Langue de l’interface\",\n    \"optionsLanguageAuto\": \"Comme le navigateur\",\n    \"optionsLanguageHint\": \"S’applique au panneau, au lecteur et à cette page. Les onglets déjà ouverts gardent l’ancienne langue jusqu’à leur rechargement.\",\n    \"optionsOpenInApp\": \"Ouvrir ces réglages dans l’application PanelFlow →\",\n    \"optionsAdvancedLegend\": \"Avancé\",\n    \"optionsApiUrl\": \"Adresse de l’API\",\n    \"optionsBackendHint\": \"Les règles de détection, la bibliothèque et la progression viennent d’ici. Mettez <code>http://localhost:8787</code> pour travailler contre votre propre serveur.\",\n    \"optionsAppearanceLegend\": \"Apparence\",\n    \"optionsTheme\": \"Thème\",\n    \"optionsThemeSystem\": \"Suivre le système\",\n    \"optionsThemeLight\": \"Clair\",\n    \"optionsThemeDark\": \"Sombre\",\n    \"optionsThemeHint\": \"Suit votre compte : changé ici, changé aussi sur le site et dans l’application mobile. « Suivre le système » est une réponse aussi — choisissez-la et chaque appareil demande à la machine où il tourne. Le lecteur, lui, répond tout seul — voir plus bas.\",\n    \"optionsReaderLegend\": \"Lecteur\",\n    \"optionsDefaultMode\": \"Mode de lecture par défaut\",\n    \"optionsReaderHint\": \"Alt+R affiche ou masque le lecteur sur toute page détectée. Le reste — luminosité, écart entre les pages, largeur de la bande — se règle dans le lecteur lui-même, sur le chapitre que vous avez sous les yeux.\",\n    \"optionsAutoShow\": \"Ouvrir le lecteur tout seul sur une page de chapitre\",\n    \"optionsAutoNext\": \"Enchaîner sur le chapitre suivant à la fin d’un chapitre\",\n    \"optionsHideRead\": \"Masquer les chapitres déjà lus dans la liste des chapitres\",\n    \"optionsReaderDark\": \"Garder le lecteur sombre, quel que soit le thème\",\n    \"optionsTapZones\": \"Toucher la page la tourne\",\n    \"optionsTapSides\": \"Sur un tiers de l’écran de chaque côté\",\n    \"optionsTapEdges\": \"Seulement près des bords\",\n    \"optionsTapOff\": \"Jamais — flèches et défilement uniquement\",\n    \"optionsTapHint\": \"Les bords étroits, c’est pour lire le doigt sur l’écran : là, un appui au milieu vise en général la case, pas la page suivante.\",\n    \"optionsUpdatesLegend\": \"Nouveaux chapitres\",\n    \"optionsCheckEvery\": \"Chercher les nouveaux chapitres\",\n    \"optionsEvery1h\": \"Toutes les heures\",\n    \"optionsEvery3h\": \"Toutes les 3 heures\",\n    \"optionsEvery6h\": \"Toutes les 6 heures\",\n    \"optionsEvery12h\": \"Toutes les 12 heures\",\n    \"optionsEvery24h\": \"Une fois par jour\",\n    \"optionsUpdatesHint\": \"Uniquement quand le navigateur tourne. Le serveur, lui, continue de surveiller les sites qu’il sait atteindre et transmet ce qu’il a trouvé dès que l’extension le lui demande — chercher rarement coûte donc moins cher qu’il n’y paraît.\",\n    \"optionsSitesLegend\": \"Sites\",\n    \"optionsAllSites\": \"Autoriser PanelFlow sur tous les sites\",\n    \"optionsAllSitesHint\": \"PanelFlow ne fonctionne que sur les sites qu’il connaît, et demande le reste depuis la barre d’outils, site par site. Activez ceci pour un site qu’il ne connaît pas encore, ou si les couvertures et les téléchargements de chapitres échouent sur un site qui marche par ailleurs — ces images viennent presque toujours d’un domaine à part qu’aucune liste de sites ne mentionne.\",\n    \"optionsAdblockLegend\": \"Blocage des publicités\",\n    \"optionsWhitelistLabel\": \"Domaines autorisés (un par ligne — le blocage y est désactivé)\",\n    \"optionsAdblockHint\": \"Une liste de filtres est fournie avec l’extension et remplacée par une plus récente venue du serveur dès qu’il y en a une — sans mise à jour de l’extension. Autoriser un domaine couvre ses sous-domaines et toutes les requêtes que ses pages émettent.\",\n    \"optionsReplayHint\": \"Perdu ? <a href=\\\"#\\\" id=\\\"replay\\\">Relancez la configuration</a> — elle reprend l’épinglage du bouton, le moment où le lecteur s’ouvre, et le choix entre une bibliothèque locale et un compte.\",\n    \"welcomeDocTitle\": \"Bienvenue dans PanelFlow\",\n    \"welcomeSkip\": \"Continuer sans compte pour le moment\",\n    \"welcomeStep1Title\": \"Épinglez PanelFlow à votre barre d’outils\",\n    \"welcomeStep1Lede\": \"Chrome regroupe les nouvelles extensions sous l’icône en forme de pièce de puzzle. Cliquez dessus, puis sur l’épingle située à côté de PanelFlow.\",\n    \"welcomeStep1Hint\": \"Ce bouton ouvre votre bibliothèque, vos statistiques de lecture et la liste des sites. Le lecteur s’ouvre aussi avec <kbd>Alt</kbd> + <kbd>R</kbd> sur n’importe quelle page.\",\n    \"welcomeStep1Next\": \"Terminé, suivant\",\n    \"welcomeThemeTitle\": \"Quelle apparence préférez-vous ?\",\n    \"welcomeThemeHint\": \"L’aperçu se met à jour à mesure que vous choisissez. Ce réglage est conservé sur votre compte : le site web et l’application mobile adopteront la même apparence.\",\n    \"welcomeStep2Title\": \"Quand le lecteur doit-il s’ouvrir ?\",\n    \"welcomeStep2Lede\": \"PanelFlow détermine si la page que vous consultez est un chapitre. Vous décidez de ce qui se passe ensuite, et vous pourrez le modifier site par site depuis le bouton de la barre d’outils.\",\n    \"welcomeAutoOnTitle\": \"Automatiquement\",\n    \"welcomeAutoOnBody\": \"Le lecteur s’ouvre dès votre arrivée sur un chapitre. Recommandé.\",\n    \"welcomeAutoOffTitle\": \"Seulement quand je le demande\",\n    \"welcomeAutoOffBody\": \"La page reste inchangée jusqu’à ce que vous utilisiez le raccourci <kbd>Alt</kbd>+<kbd>R</kbd> ou le bouton de la barre d’outils.\",\n    \"welcomeDirectionLabel\": \"Sens de lecture par défaut\",\n    \"welcomeModeVertical\": \"Bande verticale — défile vers le bas (webtoons)\",\n    \"welcomeModeLtr\": \"Page simple →\",\n    \"welcomeModeSpread\": \"Double page →\",\n    \"welcomeDirectionHint\": \"Un site qui déclare son propre sens de lecture reste prioritaire, et toute modification effectuée dans le lecteur est conservée.\",\n    \"welcomeStep3Title\": \"Créez votre compte\",\n    \"welcomeStep3Lede1\": \"PanelFlow conserve votre bibliothèque, votre progression et vos chapitres enregistrés sur votre compte. C’est ce qui vous permet de les retrouver dans un autre navigateur et dans l’application mobile.\",\n    \"welcomeStep3Lede2\": \"C’est également ce qui permet à nos serveurs de rechercher les nouveaux chapitres de vos séries lorsque Chrome est fermé. La configuration reprend dès que vous êtes connecté.\",\n    \"welcomeHaveAccount\": \"J’ai déjà un compte\",\n    \"welcomeLibraryFollows\": \". Votre bibliothèque est désormais synchronisée.\",\n    \"welcomeStep4Title\": \"Tout est prêt\",\n    \"welcomeStep4Lede\": \"PanelFlow ne propose pas de catalogue : il s’appuie sur le site que vous consultez. Rendez-vous sur vos sites de scans habituels et ouvrez un chapitre.\",\n    \"welcomeStep4Lede2\": \"S’il est reconnu, le lecteur s’ouvre ou le bouton PanelFlow apparaît sur la page : vous saurez donc immédiatement si le site fonctionne. <kbd>Alt</kbd>+<kbd>R</kbd> l’ouvre également à tout moment.\",\n    \"welcomeStep4Hint\": \"La compatibilité ne se limite pas à une liste : PanelFlow reconnaît la plupart des sites par lui-même et embarque des règles sur mesure pour les principaux. Si un site n’est pas détecté, signalez-le depuis le menu de l’extension.\",\n    \"authContacting\": \"Contact du serveur…\",\n    \"authNoAnswer\": \"Pas de réponse du serveur — vérifiez l’adresse de l’API dans les options.\",\n    \"popupOpenApp\": \"Ouvrir PanelFlow\",\n    \"popupCompatibleSites\": \"Sites compatibles\",\n    \"popupReadingStats\": \"Statistiques de lecture\",\n    \"navTrackers\": \"Trackers\",\n    \"popupSavedChapters\": \"Chapitres enregistrés\",\n    \"popupAddToLibrary\": \"Ajouter à la bibliothèque\",\n    \"popupShowReader\": \"Afficher le lecteur\",\n    \"popupHideReader\": \"Masquer le lecteur\",\n    \"popupReportIssue\": \"Signaler un problème\",\n    \"navLibrary\": \"Bibliothèque\",\n    \"popupGroupRecent\": \"Lus récemment\",\n    \"popupGroupRecentWatched\": \"Vus récemment\",\n    \"popupGroupNovels\": \"Light novels\",\n    \"popupGroupWebtoons\": \"Webtoons\",\n    \"popupGroupDefaults\": \"Réglages par défaut\",\n    \"popupPanelInfo\": \"Infos\",\n    \"popupChapters\": \"Chapitres\",\n    \"searchLibrary\": \"Chercher dans votre bibliothèque…\",\n    \"popupSortOrder\": \"Ordre\",\n    \"actionReverseOrder\": \"Inverser l’ordre\",\n    \"popupOnlyOneTag\": \"N’afficher qu’un tag\",\n    \"popupUnread\": \"Non lus\",\n    \"popupUnreadTitle\": \"Seulement les séries avec un chapitre que vous n’avez pas lu\",\n    \"popupAllTags\": \"Tous les tags\",\n    \"popupLibraryEmpty\": \"Rien pour l’instant. Ajoutez des séries à votre bibliothèque depuis n’importe quel site compatible.\",\n    \"statsNothingRead\": \"Rien de lu pour l’instant.\",\n    \"popupAutoShowReader\": \"Ouvrir le lecteur tout seul\",\n    \"switchOn\": \"OUI\",\n    \"switchOff\": \"NON\",\n    \"switchDefault\": \"DÉFAUT\",\n    \"actionCheckNow\": \"Chercher les nouveaux chapitres\",\n    \"navSettings\": \"Réglages\",\n    \"navSites\": \"Sites\",\n    \"webSitesTitle\": \"Où vous lisez\",\n    \"webSitesLede\": \"PanelFlow n’a pas de catalogue : il lit le site où vous êtes déjà. Voici les sites pour lesquels il embarque des règles sur mesure ; il détecte la plupart des autres tout seul. Mettez une étoile à ceux que vous utilisez : ils passeront en premier ici et dans l’extension.\",\n    \"webSitesYours\": \"Vos sites\",\n    \"webSitesAll\": \"Tous les sites\",\n    \"webSitesPin\": \"Ajouter à vos sites\",\n    \"webSitesUnpin\": \"Retirer de vos sites\",\n    \"webSitesUnavailable\": \"La liste n’a pas pu être chargée. PanelFlow reconnaît quand même les chapitres tout seul.\",\n    \"popupSearchSites\": \"Chercher un site…\",\n    \"popupSitesLede\": \"PanelFlow n’a pas de liste figée — toute page qui ressemble à un chapitre est détectée automatiquement, un site n’a donc pas besoin d’apparaître ici pour fonctionner. Voici ceux qu’il connaît déjà.\",\n    \"popupNoMatch\": \"Aucun résultat.\",\n    \"statsLast30Days\": \"30 derniers jours\",\n    \"statsMostRead\": \"Les plus lus\",\n    \"statsReadingLog\": \"Journal de lecture\",\n    \"trackerLinkedSeries\": \"Séries liées\",\n    \"trackerFindOnTracker\": \"La trouver sur le tracker\",\n    \"popupSearchTracker\": \"Chercher sur le tracker…\",\n    \"trackerNeverSend\": \"Ne jamais envoyer celle-ci\",\n    \"trackerNoResults\": \"Rien n’est revenu pour ça.\",\n    \"popupLocalOnly\": \"sans compte — connectez-vous\",\n    \"popupIntroWhat\": \"PanelFlow détecte les chapitres sur les sites de scans et les ouvre dans son propre lecteur : les mêmes commandes, quelle que soit la mise en page du site.\",\n    \"popupIntroWhere\": \"Sur un chapitre, le bouton du lecteur apparaît sur la page, et <kbd>Alt+R</kbd> l’ouvre depuis n’importe où. Cette fenêtre correspond à l’icône PanelFlow dans la barre d’outils : épinglez-la pour la garder à portée de main.\",\n    \"popupIntroAccount\": \"Votre bibliothèque, votre page en cours et vos statistiques sont conservées sur votre compte : vous les retrouvez sur un autre ordinateur et dans l’application mobile. <a href=\\\"#\\\" id=\\\"intro-account\\\">Créez un compte</a> pour commencer.\",\n    \"popupIntroDismiss\": \"J’ai compris\",\n    \"actionRemoveFromLibrary\": \"Retirer de la bibliothèque\",\n    \"popupRemoveFromHistory\": \"Retirer de l’historique\",\n    \"popupBadgeTuned\": \"règles sur mesure\",\n    \"popupBadgeInLibrary\": \"en bibliothèque\",\n    \"popupNoAccountBody\": \"Votre bibliothèque n’existe que sur cet ordinateur, et personne ne guette les nouveaux chapitres quand Chrome est fermé. Un compte règle les deux.\",\n    \"popupNoAccountAction\": \"En créer un\",\n    \"popupBadgeFavourite\": \"favori\",\n    \"standUnread\": \"Pas à jour — il y a quelque chose à lire\",\n    \"standReading\": \"Chapitre commencé\",\n    \"standRead\": \"À jour\",\n    \"pageStateNoTab\": \"Aucune page au premier plan — ouvrez d’abord un chapitre.\",\n    \"pageStateScheme\": \"PanelFlow ne s’exécute pas sur les pages du navigateur.\",\n    \"pageStateUnreachable\": \"PanelFlow ne s’est pas chargé sur cet onglet — rechargez-le.\",\n    \"pageStateUndetected\": \"Aucun chapitre trouvé sur cette page.\",\n    \"pageStateUngranted\": \"PanelFlow ne fonctionne pas encore sur ce site — activez-le ici.\",\n    \"chapterBadge\": \"Ch.$1\",\n    \"chapterN\": \"Ch. $1\",\n    \"chaptersShort\": \"$1 ch.\",\n    \"sinceDate\": \"depuis le $1\",\n    \"actionReadChapter\": \"Lire $1\",\n    \"actionContinueChapter\": \"Reprendre — $1\",\n    \"actionOpenSeriesPage\": \"Ouvrir la page de la série\",\n    \"badgeNewChapter\": \"Nouveau · $1\",\n    \"badgeNew\": \"Nouveau\",\n    \"agoNow\": \"à l’instant\",\n    \"agoMinutes\": \"il y a $1 min\",\n    \"agoHours\": \"il y a $1 h\",\n    \"agoDays\": \"il y a $1 j\",\n    \"agoMonths\": \"il y a $1 mois\",\n    \"dayToday\": \"Aujourd’hui\",\n    \"dayYesterday\": \"Hier\",\n    \"durationMinutes\": \"$1 min\",\n    \"durationHours\": \"$1 h $2\",\n    \"toastReadingPage\": \"Lecture de la page…\",\n    \"toastNotReadable\": \"Page illisible — rechargez-la et réessayez.\",\n    \"toastCouldNotRead\": \"Impossible de lire cette page.\",\n    \"toastWakingUp\": \"L’extension se réveille encore — réessayez.\",\n    \"toastConnecting\": \"Connexion…\",\n    \"toastBackendDown\": \"$1 ne répond pas — vérifiez l’adresse de l’API dans les options.\",\n    \"trackerAsking\": \"Interrogation de votre compte…\",\n    \"trackerUnreachable\": \"Impossible de joindre votre compte.\",\n    \"trackerConnectedAs\": \"Connecté en tant que $1\",\n    \"trackerConnected\": \"Connecté\",\n    \"trackerNotConnected\": \"Non connecté\",\n    \"trackerNoCredentials\": \"Aucun identifiant sur le serveur\",\n    \"trackerPasswordAuth\": \"Demande un mot de passe plutôt qu’une page d’autorisation — non pris en charge\",\n    \"trackerSendAll\": \"Tout envoyer\",\n    \"trackerImportPending\": \"Importer $1 + $2\",\n    \"trackerImportList\": \"Importer la liste\",\n    \"trackerUpToChapter\": \" · jusqu’au ch. $1\",\n    \"trackerSending\": \"Envoi de votre bibliothèque vers $1…\",\n    \"trackerSent\": \"$1 envoyées\",\n    \"trackerSkipped\": \"$1 ignorées\",\n    \"trackerFailed\": \"$1 en échec\",\n    \"trackerRemaining\": \"$1 restantes — appuyez encore\",\n    \"trackerNothingMissing\": \"$1 n’a rien qui manque à cette bibliothèque\",\n    \"trackerImportPreview\": \"$1 à ajouter · $2 à compléter — appuyez encore\",\n    \"trackerImportDone\": \"$1 ajoutées · $2 complétées\",\n    \"trackerNotListening\": \" — rien ne lui est encore envoyé\",\n    \"trackerRefusing\": \"Refuse ce que PanelFlow envoie : $1\",\n    \"trackerFetch\": \"Récupérer\",\n    \"trackerFetching\": \"Lecture de ce que $1 possède déjà…\",\n    \"trackerFetched\": \"$1 séries remises à jour\",\n    \"trackerFetchedAhead\": \"$1 remises à jour · $2 plus avancées là-bas qu’ici\",\n    \"statsLoadError\": \"Les statistiques n’ont pas pu être chargées : $1. Ce que vous avez lu sur cet appareil est conservé ci-dessous.\",\n    \"statsLocalOnly\": \"Compté sur cet appareil. Un compte additionne tous les appareils où vous lisez — et garde plus que les derniers milliers de chapitres.\",\n    \"statChaptersRead\": \"Chapitres lus\",\n    \"statTimeRead\": \"Temps de lecture\",\n    \"statSeriesRead\": \"Séries lues\",\n    \"statPerReadingDay\": \"Par jour de lecture\",\n    \"statCurrentStreak\": \"Série en cours\",\n    \"statLongestStreak\": \"Plus longue série\",\n    \"statInLibrary\": \"En bibliothèque\",\n    \"statAverageOfN\": \"Moyenne de $1 notes\",\n    \"statAverageScore\": \"Note moyenne\",\n    \"statDays\": \"$1 j\",\n    \"reportWhatWentWrong\": \"Ce qui n’a pas marché :\",\n    \"readerClose\": \"Fermer (Échap)\",\n    \"readerPrevChapter\": \"Chapitre précédent\",\n    \"readerNextChapter\": \"Chapitre suivant\",\n    \"readerChapters\": \"Chapitres\",\n    \"readerChaptersTitle\": \"Chapitres (C)\",\n    \"readerDownloadCbz\": \"Télécharger le chapitre (.cbz)\",\n    \"readerDownloadTxt\": \"Télécharger le chapitre (.txt)\",\n    \"readerSaveOffline\": \"Enregistrer pour la lecture hors ligne\",\n    \"readerSavedOffline\": \"Enregistré hors ligne — cliquez pour retirer\",\n    \"readerNotSaved\": \"Non enregistré — $1\",\n    \"readerNeedsAccess\": \"PanelFlow n’a pas l’autorisation de lire les pages depuis $1. Accordez-la dans les réglages pour télécharger ce chapitre.\",\n    \"readerGrantAccess\": \"Ouvrir les réglages\",\n    \"readerNotDownloaded\": \"Téléchargement impossible — $1\",\n    \"readerPrefs\": \"Préférences du lecteur (S)\",\n    \"readerResetZoom\": \"Réinitialiser le zoom (0)\",\n    \"readerFullscreen\": \"Plein écran (F)\",\n    \"readerHelpTitle\": \"Touches et gestes (?)\",\n    \"readerHideControls\": \"Masquer les commandes (H)\",\n    \"readerCurrentPage\": \"Page actuelle\",\n    \"readerBreakFirst\": \"Détacher la 1re page (B)\",\n    \"readerAutoPlay\": \"Défilement auto (bande verticale)\",\n    \"readerReadingMode\": \"Mode de lecture\",\n    \"readerBrightness\": \"Luminosité\",\n    \"readerContrast\": \"Contraste\",\n    \"readerGapSize\": \"Écart entre les pages\",\n    \"readerStripWidth\": \"Largeur de la bande\",\n    \"readerTextSize\": \"Taille du texte\",\n    \"readerLineSpacing\": \"Interligne\",\n    \"readerTextWidth\": \"Largeur du texte\",\n    \"readerPlaySpeed\": \"Vitesse de défilement\",\n    \"readerProgressSize\": \"Taille de la barre\",\n    \"readerTapZones\": \"Zones tactiles\",\n    \"readerTapSides\": \"Tiers gauche / droit\",\n    \"readerTapEdges\": \"Bords étroits\",\n    \"readerTapOff\": \"Aucune (clavier seulement)\",\n    \"readerSwapTap\": \"Inverser les côtés\",\n    \"readerAutoNext\": \"Chapitre suivant automatique\",\n    \"readerHideRead\": \"Masquer les chapitres déjà lus\",\n    \"readerHiddenOne\": \"— $1 chapitre lu masqué —\",\n    \"readerHiddenMany\": \"— $1 chapitres lus masqués —\",\n    \"readerHelpHead\": \"Touches et gestes\",\n    \"readerHelpTap\": \"<b>Touchez</b> un côté pour tourner la page, le milieu pour les commandes — la bande verticale défile à la place\",\n    \"readerHelpPinch\": \"<b>Pincez</b> ou <b>ctrl+molette</b> pour zoomer, <b>glissez</b> pour vous déplacer — la vue ne revient jamais en arrière toute seule\",\n    \"readerHelpDoubleTap\": \"<b>Double touche</b> pour zoomer sur une case, encore une fois pour revoir la page entière\",\n    \"readerHelpArrows\": \"<b>←</b> <b>→</b> <b>espace</b> tournent la page · <b>↑</b> <b>↓</b> font défiler la bande\",\n    \"readerHelpWheel\": \"<b>C</b> la roue des chapitres — <b>molette</b> ou <b>↑</b> <b>↓</b> pour en trouver un, <b>entrée</b> pour l’ouvrir\",\n    \"readerHelpKeys1\": \"<b>S</b> préférences · <b>B</b> détacher la première page · <b>0</b> réinitialiser le zoom\",\n    \"readerHelpKeys2\": \"<b>F</b> plein écran · <b>H</b> masquer les commandes · <b>?</b> cette liste · <b>Échap</b> fermer\",\n    \"readerHelpNote\": \"Le sens de lecture, les zones tactiles et tout le reste se trouvent sous ⚙.\",\n    \"readerResetDefaults\": \"Remettre les réglages par défaut\",\n    \"readerHelpToggleChrome\": \"Touchez le milieu de la page pour masquer les contrôles — et de nouveau pour les faire revenir.\",\n    \"readerSeriesPrefs\": \"Retenir pour cette série\",\n    \"readerSeriesOn\": \"Ces réglages suivent désormais cette série\",\n    \"readerSeriesOff\": \"Retour à vos réglages habituels\",\n    \"readerEndTitle\": \"Fin du chapitre\",\n    \"readerEndOf\": \"Fin de $1\",\n    \"readerEndThis\": \"ce chapitre\",\n    \"readerEndNext\": \"Chapitre suivant\",\n    \"readerEndStay\": \"Rester ici\",\n    \"readerEndMarkRead\": \"Marquer comme lu\",\n    \"readerEndMarked\": \"Marqué comme lu\",\n    \"readerEndCaughtUp\": \"C’est le chapitre le plus récent ici.\",\n    \"readerEndLeftOne\": \"Encore 1 chapitre dans cette liste.\",\n    \"readerEndLeftMany\": \"Encore $1 chapitres dans cette liste.\",\n    \"zoneBack\": \"Retour\",\n    \"zoneNext\": \"Suite\",\n    \"zoneControls\": \"Commandes\",\n    \"zoneControlsKeys\": \"Commandes — le clavier tourne les pages\",\n    \"pillReaderMode\": \"Mode lecture\",\n    \"pillReaderModeTitle\": \"PanelFlow : ouvrir ce chapitre en mode lecture\",\n    \"pillAddAnime\": \"Ajouter à la bibliothèque\",\n    \"pillAddAnimeTitle\": \"Suivre cet anime dans PanelFlow\",\n    \"pillHideControls\": \"Masquer ces contrôles\",\n    \"pillShowControls\": \"Contrôles PanelFlow\",\n    \"modalAlreadyInLibrary\": \"Déjà dans votre bibliothèque\",\n    \"modalSameTitleLead\": \"Vous suivez déjà cette série sur un autre site.\",\n    \"modalLikelyLead\": \"Cela ressemble à une série que vous suivez déjà.\",\n    \"modalInYourLibrary\": \"Dans votre bibliothèque\",\n    \"modalThisPage\": \"Cette page\",\n    \"modalCloseTitlesWarning\": \"Les titres sont proches mais pas identiques — vérifiez qu’il s’agit bien de la même série avant de migrer.\",\n    \"modalKeepsProgress\": \"Votre progression, votre note, vos commentaires et vos tags\",\n    \"modalKeepsDate\": \"La date à laquelle vous l’avez ajoutée\",\n    \"modalKeepsOldSite\": \"L’ancien site, gardé dans l’historique de la fiche\",\n    \"modalMigrating\": \"Migration…\",\n    \"modalMigrationFailed\": \"La migration a échoué.\",\n    \"modalAddSeparately\": \"Ajouter comme série distincte\",\n    \"modalEditEntry\": \"Modifier la fiche\",\n    \"modalTranslatedLanguage\": \"Langue de la traduction\",\n    \"modalCurrentProgress\": \"Progression actuelle\",\n    \"modalAddTagHint\": \"Ajoutez un tag et appuyez sur Entrée\",\n    \"modalLocalOnlyHint\": \"Enregistré sur cet appareil uniquement — connectez-vous depuis les réglages de l’extension PanelFlow pour le voir dans l’application web.\",\n    \"modalConnectTrackerLead\": \"Connectez un tracker pour remplir ceci depuis votre propre liste :\",\n    \"modalConnectTrackerHint\": \"Connectez AniList ou MyAnimeList dans l’application web PanelFlow pour remplir ceci depuis votre propre liste.\",\n    \"modalTrackerConnect\": \"Connecter $1\",\n    \"modalTrackerMatchedAs\": \"reconnu comme « $1 »\",\n    \"modalTrackerUnreachable\": \"$1 est injoignable — $2\",\n    \"modalTrackerNotListed\": \"Pas encore dans votre liste $1.\",\n    \"modalTrackerAdd\": \"Ajouter\",\n    \"modalTrackerAdding\": \"Ajout en cours…\",\n    \"modalTrackerAdded\": \"Ajouté à $1 au chapitre $2.\",\n    \"modalTrackerAddedPlain\": \"Ajouté à $1.\",\n    \"modalTrackerAhead\": \"$1 est déjà à ce chapitre ou plus loin.\",\n    \"modalTrackerPickSeries\": \"$1 ne connaît pas ce titre. Choisissez la bonne série :\",\n    \"modalTrackerNoHits\": \"$1 n’a rien trouvé pour « $2 ».\",\n    \"modalTrackerFailed\": \"$1 a refusé — $2\",\n    \"modalTrackerNoAnswer\": \"aucune réponse\",\n    \"modalNoTitle\": \"Impossible de lire un titre sur cette page.\",\n    \"offlineDocTitle\": \"PanelFlow — Chapitres enregistrés\",\n    \"offlineEmpty\": \"Rien d’enregistré pour l’instant. Ouvrez un chapitre, puis appuyez sur <code>📥</code> dans la barre latérale du lecteur pour le garder sur cet appareil.\",\n    \"offlineChapterOne\": \"$1 chapitre\",\n    \"offlineChapterMany\": \"$1 chapitres\",\n    \"offlinePageOne\": \"$1 page\",\n    \"offlinePageMany\": \"$1 pages\",\n    \"offlineParagraphOne\": \"$1 paragraphe\",\n    \"offlineParagraphMany\": \"$1 paragraphes\",\n    \"offlineUsage\": \"$1 · $2 sur cet appareil · gardés $3 jours\",\n    \"offlineExpiresToday\": \"expire aujourd’hui\",\n    \"offlineExpiresIn\": \"expire dans $1 jours\",\n    \"offlineUnknownSeries\": \"Série inconnue\",\n    \"notifyNewChapterTitle\": \"Nouveau chapitre !\",\n    \"notifyNewChapterBody\": \"$1 — le chapitre $2 est sorti sur $3\",\n    \"navUpdates\": \"Nouveautés\",\n    \"navStatistics\": \"Statistiques\",\n    \"navHistory\": \"Historique\",\n    \"webTagline\": \"Votre bibliothèque de mangas, partout.\",\n    \"webForgotLede\": \"Nous vous envoyons par courriel un lien pour choisir un nouveau mot de passe.\",\n    \"webSendTheLink\": \"Envoyer le lien\",\n    \"webBackToSignIn\": \"Retour à la connexion\",\n    \"webResetLede\": \"Choisissez un nouveau mot de passe.\",\n    \"webNewPassword\": \"Nouveau mot de passe\",\n    \"webChangePassword\": \"Changer mon mot de passe\",\n    \"webPushTest\": \"S’envoyer une notification de test\",\n    \"webTryAgain\": \"Réessayer\",\n    \"libraryContinueReading\": \"Reprendre la lecture\",\n    \"webImport\": \"Importer\",\n    \"webImportTitle\": \"Importer une liste depuis AniList ou MyAnimeList\",\n    \"webExport\": \"Exporter\",\n    \"webExportTitle\": \"Télécharger une sauvegarde, ou en restaurer une\",\n    \"webMoveSite\": \"Changer de site\",\n    \"webMoveSiteTitle\": \"Déplacer toutes les séries d’un site mort\",\n    \"webAddSeries\": \"Ajouter une série\",\n    \"webSort\": \"Trier\",\n    \"webUnreadOnly\": \"Non lus seulement\",\n    \"webLibraryEmpty\": \"Rien ici pour l’instant. Ajoutez une série, ou épinglez-en une depuis l’extension PanelFlow en parcourant votre site de mangas préféré.\",\n    \"webNewChapters\": \"Nouveaux chapitres\",\n    \"webCheckUpdatesTitle\": \"Reparcourir toutes les séries à la recherche de nouveaux chapitres\",\n    \"webUpdatesEmpty\": \"Rien de neuf. Toutes les séries que vous suivez sont lues jusqu’à leur dernier chapitre.\",\n    \"webReadingStatistics\": \"Statistiques de lecture\",\n    \"webStatsEmpty\": \"Rien de lu pour l’instant. Le mode lecture de l’extension enregistre le temps de lecture au fil de l’eau.\",\n    \"webLibraryBreakdown\": \"Répartition de la bibliothèque\",\n    \"webReadingHistory\": \"Historique de lecture\",\n    \"webClearHistory\": \"Vider l’historique\",\n    \"webClearHistoryTitle\": \"Supprimer toutes les lectures enregistrées\",\n    \"webConnectedAccounts\": \"Comptes connectés\",\n    \"webTrackersLede\": \"Chaque chapitre que vous terminez est envoyé aux comptes ci-dessous. PanelFlow n’écrit jamais que le nombre de chapitres, et ne fait que transmettre.\",\n    \"webNoLinksYet\": \"Rien de lié pour l’instant. Une série se retrouve sur le tracker à la première lecture d’un de ses chapitres.\",\n    \"webAppearance\": \"Apparence\",\n    \"webThemeHint\": \"Suit votre compte : changez-le ici et l’extension et l’application mobile changent aussi. Si le bureau où vous lisez et le téléphone dans votre poche ne veulent pas la même réponse à la même heure, c’est à ça que sert « Suivre le système » — c’est un choix, et chaque appareil demande à la machine sur laquelle il tourne.\",\n    \"webLanguageHint\": \"Suit votre compte, et s’applique ici autant que dans l’extension et l’application mobile.\",\n    \"navAccount\": \"Compte\",\n    \"webAccountHint\": \"Votre bibliothèque, votre progression et vos trackers suivent cette adresse.\",\n    \"webPasswordHint\": \"Nous envoyons un lien à votre adresse, et c’est le lien qui change le mot de passe — ainsi un navigateur que quelqu’un d’autre a laissé connecté ne peut pas le changer.\",\n    \"webEmailMeALink\": \"M’envoyer un lien\",\n    \"webReading\": \"Lecture\",\n    \"webNoExtension\": \"Ces réglages vivent dans l’extension PanelFlow, que ce navigateur n’a pas. Installez-la et cette section se remplit.\",\n    \"webReadingDirectionHint\": \"Comment un chapitre s’ouvre, sauf avis contraire du site.\",\n    \"webCheckEveryHint\": \"À quelle fréquence l’extension regarde, en arrière-plan.\",\n    \"webWhitelist\": \"Sites à laisser tranquilles\",\n    \"webWhitelistHint\": \"Un domaine par ligne. Les publicités n’y sont pas bloquées — pour un site qui casse quand elles le sont.\",\n    \"webFieldTitle\": \"Titre\",\n    \"webFieldSeriesUrl\": \"Adresse de la série\",\n    \"webFieldCoverUrl\": \"Adresse de l’image de couverture\",\n    \"webFieldCoverHint\": \"(remplie depuis la page)\",\n    \"webFieldStatus\": \"Statut\",\n    \"webFieldPublication\": \"Publication\",\n    \"webOngoing\": \"En cours\",\n    \"webFinished\": \"Terminée\",\n    \"webLangJa\": \"Japonais (manga)\",\n    \"webLangKo\": \"Coréen (manhwa)\",\n    \"webLangZh\": \"Chinois (manhua)\",\n    \"webLangEn\": \"Anglais\",\n    \"webLangFr\": \"Français\",\n    \"webLangEs\": \"Espagnol\",\n    \"webStarted\": \"Commencée\",\n    \"webCommaSeparated\": \"(séparées par des virgules)\",\n    \"webTagsPlaceholder\": \"romance, école\",\n    \"webNotePlaceholder\": \"Ce dont vous voulez vous souvenir\",\n    \"webReadingProgress\": \"Progression de lecture\",\n    \"webFieldChapter\": \"Chapitre\",\n    \"webChapterPlaceholder\": \"Chapitre 42\",\n    \"webFieldPage\": \"Page\",\n    \"webFieldChapterUrl\": \"Adresse du chapitre\",\n    \"webImportList\": \"Importer une liste\",\n    \"webImportLede\": \"Ajoute ce qui manque et complète les vides. Rien de ce qui est déjà dans votre bibliothèque n’est écrasé.\",\n    \"webFromConnected\": \"Depuis un compte que vous avez connecté :\",\n    \"webOr\": \"ou\",\n    \"webAnilistUsername\": \"Nom d’utilisateur AniList\",\n    \"webYourName\": \"votrenom\",\n    \"webMalExport\": \"Export MyAnimeList\",\n    \"webMalExportHint\": \"(le .xml issu de Export)\",\n    \"webClose\": \"Fermer\",\n    \"webPreview\": \"Aperçu\",\n    \"webExportLede\": \"La sauvegarde contient tout — étagère, marque-pages et historique de lecture — et c’est le seul format qui puisse être restauré ici.\",\n    \"webBackupJson\": \"Sauvegarde (.json)\",\n    \"webSpreadsheetCsv\": \"Tableur (.csv)\",\n    \"webRestoreBackup\": \"Restaurer une sauvegarde\",\n    \"webRestoreLede\": \"Complète ce qui manque. Rien de ce qui est déjà dans votre bibliothèque n’est écrasé.\",\n    \"webPanelflowBackup\": \"Sauvegarde PanelFlow\",\n    \"webPanelflowBackupHint\": \"(le .json ci-dessus)\",\n    \"webPreviewRestore\": \"Aperçu de la restauration\",\n    \"webRestore\": \"Restaurer\",\n    \"webMoveWholeSite\": \"Déplacer tout un site\",\n    \"webLeaving\": \"On quitte\",\n    \"webGoingTo\": \"On va sur\",\n    \"webGoingToHint\": \"(domaine du nouveau site)\",\n    \"webFindMatches\": \"Chercher les correspondances\",\n    \"webMoveSelected\": \"Déplacer la sélection\",\n    \"webYourShelves\": \"Vos étagères\",\n    \"webShelvesLede\": \"Une étagère est un onglet à vous. Elle compte toujours comme l’un des cinq statuts de lecture — c’est ce qui décide si PanelFlow surveille ces séries et sous quel statut elles s’exportent.\",\n    \"webNewShelfPlaceholder\": \"Nouvelle étagère — « Hebdo », « Avec mon frère »…\",\n    \"webAdd\": \"Ajouter\",\n    \"webAlreadyRegistered\": \"Déjà inscrit ?\",\n    \"webNoAccountYet\": \"Pas encore de compte ?\",\n    \"webCreateOne\": \"Créer un compte\",\n    \"webForgotSent\": \"Si cette adresse a un compte, un lien est en route.\",\n    \"webLinkOnItsWay\": \"Un lien est en route vers votre boîte mail.\",\n    \"webPasswordChanged\": \"Mot de passe changé — connectez-vous avec le nouveau.\",\n    \"webLoading\": \"Chargement…\",\n    \"webLoadFailed\": \"Impossible de charger votre bibliothèque\",\n    \"actionResume\": \"Reprendre\",\n    \"webNotStarted\": \"Pas commencé\",\n    \"webEditDetails\": \"Modifier les détails\",\n    \"webEditSeries\": \"Modifier la série\",\n    \"webSetProgress\": \"Définir la progression\",\n    \"webCouldNotRemove\": \"Impossible de retirer $1\",\n    \"webCouldNotMove\": \"Impossible de déplacer $1\",\n    \"webChaptersAhead\": \"Le dernier chapitre a $1 chapitres d’avance sur votre marque-page\",\n    \"webYourScore\": \"Votre note : $1/10\",\n    \"webReadThrough\": \"Lu $1 fois\",\n    \"webNewChapter\": \"Nouveau chapitre\",\n    \"webNewChapterOut\": \"Un chapitre que vous n’avez pas encore lu est sorti\",\n    \"webOneNewChapter\": \"$1 nouveau chapitre\",\n    \"webNNewChapters\": \"$1 nouveaux chapitres\",\n    \"webCheckingAll\": \"Vérification de toutes les séries…\",\n    \"webNoNewChapters\": \"Aucun nouveau chapitre ($1 séries vérifiées)\",\n    \"webOneHasNew\": \"$1 série a de nouveaux chapitres !\",\n    \"webNHaveNew\": \"$1 séries ont de nouveaux chapitres !\",\n    \"webSortAscending\": \"Croissant — cliquez pour décroissant\",\n    \"webSortDescending\": \"Décroissant — cliquez pour croissant\",\n    \"webOnlyTagged\": \"N’afficher que les séries étiquetées $1\",\n    \"webShownOfTotal\": \"$1 sur $2\",\n    \"webMakeShelf\": \"Créer votre propre étagère\",\n    \"webNoShelves\": \"Aucune étagère pour le moment.\",\n    \"webMoveUp\": \"Monter\",\n    \"webShelfCountsAs\": \"Votre étagère — compte comme $1\",\n    \"webShelfStatusHint\": \"Ce que compte cette étagère : si ses séries sont surveillées pour de nouveaux chapitres, et sous quel statut elles s’exportent\",\n    \"webShelfRemoveHint\": \"Retirer cette étagère — ses séries passent dans $1\",\n    \"webShelfMovesOne\": \"$1 série passe dans $2.\",\n    \"webShelfMovesN\": \"$1 séries passent dans $2.\",\n    \"webRemoveShelf\": \"Retirer l’étagère « $1 » ?\",\n    \"webFetchingPage\": \"Lecture des infos de la page…\",\n    \"webPageUnreadable\": \"Impossible de lire la page ($1) — remplissez à la main.\",\n    \"webClearHistoryConfirm\": \"Supprimer toutes les lectures enregistrées ? Votre bibliothèque et vos marque-pages ne sont pas touchés.\",\n    \"webClearHistoryFailed\": \"Impossible d’effacer votre historique\",\n    \"webReadingList\": \"Lecture de la liste…\",\n    \"webNothingWritten\": \"Rien n’a encore été écrit.\",\n    \"webImporting\": \"Importation…\",\n    \"webReadingBackup\": \"Lecture de la sauvegarde…\",\n    \"webRestoring\": \"Restauration…\",\n    \"webMigrateSearching\": \"Recherche de chaque série sur le nouveau site — cela prend un moment…\",\n    \"webSendingTest\": \"Envoi d’une notification de test…\",\n    \"webExtensionSilent\": \"L’extension n’a pas répondu — réessayez.\",\n    \"webSavedHereOnly\": \"Enregistré ici, mais le compte n’en a rien su.\",\n    \"webTrackerRefusingFix\": \". Déconnecter puis reconnecter le compte suffit en général.\",\n    \"webEverySite\": \"Tous les sites ($1 séries)\",\n    \"badgeNewChapterNo\": \"Nouveau · ch. $1\",\n    \"folder_all\": \"Tous\",\n    \"agoWeeks\": \"il y a $1 sem.\",\n    \"agoSomeTime\": \"à un moment\",\n    \"durationSeconds\": \"$1 s\",\n    \"statReadingSince\": \"Lit depuis\",\n    \"webSessionExpired\": \"session expirée\",\n    \"webRequestFailed\": \"la requête a échoué ($1)\",\n    \"webOneBehind\": \"$1 chapitre de retard\",\n    \"webNBehind\": \"$1 chapitres de retard\",\n    \"webFinishedHint\": \"La série elle-même est terminée\",\n    \"webOngoingHint\": \"La série paraît toujours\",\n    \"webCoverFound\": \"couverture trouvée\",\n    \"webNoCoverFound\": \"aucune couverture trouvée\",\n    \"webLatestChapter\": \"dernier chapitre : $1\",\n    \"webChartDay\": \"$1 · $2 ch · $3\",\n    \"webChartNothing\": \"$1 · rien de lu\",\n    \"statChaptersAndTime\": \"$1 chapitres · $2\",\n    \"webShelves\": \"Étagères\",\n    \"trackerSendMyLibrary\": \"Envoyer ma bibliothèque\",\n    \"trackerSendMyLibraryHint\": \"Mettre le tracker à jour avec tous les marque-pages que vous avez déjà\",\n    \"trackerFetchAll\": \"Récupérer ce qu’il a déjà\",\n    \"trackerFetchAllHint\": \"Relire vos compteurs de chapitres depuis le tracker, pour que PanelFlow ne reste pas en dessous de ce que vous y avez déjà lu. Vos marque-pages ne sont pas touchés.\",\n    \"trackerLastUpdate\": \" — dernier envoi $1\",\n    \"trackerWaitingFor\": \"En attente de $1…\",\n    \"trackerDisconnectConfirm\": \"Déconnecter $1 ?\\n\\nRien n’est retiré de votre tracker. PanelFlow oublie quelle série correspondait à laquelle, donc une reconnexion recommence l’appariement.\",\n    \"trackerNoMatch\": \"$1 · aucune correspondance — choisissez-la vous-même\",\n    \"trackerRematchHint\": \"Réapparier celle-ci de zéro au prochain chapitre lu\",\n    \"webPopupBlocked\": \"le navigateur a bloqué la fenêtre — autorisez les popups pour ce site\",\n    \"webNoGunzip\": \"ce navigateur ne sait pas décompresser un .gz — décompressez l’export d’abord\",\n    \"webNotJson\": \"ce fichier n’est pas du JSON\",\n    \"webImportNeedsSource\": \"donnez un pseudo AniList ou choisissez un export MyAnimeList\",\n    \"webExportFailed\": \"l’export a échoué ($1)\",\n    \"webEntriesInList\": \"$1 entrées dans la liste\",\n    \"webPlanCounts\": \"$1 à ajouter · $2 à compléter · $3 déjà à jour\",\n    \"webPlanBookmarks\": \"$1 recevront un marque-page de chapitre\",\n    \"webPlanHistory\": \"$1 marque-pages · $2 lectures dans l’historique\",\n    \"webFilledIn\": \"Complétées\",\n    \"webImportDone\": \"Terminé — $1 ajoutées, $2 mises à jour.\",\n    \"webMigrateFound\": \"$1 sur $2 trouvées sur $3\",\n    \"webMigrateTruncated\": \"la recherche a manqué de temps — relancez-la pour le reste\",\n    \"webMigrateSkipped\": \"$1 non cherchées\",\n    \"webMoving\": \"Déplacement de $1 séries…\",\n    \"webMoved\": \"$1 déplacées\",\n    \"webMoveRefused\": \" · $1 refusées : $2\",\n    \"webPushBlocked\": \"Ce navigateur bloque les notifications de PanelFlow — autorisez-les dans les réglages du site\",\n    \"webPushOnHint\": \"Les nouveaux chapitres sont annoncés même quand PanelFlow est fermé. Cliquez pour arrêter.\",\n    \"webPushOffHint\": \"Être prévenu des nouveaux chapitres même quand PanelFlow est fermé\",\n    \"webPushTookOne\": \"$1 navigateur l’a reçue\",\n    \"webPushTookN\": \"$1 navigateurs l’ont reçue\",\n    \"webPushExpiredOne\": \"$1 abonnement avait expiré et a été retiré\",\n    \"webPushExpiredN\": \"$1 abonnements avaient expiré et ont été retirés\",\n    \"webPushUnreachableOne\": \"$1 navigateur n’a pas pu être joint — réessayez plus tard\",\n    \"webPushUnreachableN\": \"$1 navigateurs n’ont pas pu être joints — réessayez plus tard\",\n    \"webPushKeysHint\": \". Si aucune notification n’apparaît, les clés du serveur sont mauvaises.\",\n    \"navSearch\": \"Recherche\",\n    \"navStats\": \"Stats\",\n    \"badgeNNew\": \"$1 nouv.\",\n    \"searchNoResults\": \"Aucun résultat.\",\n    \"statusRemoved\": \"Retirée\",\n    \"statusSignedIn\": \"Connecté\",\n    \"statusUpToDate\": \"À jour\",\n    \"mobileLibraryEmpty\": \"Rien ici pour l’instant. Cherchez une série, ouvrez-la, et ajoutez-la depuis le lecteur.\",\n    \"mobileNothingFiled\": \"Rien de classé dans « $1 » pour l’instant.\",\n    \"mobileSearchPlaceholder\": \"Chercher une série sur le web\",\n    \"mobileGo\": \"OK\",\n    \"mobileScansOnly\": \"Privilégier les sites de lecture\",\n    \"mobileSearchKind\": \"Privilégier les sites de\",\n    \"mobileSearchHint\": \"PanelFlow n’héberge et n’indexe rien. C’est une simple recherche web sur vos propres mots — chaque résultat est vérifié pour vous dire si le mode Lecture y fonctionnera avant que vous l’ouvriez.\",\n    \"mobileSearchWebHint\": \"PanelFlow n’héberge et n’indexe rien. Ceci lance vos propres mots comme une recherche web ordinaire, dans le navigateur de l’app — un résultat qui se trouve être un chapitre est reconnu dès son ouverture.\",\n    \"mobileMediumManga\": \"Manga\",\n    \"mobileMediumAnime\": \"Animés\",\n    \"mobileLibraryCount\": \"$1 séries dans votre bibliothèque\",\n    \"mobileSearchFailed\": \"La recherche a échoué : $1\",\n    \"mobileSearchNeedsAccount\": \"La recherche tourne sur le serveur PanelFlow — connectez-vous d’abord dans l’onglet Compte.\",\n    \"mobileVerdictReady\": \"Le mode Lecture marche ici\",\n    \"mobileVerdictLikely\": \"Le mode Lecture marchera sans doute\",\n    \"mobileVerdictUnknown\": \"Impossible à dire avant ouverture\",\n    \"mobileVerdictUnlikely\": \"Le mode Lecture ne marchera sans doute pas\",\n    \"mobileLatestCh\": \"dernier ch. $1\",\n    \"mobileYouAreOn\": \"vous en êtes à $1\",\n    \"mobileNotStarted\": \"pas commencé\",\n    \"mobileReadNew\": \"Lire $1 — nouveau\",\n    \"mobileFindElsewhere\": \"La trouver sur un autre site\",\n    \"mobileChartDay\": \"$1 — $2\",\n    \"mobileStatsSignedOut\": \"Les statistiques vivent sur votre compte — connectez-vous dans l’onglet Compte pour les voir. Ce que vous lisez sur ce téléphone est gardé ci-dessous de toute façon.\",\n    \"mobileSignedInAs\": \"Connecté en tant que \",\n    \"mobileMergeDuplicates\": \"Fusionner les doublons\",\n    \"mobileMerged\": \"$1 doublon(s) fusionné(s)\",\n    \"mobileNoDuplicates\": \"Aucun doublon trouvé\",\n    \"mobileAccountHint\": \"Votre bibliothèque marche entièrement hors ligne et sans compte. Un compte ajoute seulement la synchro entre votre téléphone et votre navigateur, et fait marcher la recherche.\",\n    \"mobileNoBridge\": \"Ouvert en dehors de l’application : il n’y a pas de pont natif ici, donc pas de bibliothèque à montrer.\",\n  },\n};\n\n}catch(e){if(window.PanelFlowFailed)window.PanelFlowFailed(\"messages.js\",e);else console.warn('panelflow: messages.js failed',e)}\ntry{\n// `t()` for pages that are not in Chrome.\n//\n// The second mobile-specific file in the injection set, and it exists for the\n// same reason chrome-shim.js does: the extension's manifest injects\n// `extension/i18n.js` ahead of detect.js, library-modal.js and reader.js, which\n// call `t()` a hundred and twenty-eight times between them. That file asks\n// `chrome.i18n` — an API no WebView has — so on a phone `t` was simply not\n// defined, and the first label any of those three tried to draw threw.\n//\n// The consequence was invisible and total: detect.js loaded fine and then died\n// on `t('pillReaderMode')`, so no Reader Mode pill ever appeared; reader.js and\n// library-modal.js died the same way at their first label, so \"Read\" and \"Add\n// to library\" did nothing at all. Nothing in the log said \"translation\",\n// because nothing was missing at load time.\n//\n// So this is the placement, and only the placement. The sentences themselves\n// are `messages.js`, generated from `shared/_locales/` by\n// scripts/build-messages.mjs — the same catalogue the website and the app shell\n// read. Injected immediately before it, and before anything that speaks.\n(function (root) {\n  'use strict';\n  if (root.PanelFlowI18n) return;\n\n  const MESSAGES = root.PanelFlowMessages || {};\n  const DEFAULT = 'en';\n  const LANGS = [\n    { code: 'en', label: 'English' },\n    { code: 'fr', label: 'Français' },\n  ];\n\n  const known = (code) => LANGS.some((l) => l.code === code);\n\n  /**\n   * The language, in three tries, cheapest first.\n   *\n   * 1. `PanelFlowLang`, which a shell that already knows the answer sets on the\n   *    page before this file runs. React Native does: it holds the account's\n   *    language in its own process and can spell it into the injection.\n   * 2. the device's, which is right for most readers and is what the extension\n   *    falls back to as well.\n   * 3. English.\n   *\n   * Then `ready` below corrects it from the account, for the shells that cannot\n   * do (1) — Kotlin and Swift keep the store in a WebView they cannot read\n   * synchronously.\n   */\n  function initial() {\n    if (known(root.PanelFlowLang)) return root.PanelFlowLang;\n    const tags = []\n      .concat(root.navigator?.languages || [], root.navigator?.language || [])\n      .map((tag) => String(tag).toLowerCase().split('-')[0]);\n    return tags.find(known) || DEFAULT;\n  }\n\n  let lang = initial();\n\n  /** `$1`…`$9`, filled from the call site — chrome.i18n's own convention. */\n  const fill = (message, subs) => {\n    const list = subs === undefined || subs === null ? [] : [].concat(subs);\n    return message.replace(/\\$([1-9])/g, (whole, n) => {\n      const sub = list[Number(n) - 1];\n      return sub === undefined ? whole : String(sub);\n    });\n  };\n\n  /**\n   * A message by key.\n   *\n   * Falls through to English rather than to nothing — a key a translator has\n   * not reached yet is a readable sentence, where the honest answer is a blank\n   * label. The key itself is the last resort, and seeing `pillReaderMode` on a\n   * page means the catalogue and the source have drifted.\n   */\n  function t(key, subs) {\n    const said = (MESSAGES[lang] && MESSAGES[lang][key]) ||\n      (MESSAGES[DEFAULT] && MESSAGES[DEFAULT][key]);\n    return said === undefined ? key : fill(said, subs);\n  }\n\n  /**\n   * The account's answer, once the shell can be asked for it.\n   *\n   * reader.js awaits this before it draws (`await PanelFlowI18n.ready`), which\n   * is exactly why the extension's version exposes it: the alternative is a\n   * panel painted in one language and corrected in another.\n   *\n   * `chrome.storage.local` here is chrome-shim.js, which routes to the same\n   * store the app shell reads — so the language chosen on the website reaches a\n   * chapter page on the phone without anything being told twice.\n   */\n  const ready = new Promise((resolve) => {\n    try {\n      root.chrome.storage.local.get(['accountPrefs'], (values) => {\n        const chosen = values && values.accountPrefs && values.accountPrefs.uiLang;\n        if (known(chosen)) lang = chosen;\n        resolve(lang);\n      });\n    } catch {\n      // No shim on this page, or no answer. What the device said stands.\n      resolve(lang);\n    }\n  });\n\n  // Deliberately no `apply()` and no `markLanguage()`. Both belong to a page of\n  // our own: this file runs inside somebody else's site, where there is no\n  // annotated markup to fill and where relabelling `<html lang>` would be a\n  // content script rewriting the document it was lent.\n  root.PanelFlowI18n = { t, ready, LANGS };\n\n  // The bare name, because the three files below call it hundreds of times and\n  // `PanelFlowI18n.t(...)` at every call site would drown the strings it wraps.\n  if (!root.t) root.t = t;\n})(typeof globalThis !== 'undefined' ? globalThis : self);\n\n}catch(e){if(window.PanelFlowFailed)window.PanelFlowFailed(\"i18n.js\",e);else console.warn('panelflow: i18n.js failed',e)}\ntry{\n// Does this page show a work the library already has?\n//\n// seriesKey() answers that for one site: it reduces a URL to host + slug, so\n// /manga/ao-no-hako and /chapter/ao-no-hako-109 collapse together. It cannot\n// answer it across sites — the same book is /ao-no-hako on one scan site and\n// /blue-box on another, and nothing in the URLs says so.\n//\n// So the cross-site question is answered on titles. That is fuzzy by nature\n// (a site writes \"Ao no Hako VF - Lecture en ligne\", another writes\n// \"Blue Box\"), which is why this module reports a confidence instead of a\n// boolean: the caller asks the user before merging anything.\n//\n// Plain script, not a module: Chrome content scripts cannot be ESM. The copy\n// under extension/shared is generated from this file — run `npm run sync:shared`\n// after editing, and `shared sources are in sync` in the test suite will catch\n// it if you forget.\n(function (root) {\n  'use strict';\n\n  const normUrl = (u) => String(u || '').toLowerCase().replace(/\\/+$/, '');\n\n  // Path segments that name a section of the site rather than the work.\n  const SECTIONS = /^(manga|manhwa|manhua|comics?|series|scan|scans|read|reader|lecture|lecture-en-ligne|chapter|chapitre|chap|ch|episode|viewer|title|webtoon)s?$/i;\n\n  // A segment that is only a chapter counter: \"chapitre-109\", \"vol_3\", \"ch.12\".\n  const COUNTER_SEG = /^(chapter|chapitre|chap|ch|episode|ep|tome|volume|vol|saison|season|part|partie)[-_. ]?\\d+(\\.\\d+)?$/i;\n\n  function seriesKey(url) {\n    let u;\n    try { u = new URL(url); } catch { return normUrl(url); }\n    const host = u.hostname.replace(/^www\\./, '').toLowerCase();\n    const slug = u.pathname.split('/')\n      .map((seg) => seg\n        .replace(/\\.(html?|php)$/i, '')\n        // \"Ao-no-Hako-Chapitre-109-FR_330666\" → \"Ao-no-Hako\"\n        .replace(/[-_.](chapter|chapitre|chap|ch|episode)[-_. ]?[\\d.].*$/i, '')\n        .replace(/^[-_.\\s]+|[-_.\\s]+$/g, '')\n        .toLowerCase())\n      .filter((seg) => seg && !SECTIONS.test(seg) && !COUNTER_SEG.test(seg)\n        && !/^\\d+(\\.\\d+)?$/.test(seg))\n      // The work's slug is the deepest segment left: /manga/<slug>/<chapter>.\n      .pop();\n    return slug ? `${host}|${slug}` : normUrl(url);\n  }\n\n  function sameSeries(a, b) {\n    const na = normUrl(a);\n    const nb = normUrl(b);\n    if (!na || !nb) return false;\n    if (na === nb) return true;\n    return seriesKey(a) === seriesKey(b);\n  }\n\n  // Words a scan site adds around the title: the language it was translated\n  // into, the format, and the SEO tail. None of them identify the work.\n  const NOISE_WORDS = [\n    'vostfr', 'vf', 'vostf', 'raw', 'raws',\n    'scan', 'scans', 'scanlation', 'scantrad',\n    'manga', 'manhwa', 'manhua', 'webtoon', 'webtoons', 'comic', 'comics',\n    'novel', 'lightnovel', 'ln', 'wn',\n    'lecture en ligne', 'lire en ligne', 'read online', 'online',\n    'gratuit', 'gratuitement', 'free', 'hd', 'vip', 'complet',\n    'official', 'officiel', 'traduction', 'translation',\n  ];\n  const NOISE = new RegExp('\\\\b(' + NOISE_WORDS.join('|') + ')\\\\b', 'g');\n\n  const COUNTER = /\\b(chapitre|chapter|chap|ch|episode|episodes|ep|tome|tomes|vol|volume|volumes|saison|season|part|partie|arc)\\s*\\.?\\s*\\d+(\\.\\d+)?\\b/g;\n\n  // Normalising is pure but not cheap — an NFKD pass and six regexes per title\n  // — and findMatches asks for the candidate's title again for every entry it\n  // compares against, so a library of 200 pays for the same work 200 times.\n  // Bounded so a long-running service worker cannot grow it without limit.\n  const titleCache = new Map();\n  const TITLE_CACHE_MAX = 1000;\n\n  /**\n   * Reduce a displayed title to the part that identifies the work.\n   * \"Ao no Hako VF — Chapitre 109 | Lecture en ligne\" → \"ao no hako\"\n   */\n  function normalizeTitle(raw) {\n    const key = typeof raw === 'string' ? raw : null;\n    if (key !== null && titleCache.has(key)) return titleCache.get(key);\n    const out = computeNormalizedTitle(raw);\n    if (key !== null) {\n      if (titleCache.size >= TITLE_CACHE_MAX) titleCache.delete(titleCache.keys().next().value);\n      titleCache.set(key, out);\n    }\n    return out;\n  }\n\n  function computeNormalizedTitle(raw) {\n    let s = String(raw ?? '')\n      // Strip accents so \"Bleach — Édition\" and \"Bleach Edition\" agree.\n      .normalize('NFKD').replace(/[̀-ͯ]/g, '')\n      .toLowerCase()\n      // Apostrophes join, they do not separate: \"O'Brien\" is one word.\n      .replace(/['’ʼ`]/g, '')\n      .replace(/&/g, ' and ')\n      .replace(/[^\\p{L}\\p{N}]+/gu, ' ')\n      .replace(/\\s+/g, ' ')\n      .trim();\n\n    // Only strip noise when something identifying survives it — \"Manga Dogs\"\n    // and \"Free!\" are real titles made entirely of words on that list.\n    const stripped = s.replace(COUNTER, ' ').replace(NOISE, ' ').replace(/\\s+/g, ' ').trim();\n    if (stripped) s = stripped;\n    return s;\n  }\n\n  /* ---------- the same title, for a human ---------- */\n\n  // Single words from NOISE (the multi-word entries are covered word by word by\n  // TRAILING_EXTRA), plus the language and edition tags a site appends that are\n  // deliberately NOT in NOISE: normalizeTitle also uses that list, and \"fr\" or\n  // \"en\" landing inside a real title is a bet worth losing on display and not\n  // worth losing on matching.\n  const NOISE_SET = new Set(NOISE_WORDS.filter((w) => !w.includes(' ')));\n  // `read` and `lire` are here rather than in NOISE_WORDS, where they only\n  // exist inside \"read online\" / \"lire en ligne\": stripping the tail one word\n  // at a time takes \"Online\" off first, and the \"Read\" left behind matched\n  // nothing. They earn their place at the *head* too — \"Read One Piece Manga\"\n  // is what MangaNato calls the page — which is why this is no longer named for\n  // the tail. Both ends lean on the same two-cut rule to stay honest.\n  const EXTRA_WORDS = [\n    'fr', 'en', 'es', 'it', 'pt', 'de', 'id', 'ar', 'jp', 'ja', 'ko', 'zh',\n    'sub', 'subbed', 'subs', 'eng', 'ita', 'esp', 'multi',\n    'ligne', 'lire', 'lecture', 'read', 'serie', 'series',\n    'chapitre', 'chapitres', 'chapter', 'chapters',\n  ];\n  const BUILTIN_FURNITURE = new Set([...NOISE_SET, ...EXTRA_WORDS]);\n\n  // --- the same list, but changeable without a release -----------------------\n  //\n  // Everything above is what shipped, and it is right until a site renames its\n  // tail — \"Scan VF\" becomes \"Lecture Scan FR\" — at which point a word is\n  // wrong and an extension release is a very large unit of work for one word.\n  // `detection-rules.json` already reaches every client on a six-hour TTL, so\n  // the vocabulary rides along with it:\n  //\n  //   \"titleNoise\": { \"words\": [...], \"keep\": [...] }   at the top level\n  //   \"domains\": { \"*.example.fr\": { \"titleNoise\": { ... } } }\n  //\n  // `keep` is there because the words are the dangerous half. \"Sword Art\n  // Online\", \"Manga Dogs\" and \"Free!\" are real titles built out of listed\n  // words; the two-cut rule in displayTitle is what usually saves them, and\n  // when a site proves it is not enough, `keep` says so for that site alone\n  // rather than by weakening the rule everywhere.\n  const WORDS_MAX = 200;\n\n  // Anything that is not a list of words contributes nothing and throws\n  // nothing. A rules file is edited by hand and served to every client at\n  // once, so the shape it arrives in is not something this can insist on.\n  const wordList = (v) => (Array.isArray(v) ? v : [])\n    .filter((w) => typeof w === 'string')\n    .map((w) => w.trim().toLowerCase())\n    .filter(Boolean)\n    .slice(0, WORDS_MAX);\n\n  const noiseSection = (o) =>\n    (o && typeof o === 'object' && !Array.isArray(o) && o.titleNoise) || null;\n\n  // One entry, because the callers sweep a page of results or a library of one\n  // host at a time. Keyed on the rules object's identity as well as the host:\n  // the clients replace it wholesale when the TTL expires.\n  let vocabCache = null;\n\n  /**\n   * The furniture words in force for one host: what shipped, plus the file's\n   * global additions, plus that domain's own, minus everything any of them\n   * asked to keep. The domain is applied last, so it wins.\n   *\n   * A malformed section costs its own site its extra words and nothing else —\n   * not the built-in list, not the other 49 domains, and not the call.\n   */\n  function furnitureFor(opts) {\n    const o = opts || {};\n    const rules = o.rules || {};\n    const host = o.host ? String(o.host) : '';\n    if (vocabCache && vocabCache.host === host && vocabCache.rules === o.rules) {\n      return vocabCache.words;\n    }\n\n    let own = null;\n    // Which entry covers this host is site-rules.js's answer — wildcards, the\n    // www. that is not part of the name, the apex a `*.` key also stands for.\n    // Read off the global at call time: both are plain scripts, and a client\n    // that loaded only this one must lose the per-domain list, not the title.\n    const sites = root.PanelFlowSites;\n    if (host && sites && typeof sites.domainRule === 'function') {\n      try { own = sites.domainRule(host, rules.domains); } catch { own = null; }\n    }\n\n    const words = new Set(BUILTIN_FURNITURE);\n    for (const section of [noiseSection(rules), noiseSection(own)]) {\n      if (!section || typeof section !== 'object') continue;\n      for (const w of wordList(section.words)) words.add(w);\n      for (const w of wordList(section.keep)) words.delete(w);\n    }\n    vocabCache = { host, rules: o.rules, words };\n    return words;\n  }\n  const SEP = '\\\\s»«|•·:,;–—\\\\-/()\\\\[\\\\]{}';\n  const SEP_END = new RegExp(`[${SEP}.!]+$`);\n  // Trimming the ends is not the same job as finding a word boundary, so it\n  // works off a smaller class: a bracket at the edge belongs to the title as\n  // long as its partner is still in the string. MANGA Plus prints\n  // \"[#002] Shunrai Table Tennis\", and dropping the \"[\" alone left\n  // \"#002] Shunrai Table Tennis\" — a worse title than the one we were given.\n  const EDGE = '\\\\s»«|•·:,;–—\\\\-/';\n  const EDGES = new RegExp(`^[${EDGE}]+|[${EDGE}]+$`, 'g');\n  const PARTNER = { '(': ')', '[': ']', '{': '}', ')': '(', ']': '[', '}': '{' };\n\n  // An orphaned bracket *is* furniture: it is what is left when the words it\n  // wrapped were the site's, and it is the only thing this strips that the\n  // string did not arrive with a matching half of.\n  function trimEdges(str) {\n    let out = String(str).replace(EDGES, '').trim();\n    for (;;) {\n      const open = /^([([{])/.exec(out);\n      if (open && !out.includes(PARTNER[open[1]])) { out = out.slice(1).replace(EDGES, ''); continue; }\n      const close = /([)\\]}])$/.exec(out);\n      if (close && !out.includes(PARTNER[close[1]])) { out = out.slice(0, -1).replace(EDGES, ''); continue; }\n      break;\n    }\n    return out.trim();\n  }\n  // Leading separators are stripped off the smaller EDGE class, not off SEP: SEP\n  // holds the brackets, and taking \"[\" off the front of \"[#002] Shunrai\" would\n  // put back the very half-a-bracket trimEdges() exists to avoid.\n  const SEP_START = new RegExp(`^[${EDGE}.!]+`);\n  const WORD_END = new RegExp(`(^|[${SEP}])([\\\\p{L}\\\\p{N}]+)$`, 'u');\n  const WORD_START = new RegExp(`^([\\\\p{L}\\\\p{N}]+)([${SEP}]|$)`, 'u');\n  const COUNTER_END = new RegExp(\n    `(^|[${SEP}])(chapitre|chapter|chap|ch|episode|ep|tome|vol|volume|saison|season|part|partie)\\\\s*\\\\.?\\\\s*\\\\d+(\\\\.\\\\d+)?$`,\n    'iu');\n\n  /**\n   * The same title, minus the site's furniture — for display, not for matching.\n   * \"Blue Box Scan VF / FR Gratuit (Webtoon)\" → \"Blue Box\"\n   *\n   * normalizeTitle() answers \"are these the same work\" and is free to lowercase\n   * the string and flatten it to nothing; this one has to hand back something a\n   * reader recognises, so it only removes whole words.\n   *\n   * Mostly from the end, which is where a site appends. But not only: MangaNato\n   * titles its chapter pages \"Read One Piece Manga Chapter 1140 …\", and trimming\n   * the tail alone left \"Read One Piece Manga\" on the shelf. So the head is\n   * tried too, once the tail has nothing left to give.\n   *\n   * What keeps that honest is the count, not the word list: a *run* of furniture,\n   * never one word. Plenty of real titles end — or start — with a word on the\n   * list: \"Sword Art Online\", \"Manga Dogs\", \"Free!\". One is not evidence. Two is.\n   * That deliberately leaves \"Naruto Scan\" alone: a conservative miss shows a\n   * slightly long title, an eager one renames somebody's series.\n   *\n   * Never empty. A run of furniture that eats the whole string is a site whose\n   * page is titled nothing but furniture, or a word list that went too far, and\n   * either way the caller would rather have what it handed in: a title is the\n   * only thing on a library card that cannot be worked out again afterwards.\n   *\n   * @param {string} raw\n   * @param {object} [opts]        omitted, the built-in word list applies\n   * @param {string} [opts.host]   so the site's own section is consulted\n   * @param {object} [opts.rules]  the parsed detection-rules.json\n   */\n  function displayTitle(raw, opts) {\n    const words = opts ? furnitureFor(opts) : BUILTIN_FURNITURE;\n    const isFurniture = (w) => words.has(String(w).toLowerCase());\n    const original = trimEdges(String(raw ?? ''));\n    let s = original;\n    let cut = 0;\n    for (;;) {\n      const trimmed = s.replace(SEP_END, '').replace(SEP_START, '');\n      const counter = COUNTER_END.exec(trimmed);\n      if (counter) { s = trimmed.slice(0, counter.index); cut++; continue; }\n      const word = WORD_END.exec(trimmed);\n      if (word && isFurniture(word[2])) { s = trimmed.slice(0, word.index); cut++; continue; }\n      // The head, last: the tail is where furniture usually is, and a title that\n      // opens on a listed word (\"Manga Dogs\") is likelier to mean it. Never the\n      // last word standing — \"Scan\" alone is not a title, it is what is left of\n      // one, and the caller would rather have the string it came in with.\n      const head = WORD_START.exec(trimmed);\n      if (head && isFurniture(head[1]) && trimEdges(trimmed.slice(head[1].length))) {\n        s = trimmed.slice(head[1].length); cut++; continue;\n      }\n      s = trimmed;\n      break;\n    }\n    const out = trimEdges(s);\n    return cut >= 2 && out ? out : original;\n  }\n\n  function bigrams(s) {\n    const out = [];\n    for (let i = 0; i < s.length - 1; i++) out.push(s.slice(i, i + 2));\n    return out;\n  }\n\n  /**\n   * Sørensen–Dice over character bigrams: 1 for identical, 0 for unrelated.\n   * Chosen over edit distance because it shrugs off word order — \"Blue Box\"\n   * vs \"Box, Blue\" — and off a missing subtitle, which is the common case.\n   */\n  function similarity(a, b) {\n    if (a === b) return a ? 1 : 0;\n    if (a.length < 2 || b.length < 2) return 0;\n    const A = bigrams(a);\n    const B = bigrams(b);\n    const counts = new Map();\n    for (const g of A) counts.set(g, (counts.get(g) ?? 0) + 1);\n    let hits = 0;\n    for (const g of B) {\n      const n = counts.get(g) ?? 0;\n      if (n > 0) { counts.set(g, n - 1); hits++; }\n    }\n    return (2 * hits) / (A.length + B.length);\n  }\n\n  // Above STRONG the titles are the same work under two spellings; between\n  // WEAK and STRONG they are close enough to be worth a question and no more.\n  const STRONG = 0.9;\n  const WEAK = 0.74;\n  // Below this length a bigram score means nothing — \"Gantz\" and \"Gantz:O\"\n  // score 0.8 but so do \"Naruto\" and \"Narumi\". Short titles must match exactly.\n  const MIN_FUZZY_LEN = 8;\n\n  const titlesOf = (x) => [x?.title, ...(x?.altTitles || []), ...(x?.alternativeTitles || [])]\n    .map(normalizeTitle)\n    .filter(Boolean);\n\n  /** Best similarity across every title/alt-title pair of two works. */\n  function bestTitleScore(a, b) {\n    const A = titlesOf(a);\n    const B = titlesOf(b);\n    let best = 0;\n    for (const x of A) {\n      for (const y of B) {\n        if (x === y) return 1;\n        if (x.length < MIN_FUZZY_LEN || y.length < MIN_FUZZY_LEN) continue;\n        const s = similarity(x, y);\n        if (s > best) best = s;\n      }\n    }\n    return best;\n  }\n\n  /**\n   * How strongly does `candidate` look like `entry`?\n   *   same-page  the very same URL\n   *   same-site  same work, same site, different URL shape\n   *   same-title titles are identical once normalised — a source migration\n   *   likely     close enough to ask about\n   *   null       unrelated\n   */\n  function classify(candidate, entry) {\n    const cUrl = candidate?.sourceUrl;\n    const eUrl = entry?.sourceUrl;\n    if (cUrl && eUrl) {\n      if (normUrl(cUrl) === normUrl(eUrl)) return { confidence: 'same-page', score: 1 };\n      if (sameSeries(cUrl, eUrl)) return { confidence: 'same-site', score: 1 };\n    }\n    const score = bestTitleScore(candidate, entry);\n    if (score >= STRONG) return { confidence: 'same-title', score };\n    if (score >= WEAK) return { confidence: 'likely', score };\n    return null;\n  }\n\n  const RANK = { 'same-page': 4, 'same-site': 3, 'same-title': 2, likely: 1 };\n\n  /**\n   * Every library entry that might already be this work, best first.\n   * `library` entries need at least { title, sourceUrl }.\n   */\n  function findMatches(candidate, library) {\n    const out = [];\n    for (const entry of library || []) {\n      const m = classify(candidate, entry);\n      if (m) out.push({ entry, ...m });\n    }\n    return out.sort((a, b) => (RANK[b.confidence] - RANK[a.confidence]) || (b.score - a.score));\n  }\n\n  /** The single match worth interrupting the user for, or null. */\n  function bestMatch(candidate, library) {\n    return findMatches(candidate, library)[0] ?? null;\n  }\n\n  // Chapter labels are free text (\"Ch. 109\", \"Chapitre 109 VF\", \"109.5\"), so\n  // the only comparable part is the first number in them.\n  function chapterNumber(label) {\n    const m = /\\d+(?:\\.\\d+)?/.exec(String(label ?? ''));\n    return m ? Number(m[0]) : null;\n  }\n\n  /**\n   * The label that is furthest into the series. An unparseable label beats\n   * nothing at all but never beats a real number — losing chapters to a\n   * merge is worse than keeping a label nobody can sort.\n   */\n  function furtherChapter(...labels) {\n    let best = null;\n    let bestRank = -Infinity;\n    for (const label of labels) {\n      if (label === undefined || label === null || label === '') continue;\n      const rank = chapterNumber(label) ?? -1;\n      if (rank > bestRank) { bestRank = rank; best = label; }\n    }\n    return best;\n  }\n\n  const api = {\n    normUrl, seriesKey, sameSeries,\n    normalizeTitle, displayTitle, similarity, bestTitleScore,\n    classify, findMatches, bestMatch,\n    chapterNumber, furtherChapter,\n    STRONG, WEAK, MIN_FUZZY_LEN,\n  };\n\n  root.PanelFlowMatch = api;\n})(typeof globalThis !== 'undefined' ? globalThis : self);\n\n}catch(e){if(window.PanelFlowFailed)window.PanelFlowFailed(\"series-match.js\",e);else console.warn('panelflow: series-match.js failed',e)}\ntry{\n// Which reader built this page, and what does that reader call things?\n//\n// The generic heuristics — `extension/content/detect.js` on a live DOM,\n// `shared/compat.js` on raw markup — work on a site nobody has ever seen, and\n// they are what carries most of the web. What they cannot do is disagree with\n// the page: image clustering picks the container holding the most panels, and on\n// a page whose \"you may also like\" strip is bigger than the chapter, that is the\n// wrong container. A per-site rule settles it.\n//\n// The obvious way to write one is a hostname, which is also why the rules file\n// shipped for months with a single fake domain in it. There are thousands of\n// scan sites, they rename themselves, and they front the same install behind\n// ww1/ww3/ww6 counters: a list of hostnames is a list that is wrong by the time\n// it ships, and every entry has to be verified against a site by hand.\n//\n// So the rules are keyed on the *engine* instead. A handful of WordPress themes\n// and open-source readers sit behind most of these sites, each with markup its\n// author wrote once and nobody since has touched. `.reading-content` is Madara\n// wherever it turns up — on a host nobody has heard of, under a name that\n// changed last week. Recognise the engine and one entry covers a thousand hosts\n// nobody had to find first.\n//\n// `domains` stays, for the site that genuinely needs its own answer, and what it\n// says wins over whatever engine it runs.\n//\n// Pure: a hostname, the rules file, and either a way to put a question to the\n// DOM or the raw markup. No DOM, no fetch, no storage — the same file runs in\n// the extension's content scripts, the phone's worker WebView and `node --test`.\n(function (root) {\n  'use strict';\n\n  /**\n   * Every key under `domains` that could stand for this host, most specific\n   * first: the host as given, the host without `www.`, then each parent as a\n   * wildcard. The wildcards are what survive a site moving to `ww6.` overnight,\n   * and `*.example.com` deliberately covers the apex as well — someone writing\n   * it means the site, not the subdomains of the site.\n   *\n   * A wildcard is never generated for a bare suffix: the loop stops one label\n   * short, so `*.com` is not a key any host can match.\n   */\n  function hostKeys(host) {\n    const raw = String(host || '').toLowerCase().replace(/\\.$/, '');\n    if (!raw) return [];\n    const bare = raw.replace(/^www\\./, '');\n    const keys = raw === bare ? [raw] : [raw, bare];\n    const parts = bare.split('.');\n    for (let i = 0; i < parts.length - 1; i++) keys.push(`*.${parts.slice(i).join('.')}`);\n    return keys;\n  }\n\n  /** This host's own entry in the rules file, or null. */\n  function domainRule(host, domains) {\n    const map = domains || {};\n    for (const key of hostKeys(host)) {\n      if (Object.prototype.hasOwnProperty.call(map, key)) return map[key];\n    }\n    return null;\n  }\n\n  /**\n   * Which engine built this page.\n   *\n   * `ask` is a `(selector) => boolean` predicate over a live DOM; `html` is the\n   * raw markup, for the callers that have no DOM to ask. A DOM is authoritative:\n   * when one is offered and none of the engines match it, the markup is *not*\n   * consulted as a second opinion — the page is simply not one of these.\n   *\n   * Engines are tried in the order the rules file lists them, so the file\n   * controls precedence and the most distinctive markup goes first.\n   */\n  function sniffEngine(engines, opts) {\n    const map = engines || {};\n    const ask = opts && typeof opts.ask === 'function' ? opts.ask : null;\n    if (ask) {\n      for (const id of Object.keys(map)) {\n        const selectors = (map[id] || {}).detect || [];\n        // A selector the rules file got wrong is a syntax error inside\n        // querySelector, and it must cost this engine its turn, not the scan.\n        if (selectors.some((sel) => { try { return !!ask(sel); } catch { return false; } })) {\n          return id;\n        }\n      }\n      return null;\n    }\n    const markup = String((opts && opts.html) || '').toLowerCase();\n    if (!markup) return null;\n    for (const id of Object.keys(map)) {\n      const signature = (map[id] || {}).signature || [];\n      if (signature.some((s) => markup.includes(String(s).toLowerCase()))) return id;\n    }\n    return null;\n  }\n\n  /**\n   * What this site calls things.\n   *\n   * @param {object} opts\n   * @param {string} opts.host    the page's hostname\n   * @param {object} opts.rules   the parsed `shared/detection-rules.json`\n   * @param {function} [opts.ask] `(selector) => boolean` against the live DOM\n   * @param {string} [opts.html]  the raw markup, when there is no DOM\n   * @returns {?object} the engine's selectors with the domain's own merged over\n   *   them, plus `engine` (the id, or null) and `knownDomain` (whether the rules\n   *   file names this host outright). Null when nothing recognised the page,\n   *   which is the case the heuristics were written for.\n   */\n  function resolveSite(opts) {\n    const o = opts || {};\n    const rules = o.rules || {};\n    const engines = rules.engines || {};\n    const own = domainRule(o.host, rules.domains);\n\n    // A domain entry may name its engine outright — that is how a site whose\n    // markup gives nothing away still gets the engine's selectors — and naming\n    // one that does not exist falls back to sniffing rather than to nothing.\n    let engine = own && own.engine ? String(own.engine) : null;\n    if (engine && !engines[engine]) engine = null;\n    if (!engine) engine = sniffEngine(engines, o);\n\n    if (!own && !engine) return null;\n\n    const site = Object.assign({}, engine ? engines[engine].rule : null, own);\n    site.engine = engine;\n    site.knownDomain = !!own;\n    return site;\n  }\n\n  // --- what a URL calls its chapter ------------------------------------------\n  //\n  // Both detectors read a chapter number out of a URL or a title, and both used\n  // to carry their own copy of one small regex. It moved here — the one file\n  // every client loads before either of them — because the rule stopped being\n  // obvious enough to duplicate.\n  //\n  // MangaDex addresses a chapter by UUID:\n  //\n  //   /chapter/3bde6546-e07c-4aca-9bb2-76764ba95ebe   ← chapter 26\n  //\n  // and the old pattern read \"chapter\", then \"/\", then \"3\", and reported\n  // chapter 3. The library believed it: `chapterVisited` is forward-only, so a\n  // UUID beginning with a 9 moved a reader's bookmark to chapter 9 of a series\n  // they were five chapters into, and nothing in the app could move it back.\n  //\n  // Two guards, both cheap:\n  //\n  //   - the digits may not be followed by another letter or digit. A chapter\n  //     number ends its token; the first digit of a hex blob does not.\n  //   - the number may not reach 10000, the bound the rest of the project\n  //     already uses to fend off timestamps, view counts and prices.\n  //\n  // The keyword needs a boundary in front of it now as well, so `ch` stops\n  // matching inside `watch2` and `recherche7`.\n  const CHAPTER_IN =\n    /(?:^|[^a-z0-9])(?:chapter|chapitre|chap|ch|episode)[-_/ .]*(\\d+(?:\\.\\d+)?)(?![0-9a-z])/i;\n\n  // The other unit people read in. Plenty of French scan sites publish a whole\n  // tome at a time — sushiscan's Bleach is 74 volumes and one stray chapter —\n  // and a page holding 188 panels was being turned away at the door because\n  // nothing on it said \"chapitre\". Same shape and same two guards as above.\n  //\n  // `volume` is listed before `vol` so the long spelling wins; `vol` on its own\n  // needs digits right after it, which is what keeps it out of `volleyball-2`.\n  const VOLUME_IN =\n    /(?:^|[^a-z0-9])(?:volume|tome|vol)[-_/ .]*(\\d+(?:\\.\\d+)?)(?![0-9a-z])/i;\n\n  const numberIn = (pattern, text) => {\n    const hit = String(text || '').match(pattern);\n    if (!hit) return null;\n    const n = parseFloat(hit[1]);\n    return Number.isNaN(n) || n >= 10000 ? null : hit[1];\n  };\n\n  /**\n   * The chapter number a string names, spelled the way it was written (\"109\",\n   * \"109.5\"), or null when it names none. A string, not a number: \"07\" and\n   * \"7\" are the same chapter but not the same label, and the callers that want\n   * arithmetic parse it themselves.\n   */\n  const chapterNumber = (text) => numberIn(CHAPTER_IN, text);\n\n  /**\n   * The volume number a string names, same rules.\n   *\n   * Deliberately a second function rather than a wider CHAPTER_IN: a volume\n   * number is not a chapter number, and everything downstream that ranks\n   * progress — the forward-only bookmark, the tracker push, \"is there a further\n   * chapter\" — reads chapterNumber() off a label. Teaching that one function\n   * about volumes would have pushed \"chapter 1\" to MyAnimeList for somebody who\n   * just read tome 1, which contains chapters 1 to 7. A volume is read, filed\n   * and resumable; it just does not claim a chapter's number.\n   */\n  const volumeNumber = (text) => numberIn(VOLUME_IN, text);\n\n  root.PanelFlowSites = {\n    resolveSite, domainRule, sniffEngine, hostKeys, chapterNumber, volumeNumber,\n  };\n})(typeof globalThis !== 'undefined' ? globalThis : self);\n\n}catch(e){if(window.PanelFlowFailed)window.PanelFlowFailed(\"site-rules.js\",e);else console.warn('panelflow: site-rules.js failed',e)}\ntry{\n// PanelFlow detection engine (content script, document_idle).\n//\n// The product's central bet lives here: PanelFlow works on **any** manga site,\n// not on a list of them. This file decides, from the page alone, whether what\n// the reader is looking at is a chapter — and if it is, offers a pill. It never\n// switches by itself. That single rule is what makes a wrong answer cost an\n// ordinary browser tab and nothing else, and it is why the heuristics below are\n// allowed to be heuristics.\n//\n// **How the answer is reached.** A page scores against\n// `shared/detection-rules.json` and passes at 50:\n//\n//     knownDomain    100   someone looked at this site and wrote a rule\n//     imageGallery    40   several big images clustered in one container\n//     knownEngine     40   the markup of a CMS theme (Madara, Themesia, …)\n//     urlPattern      20   /chapitre-109/, /read/, …\n//     chapterNav      20   a \"next chapter\" link\n//     lowTextDensity  10   pictures, not an article\n//\n// `knownEngine` is 40 and not 50 on purpose: a theme's own home page is built\n// from that theme too, so recognising the engine must not be enough on its own.\n//\n// **Reading order.** Six sections, and they are a pipeline:\n//\n//   which site is this?   → asks shared/site-rules.js; never a hostname test\n//   scoring               → galleryImages() and the weights above\n//   is this a chapter?    → the veto: a catalogue also has big images\n//   prose chapters        → light novels, where there are no images at all\n//   pill UI               → the offer, and only ever an offer\n//   scan orchestration    → when to re-run: SPA navigation, lazy loading\n//\n//     grep -n '^\\s*// ---' extension/content/detect.js\n//\n// **Before changing anything here, ask whether the fix belongs in\n// `shared/detection-rules.json` instead.** A site that stopped working is\n// almost always a selector, and that file reaches every client within six hours\n// with no release. Code changes here ship on the store's schedule.\n//\n// Dependency-free and guarded (`window.__panelflowDetectLoaded`) so it can be\n// injected repeatedly; the phones inject this exact file through a `chrome.*`\n// shim. There is no mobile fork.\n(() => {\n  'use strict';\n  if (window.top !== window) return; // top frame only\n  if (window.__panelflowDetectLoaded) return;\n  window.__panelflowDetectLoaded = true;\n\n  const FALLBACK_RULES = {\n    heuristics: {\n      scoreThreshold: 50,\n      minGalleryImages: 3,\n      minImageWidth: 400,\n      urlPatterns: [\n        '/(manga|manhwa|manhua|comic|scan|webtoon)s?/',\n        '[/_-](chapter|chapitre|chap|ch)[-_/ ]?\\\\d+',\n        '/read(er)?/',\n      ],\n      navTextPatterns: ['next chapter', 'previous chapter', 'chapitre suivant'],\n      weights: {\n        imageGallery: 40, urlPattern: 20, chapterNav: 20, lowTextDensity: 10,\n        knownEngine: 40, knownDomain: 100,\n      },\n    },\n    engines: {},\n    domains: {},\n  };\n\n  let rules = FALLBACK_RULES;\n  let detection = null; // { score, container, images, domainRule }\n  // What this site calls things, once something recognises it — see siteFor().\n  let site = null;\n  // Up here, not next to scheduleScan() 550 lines down: the getRules callback\n  // below is the first thing to call it, and a host that answers synchronously\n  // (a shim, a mock) would hit the dead zone of a `let` further down the file\n  // and take the whole detector out with a ReferenceError.\n  let scanTimer = null;\n\n  chrome.runtime.sendMessage({ type: 'getRules' }, (resp) => {\n    if (!chrome.runtime.lastError && resp && resp.rules) {\n      rules = resp.rules;\n      site = null; // whatever the fallback rules concluded was answered blind\n    }\n    scheduleScan();\n  });\n\n  // --- which site is this? -------------------------------------------------\n\n  /** The first match for a selector, without letting a bad one throw at us. */\n  const firstMatch = (sel) => {\n    try { return sel ? document.querySelector(sel) : null; } catch { return null; }\n  };\n\n  // Which chapter a URL or a title names — see shared/site-rules.js, which owns\n  // the rule so that this file and compat.js cannot disagree about it. Read\n  // through the global rather than assumed: site-rules.js is injected first\n  // everywhere (a test enforces the order), but the phone shells wrap each\n  // injected file in its own try/catch, and one that failed to parse must cost\n  // a chapter label, not the whole detector.\n  const chapterNumber = (text) => window.PanelFlowSites?.chapterNumber?.(text) ?? null;\n  const volumeNumber = (text) => window.PanelFlowSites?.volumeNumber?.(text) ?? null;\n\n  /**\n   * What this site calls things: its own entry in the rules file, or the reader\n   * engine its markup gives away (shared/site-rules.js). Kept once found —\n   * scan() runs again on every mutation of an infinite-scrolling reader, and\n   * this is a handful of querySelector calls each time. A miss is deliberately\n   * not cached: the engine's markup may simply not have been built yet.\n   */\n  function siteFor() {\n    if (!site && window.PanelFlowSites) {\n      site = window.PanelFlowSites.resolveSite({\n        host: location.hostname,\n        rules,\n        ask: (sel) => !!firstMatch(sel),\n      });\n    }\n    return site;\n  }\n\n  // --- scoring -------------------------------------------------------------\n\n  function galleryImages() {\n    const h = rules.heuristics;\n    // When the engine names the reading strip, look inside it and nowhere else.\n    // Clustering below picks the container holding the most panels, and on a\n    // page whose \"you may also like\" carousel is bigger than the chapter, that\n    // is the wrong container. Additive, never subtractive: a selector that has\n    // stopped matching — or that holds nothing yet — hands the document back.\n    const scope = firstMatch(siteFor()?.imageContainer);\n    let imgs = [...(scope ? scope.querySelectorAll('img') : document.images)].filter(sizedImage);\n    if (scope && imgs.length < h.minGalleryImages) {\n      imgs = [...document.images].filter(sizedImage);\n    }\n    if (imgs.length < h.minGalleryImages) return null;\n\n    // Group candidate images by ancestor container (up to 4 levels up) and\n    // pick the container holding the most of them — that's the reading strip.\n    // An image walks its own ancestors, each of them once, so a list can never\n    // hold the same image twice and appending beats rebuilding the array on\n    // every step: a 200-panel strip copied 800 arrays to learn nothing.\n    const counts = new Map();\n    for (const img of imgs) {\n      let node = img.parentElement;\n      for (let depth = 0; node && depth < 4; depth++, node = node.parentElement) {\n        const list = counts.get(node);\n        if (list) list.push(img);\n        else counts.set(node, [img]);\n      }\n    }\n    let best = null;\n    for (const [node, list] of counts) {\n      if (list.length >= h.minGalleryImages && (!best || list.length > best.images.length)) {\n        best = { container: node, images: list };\n      }\n    }\n    if (!best) return null;\n    best.images.sort((a, b) =>\n      a.compareDocumentPosition(b) & Node.DOCUMENT_POSITION_FOLLOWING ? -1 : 1);\n    return best;\n  }\n\n  /** An image big enough, and laid out enough, to be a page of a chapter. */\n  function sizedImage(img) {\n    const h = rules.heuristics;\n    const r = img.getBoundingClientRect();\n    // Nothing decoded yet, so there is nothing to measure but the room the page\n    // has made for it. A panel-width box with an address in it is a page on its\n    // way, and judging it on the height it does not have yet is how we used to\n    // lose whole chapters: mangas-origines holds back everything past page 2\n    // (.ori-planche-attente, rendered height 0) and natomanga holds back all 25\n    // for the first second, so both sites had two candidates and no reader.\n    //\n    // What the browser decoded is not always the panel either. A lazy-loading\n    // theme (sushiscan.net among them) parks a spacer gif in `src` and keeps\n    // the address in data-src, so naturalWidth answers for the spacer: every\n    // panel below the fold failed the width test and the reader opened on the\n    // four that happened to be on screen. Whenever lazySrc() would hand back\n    // some other address, the decoded size is the wrong thing to measure.\n    const address = lazySrc(img);\n    if (!img.naturalWidth || address !== (img.currentSrc || img.src)) {\n      return r.width >= h.minImageWidth && Boolean(address);\n    }\n    if (img.naturalWidth < h.minImageWidth || img.naturalHeight < 200) return false;\n    // One collapsed dimension is a layout choice; both collapsed is display:none,\n    // and that one still means no.\n    return r.width > 0 || r.height > 0;\n  }\n\n  function isVisible(el) {\n    const r = el.getBoundingClientRect();\n    return r.width > 0 && r.height > 0;\n  }\n\n  // --- is this page actually a chapter? ------------------------------------\n  //\n  // Scoring alone cannot tell a reading strip from a catalogue: a home page's\n  // cover carousel is also \"several big images in one container\", and on a\n  // wide screen it clears the threshold outright. Two structural gates below\n  // decide it instead, both measured on the live sites:\n  //   chapter  /comics/…/chapter/270  → 20 images, all x=273 w=720, y climbing\n  //   carousel /                      → 3+ images, all y≈250, x climbing\n  //\n  // How many distinct horizontal bands the images occupy. A carousel is one\n  // band however many covers it holds; a reading strip is one band per panel.\n  // A fixed tolerance, not one scaled to the image: panels run to 12800px tall\n  // and a proportional window would let one swallow every row below it. Items\n  // sharing a band are top-aligned within a few pixels either way.\n  const ROW_TOLERANCE = 40;\n\n  function rowCount(images) {\n    const tops = [];\n    for (const img of images) {\n      const top = img.getBoundingClientRect().top;\n      if (!tops.some((t) => Math.abs(t - top) < ROW_TOLERANCE)) tops.push(top);\n    }\n    return tops.length;\n  }\n\n  // \"Next\" on its own is a carousel arrow. Only the full phrase means the page\n  // has chapter navigation, which is the thing a catalogue never has.\n  const STRONG_NAV =\n    /(next|previous|prev)\\s+(chapter|chap|episode)|chapitre\\s+(suivant|pr[eé]c[eé]dent)|次の話|前の話/i;\n\n  // The first 400 clickable things on the page, walked without ever building\n  // the other several thousand. `[...querySelectorAll(…)].slice(0, 400)` spreads\n  // the whole list first, and the pages this runs on — a catalogue, an\n  // infinite-scrolling reader — are exactly the pages with thousands of links,\n  // on a scan that repeats until a detection sticks.\n  const CLICKABLE_SCAN = 400;\n  function someClickable(fn) {\n    const found = document.querySelectorAll('a, button');\n    const end = Math.min(found.length, CLICKABLE_SCAN);\n    for (let i = 0; i < end; i++) if (fn(found[i])) return true;\n    return false;\n  }\n\n  function hasChapterNav() {\n    return someClickable((a) => {\n      const t = (a.textContent || '').trim();\n      return t.length < 40 && STRONG_NAV.test(t);\n    });\n  }\n\n  // A chapter page names its chapter — in the URL or the <title> — or carries\n  // prev/next chapter links. A home page or a series listing does neither.\n  function chapterEvidence() {\n    if (location.pathname === '/') return false; // a home page is never a chapter\n    if (chapterLabelHere()) return true;\n    if (/\\/read(er)?(\\/|$)/i.test(location.pathname)) return true;\n    return hasChapterNav();\n  }\n\n  // --- prose chapters ------------------------------------------------------\n  //\n  // Plenty of what people follow is text: web novels, light-novel translations,\n  // the novel half of sites that carry both. None of it has a reading strip, so\n  // everything above rejects it, and the reader — bookmark, progress, history,\n  // reading clock — never applies to a series that happens not to be drawn.\n  //\n  // The container is found the way the gallery is: group the candidates by\n  // parent and take the parent holding the most. Prose is the one thing a page\n  // has a lot of, so the risk is not missing it, it is claiming a comment\n  // thread or an article. Hence the length floors, the link-density check, and\n  // a caller that will not accept this without chapter evidence in the URL or\n  // in prev/next links.\n\n  const MIN_NOVEL_CHARS = 1200;\n  const MIN_NOVEL_PARAS = 5;\n  // How long a line has to be before it counts as prose at all.\n  //\n  // This is the guard that was missing, and a chapter picker walked straight\n  // through the gap. On a Madara site the panels arrive late, `.reading-content`\n  // is empty markup for a moment, and what is left in it is the \"Sélectionner le\n  // chapitre\" list — nine hundred entries reading \"Chapitre 882\". Numerous\n  // (well past five), long in total (well past 1200 characters), and made of\n  // <option>s rather than <a>s, so the link-density check below saw no links\n  // and waved it through. The reader then opened on the table of contents,\n  // rendered as a novel, instead of on the chapter.\n  //\n  // 60 is not a new number: `shared/compat.js` — the same verdict reached from\n  // raw markup on the server — has had `PROSE_MIN_LINE = 60` all along, and its\n  // test \"a chapter list is not prose, however long it is\" is the case this\n  // file was failing. The two are meant to agree; one of them simply had the\n  // rule and the other did not.\n  const MIN_NOVEL_LINE = 60;\n  // Where a chapter body lives when the site does not use <p> at all — a single\n  // block of text broken by <br>, which is most of the older novel sites.\n  const NOVEL_CONTAINERS =\n    '#chapter-content, .chapter-content, .entry-content, .reading-content, ' +\n    '.text-content, .chapter-c, .novel-content, article, main';\n\n  /** Text that reads like a chapter: a run of long, mostly link-free lines. */\n  function novelContent() {\n    const fromParagraphs = paragraphContainer();\n    if (fromParagraphs) return fromParagraphs;\n    for (const el of [...document.querySelectorAll(NOVEL_CONTAINERS)].slice(0, 20)) {\n      if (!isVisible(el)) continue;\n      const paras = splitLines(el.innerText || '');\n      if (!longEnough(paras) || linkDensity(el) > 0.25) continue;\n      return { container: el, paragraphs: paras };\n    }\n    return null;\n  }\n\n  function paragraphContainer() {\n    const counts = new Map();\n    for (const p of document.querySelectorAll('p')) {\n      const text = (p.innerText || '').trim();\n      // Short <p>s are bylines, captions, ad slots and cookie notices. A novel\n      // has plenty of one-line dialogue, but never five hundred of them alone.\n      if (text.length < 60 || !isVisible(p)) continue;\n      const parent = p.parentElement;\n      if (!parent) continue;\n      const list = counts.get(parent);\n      if (list) list.push(p);\n      else counts.set(parent, [p]);\n    }\n    let best = null;\n    for (const [container, ps] of counts) {\n      const chars = ps.reduce((n, p) => n + p.innerText.length, 0);\n      if (ps.length >= MIN_NOVEL_PARAS && chars >= MIN_NOVEL_CHARS &&\n          (!best || chars > best.chars) && linkDensity(container) <= 0.25) {\n        best = { container, chars, paragraphs: ps.flatMap((p) => splitLines(p.innerText)) };\n      }\n    }\n    return best && longEnough(best.paragraphs) ? best : null;\n  }\n\n  // A <br>-separated body arrives as one string; a <p> can hold line breaks of\n  // its own. Both come out as one paragraph per line either way.\n  const splitLines = (text) => String(text || '')\n    .split(/\\n+/).map((s) => s.trim()).filter((s) => s.length >= MIN_NOVEL_LINE);\n\n  const longEnough = (paras) =>\n    paras.length >= MIN_NOVEL_PARAS &&\n    paras.reduce((n, s) => n + s.length, 0) >= MIN_NOVEL_CHARS;\n\n  // A chapter body is prose with the odd link in it. A table of contents, a\n  // comment thread and a sidebar are mostly links, and all three are long.\n  function linkDensity(el) {\n    const total = (el.innerText || '').length;\n    if (!total) return 1;\n    let linked = 0;\n    // `option` alongside `a`: a chapter picker is a table of contents that\n    // happens to be a <select>, and counting only anchors scored it at zero —\n    // the emptiest possible answer for the densest possible navigation.\n    for (const a of el.querySelectorAll('a, option')) linked += (a.innerText || '').length;\n    return linked / total;\n  }\n\n  function scorePage() {\n    const h = rules.heuristics;\n    const w = h.weights;\n    let score = 0;\n    // Naming the host outright clears the threshold on its own; recognising the\n    // engine does not, because `.reading-content` is Madara on its home page too.\n    const domainRule = siteFor();\n    if (domainRule) score += domainRule.knownDomain ? w.knownDomain : (w.knownEngine || 0);\n\n    const gallery = galleryImages();\n    if (gallery) score += w.imageGallery;\n\n    const path = location.pathname + location.search;\n    if (h.urlPatterns.some((p) => { try { return new RegExp(p, 'i').test(path); } catch { return false; } })) {\n      score += w.urlPattern;\n    }\n\n    const navPat = h.navTextPatterns.map((t) => t.toLowerCase());\n    if (someClickable((a) => {\n      const t = (a.textContent || '').trim().toLowerCase();\n      return t.length < 40 && navPat.some((p) => t.includes(p));\n    })) score += w.chapterNav;\n\n    // Text density, last and only when it can still change the verdict.\n    // `innerText` is the most expensive line in the whole detector: it forces a\n    // layout of the entire document and walks it, and this runs again on every\n    // mutation of a page that has not settled yet. A known domain is already\n    // past the threshold on its own, and a page too far below it cannot be\n    // rescued by ten points — in both cases the answer is the same either way,\n    // so it is not worth asking the question.\n    if (gallery && score < h.scoreThreshold && score + w.lowTextDensity >= h.scoreThreshold) {\n      const textLen = (document.body.innerText || '').length;\n      if (textLen / gallery.images.length < 800) score += w.lowTextDensity;\n    }\n\n    return { score, gallery, domainRule };\n  }\n\n  // --- pill UI -------------------------------------------------------------\n\n  function showPill() {\n    if (document.getElementById('panelflow-pill')) return;\n    const pill = document.createElement('button');\n    pill.id = 'panelflow-pill';\n    pill.textContent = `📖 ${t('pillReaderMode')}`;\n    pill.title = t('pillReaderModeTitle');\n    // The pill goes away when the reader is up, not when the click lands: if the\n    // panels are not ready the open is a no-op, and a pill removed anyway leaves\n    // the page with no visible way in.\n    pill.addEventListener('click', (e) => {\n      // Not the page's click. The pill sits in the site's document, so without\n      // this the \"click anywhere\" pop-under handlers these sites hang off the\n      // document fire on the one button PanelFlow puts on their page — an ad\n      // tab as the price of opening the reader.\n      e.stopPropagation();\n      openReader().then((ok) => { if (ok) pill.remove(); });\n    });\n    document.documentElement.appendChild(pill);\n  }\n\n  // The rule is a parameter so the track-only path can pass its own: that path\n  // deliberately leaves `detection` unset, and reading the site's heading with\n  // no rule would fall back to <title> on the sites that need the rule most.\n  function seriesMeta(domainRule) {\n    const rule = domainRule || detection?.domainRule;\n    // A heading is usually the series and the chapter in one element, so cut\n    // everything from the chapter counter onwards (\"Blue Box Chapter 5 —\n    // SushiScan\" → \"Blue Box\"). That is a position, not a vocabulary, which is\n    // why it stays here.\n    //\n    // The words are not here any more. This used to carry its own regex of\n    // four SEO words, frozen into a file that only changes when the extension\n    // is republished; the list now lives in detection-rules.json and reaches\n    // every client on a TTL, so the site's own hostname and the rules we\n    // already fetched go along for the site-specific half of it.\n    const clean = (s) => {\n      const cut = String(s || '')\n        .replace(/\\s*(chapter|chapitre|ch\\.?|episode|volume|tome|vol\\.?)\\s*[\\d.]+.*$/i, '')\n        .replace(/^[\\s»«|•·:—–-]+|[\\s»«|•·:—–-]+$/g, '')\n        .trim();\n      const opts = { host: location.hostname, rules };\n      return window.PanelFlowMatch?.displayTitle\n        ? window.PanelFlowMatch.displayTitle(cut, opts) : cut;\n    };\n    let title = null;\n    // The engine's own heading, put through that same cleaner rather than taken\n    // as it stands: most of these readers use one element for the series and the\n    // chapter, so what it holds here is \"Blue Box Chapter 5\" and the series is\n    // called Blue Box. A heading that cleans away to nothing falls through.\n    if (rule && rule.title) title = clean(firstMatch(rule.title)?.textContent);\n    if (!title) {\n      // Falling back to the raw <title> would reintroduce the very chevrons\n      // and separators we just stripped, so clean the fallback too.\n      title = clean(document.title) || clean(document.title.replace(/[|–—:].*$/, '')) ||\n        document.title.replace(/^[\\s»«|•·:—–-]+|[\\s»«|•·:—–-]+$/g, '').trim();\n    }\n    return {\n      title,\n      sourceDomain: location.hostname,\n      sourceUrl: seriesUrlFromDom(title) || seriesUrlGuess(),\n      chapterUrl: location.href,\n      chapterLabel: chapterLabelHere(),\n      coverUrl: coverGuess(),\n      lastKnownChapter: latestChapterInDom(),\n      genres: genresInDom(document, title),\n      // What kind of work this is, decided once and here.\n      //\n      // Three answers, in order of how much they know. The rules file is first\n      // because it is the only one that was told: a domain that says it serves\n      // novels is a fact somebody wrote down, and it costs a line to correct\n      // when a site changes. Then the page itself — a chapter made of prose is\n      // a novel whatever the site is, which is what carries the sites nobody\n      // has classified. Manga last, because it is what every entry in this\n      // library was before the field existed.\n      //\n      // Deliberately not guessing `webtoon` from a long strip: a vertical\n      // manga chapter looks exactly like one, and a wrong shelf that the reader\n      // has to notice is worse than a right one they have to pick.\n      medium: siteFor()?.medium || (novelContent() ? 'novel' : 'manga'),\n      language: languageGuess(),\n      seriesStatus: statusGuess(),\n    };\n  }\n\n  // Genre links are the one piece of catalogue metadata almost every scan site\n  // exposes the same way: anchors pointing at a /genre/ or /tag/ listing.\n  // Used to prefill the tag chips when adding to the library.\n  //\n  // Two kinds of genre link had to be told apart from the series' own, and\n  // neither was. Every page of a scan site carries the site's genre *menu* in\n  // its header, and most carry a \"you may also like\" rail whose cards link to\n  // genres of their own. Kingdom — a war manga running since 2006 — was offered\n  // \"Romance\" and \"Adulte\" as its tags, because those are two entries of\n  // sushiscan's own dropdown and the dropdown is on every page of the site.\n  //\n  // So the site's furniture is skipped, and a page that offers more genres than\n  // any one series has is read as a catalogue rather than as a description.\n  const SITE_CHROME = 'nav, header, footer, aside, form, [role=\"navigation\"],'\n    + ' [role=\"banner\"], [role=\"contentinfo\"], .menu, .menus, .nav, .navbar,'\n    + ' .navigation, .site-header, .site-footer, .mega-menu, .dropdown, .submenu';\n\n  // Eight is the cap the library sheet applies anyway; twelve is the point at\n  // which a list has stopped describing one book. Sushiscan's menu holds forty.\n  const GENRE_CEILING = 12;\n\n  /**\n   * The words a genre may not be made of.\n   *\n   * A genre classifies a work; it never names it. Voiranime lists a series'\n   * seasons and language editions as tags — \"Détective Conan\", \"Détective Conan\n   * saison 3\", \"Détective Conan vostfr\" — and they arrived in the sheet as the\n   * eight tags offered for the series. Every one of them was about that one\n   * series, which is exactly what a genre is not.\n   *\n   * So a candidate that shares its words with the title is dropped. This is not\n   * a rule about that site: any page whose \"tags\" are really links to the work's\n   * own sub-pages produces the same thing, and the same test rejects it.\n   */\n  function namesTheWork(text, title) {\n    if (!title) return false;\n    const bare = (s) => String(s).toLowerCase()\n      .normalize('NFD').replace(/[̀-ͯ]/g, '')\n      .replace(/[^a-z0-9]+/g, ' ').trim();\n    const a = bare(text);\n    const b = bare(title);\n    if (!a || !b) return false;\n    // Either direction: the tag may be the title plus a season, or the title may\n    // be the tag plus an episode.\n    return a === b || a.startsWith(`${b} `) || b.startsWith(`${a} `);\n  }\n\n  function genresInDom(root = document, title = null) {\n    const seen = new Set();\n    for (const a of root.querySelectorAll('a[href*=\"/genre\" i], a[href*=\"/tag\" i], a[rel~=\"tag\"]')) {\n      // `closest` is missing on the elements some of our own tests hand in, and\n      // on nothing a browser produces; no ancestor to check is not a menu.\n      if (a.closest && a.closest(SITE_CHROME)) continue;\n      const text = (a.textContent || '').trim().replace(/\\s+/g, ' ');\n      // Genre labels are short words, never sentences or chapter titles.\n      if (!text || text.length > 24 || /\\d{2,}/.test(text)) continue;\n      if (CHAPTERISH.test(text)) continue;\n      if (namesTheWork(text, title)) continue;\n      seen.add(text);\n      // A menu this file failed to recognise, or the site's own genre index.\n      // Nothing on such a page belongs to the series being read, and the wrong\n      // eight out of forty is worse than none at all: no tags is a form the\n      // reader fills in, wrong tags is one they have to notice first.\n      if (seen.size > GENRE_CEILING) return [];\n    }\n    return [...seen];\n  }\n\n  // Latest chapter = max over the page's actual chapter links/dropdown\n  // (a whole-page number max returns the chapter being read or a view count\n  // instead of the real latest).\n  //\n  // Two passes over those elements, never one. A link's href names the series it\n  // belongs to; its text does not, and the \"you may also like\" cards are links\n  // too — theirs read \"Hajime no Ippo Chapitre 1515\". Taking href-or-text per\n  // element and one maximum over the lot let a carousel that rotates on every\n  // load answer for the series being looked at: 1515 on a page whose own\n  // chapters stop at 125, and a different number a reload later.\n  function latestChapterInDom(root = document) {\n    const els = root.querySelectorAll('a[href], option');\n    const scan = (textOf) => {\n      let max = null;\n      for (const el of els) {\n        const found = chapterNumber(textOf(el));\n        if (found === null) continue;\n        const n = parseFloat(found);\n        if (max === null || n > max) max = n;\n      }\n      return max;\n    };\n    const max = scan((el) => el.getAttribute('href') || el.getAttribute('value'))\n      ?? scan((el) => (el.textContent || '').slice(0, 80));\n    return max !== null ? String(max) : null;\n  }\n\n  // The chapter this page IS, normalised to \"Ch. 109\". Reads the URL first\n  // (unambiguous), then falls back to the <title>; a bare number found loose\n  // in the page is not trustworthy enough to label a chapter with.\n  //\n  // The fallback is what carries a site that addresses chapters by id rather\n  // than by number: MangaDex's path says nothing chapterNumber will accept, and\n  // its <title> says \"Chapter 26\".\n  //\n  // A volume is the fallback, never the winner: a page that says \"Volume 3,\n  // Chapter 21\" is chapter 21. It is labelled \"Vol. 3\" and not \"Ch. 3\" on\n  // purpose — see volumeNumber() in shared/site-rules.js for what reads these\n  // labels back and why a volume must not answer to a chapter's number.\n  function chapterLabelHere() {\n    const path = location.pathname + location.search;\n    const fromUrl = chapterNumber(path);\n    const fromTitle = chapterNumber(document.title);\n    // \"/bleach-chapitre-686-5/\" spells the decimal with a hyphen, which the\n    // pattern reads as the end of chapter 686 — and 686 is a chapter of its\n    // own, five days older. When the title agrees on the whole number and\n    // knows a decimal too, it is the one that saw the point.\n    const chapter = fromUrl !== null && fromTitle !== null && fromTitle.startsWith(`${fromUrl}.`)\n      ? fromTitle\n      : fromUrl ?? fromTitle;\n    if (chapter !== null) return `Ch. ${chapter}`;\n    const volume = volumeNumber(path) ?? volumeNumber(document.title);\n    return volume === null ? null : `Vol. ${volume}`;\n  }\n\n  function coverGuess(root = document) {\n    const og = root.querySelector('meta[property=\"og:image\"], meta[name=\"twitter:image\"]')?.content;\n    if (og) return og;\n    // Series pages without og:image: look for an <img> that smells like a cover.\n    //\n    // Read through lazySrc(), for the same reason panels are. A theme that\n    // parks a transparent gif in `src` and keeps the address in data-src used\n    // to have that gif filed as the cover — and unlike a panel, a cover is\n    // written to the library once and shown on every card afterwards. An <img>\n    // with no address behind it at all is not a cover either, whatever its\n    // class says, so it does not get to win over the ones below it.\n    const imgs = [...root.querySelectorAll('img')];\n    const cand = imgs.find((img) => {\n      const address = lazySrc(img);\n      if (!address\n        || !/cover|poster|thumb|affiche|wp-post-image/i.test(\n          img.className + ' ' + address + ' ' + (img.alt || ''))) return false;\n      // Only measure the element when what is decoded is the address itself.\n      // Behind a spacer it is a 1x1 gif, and on a page we fetched and parsed\n      // nothing is decoded at all; both are \"no size yet\", not \"too small\".\n      if (address !== (img.currentSrc || img.src)) return true;\n      return (img.naturalWidth || img.width || 100) >= 80;\n    });\n    if (cand) return absolute(lazySrc(cand));\n    // Last resort: the tallest portrait image on the page. Covers are portrait;\n    // chapter pages and banners are not.\n    let best = null;\n    for (const img of imgs) {\n      const w = img.naturalWidth || img.width;\n      const h = img.naturalHeight || img.height;\n      if (!w || !h || w < 80 || h < w * 1.2 || h > w * 2.2) continue;\n      if (!lazySrc(img)) continue;\n      if (!best || h > best.h) best = { h, img };\n    }\n    return best ? absolute(lazySrc(best.img)) : null;\n  }\n\n  const absolute = (src) => {\n    try { return src ? new URL(src, location.href).href : null; } catch { return src || null; }\n  };\n\n  // Which language the scan is in. The page itself declares this far more\n  // reliably than any guess from the title, so preselect it in the add form.\n  const LANG_NAMES = {\n    en: 'English', ja: 'Japanese', ko: 'Korean',\n    zh: 'Chinese (Simplified)', fr: 'French',\n  };\n\n  function languageGuess(root = document) {\n    const raw =\n      root.documentElement?.getAttribute('lang') ||\n      root.querySelector('meta[property=\"og:locale\"]')?.content ||\n      root.querySelector('html')?.getAttribute('lang') ||\n      '';\n    const code = String(raw).toLowerCase().split(/[-_]/)[0];\n    return LANG_NAMES[code] || null;\n  }\n\n  // \"Ongoing\" vs \"Completed\" decides whether the chapter count gets a \"+\".\n  function statusGuess(root = document) {\n    // innerText is empty on a detached DOMParser document, so fall back to\n    // textContent when reading a fetched series page.\n    const body = root.body?.innerText || root.body?.textContent || '';\n    // Look near a Status/Statut label so a stray \"completed\" elsewhere on the\n    // page cannot flip an ongoing series.\n    const m = body.match(/(status|statut|état|state)\\s*[:\\-]?\\s*([A-Za-zéèêç ]{3,20})/i);\n    const value = (m ? m[2] : '').toLowerCase();\n    if (/complete|completed|finished|terminé|termine|fini/.test(value)) return 'completed';\n    if (/ongoing|publishing|en cours|releasing/.test(value)) return 'ongoing';\n    return null;\n  }\n\n  // The page usually links to the series' real page (header title link,\n  // breadcrumb). A real link beats any URL we could fabricate — fabricated\n  // ones 404 on sites like scan-manga whose URL scheme is opaque.\n  function seriesUrlFromDom(title) {\n    const chapterish = /(chapter|chapitre|chap|ch|episode)[-_/ .]?\\d/i;\n    const candidate = (a) =>\n      a.host === location.host && a.href !== location.href &&\n      !chapterish.test(a.pathname) && a.protocol.startsWith('http');\n    if (title && title.trim().length >= 4) {\n      const t = title.trim().replace(/\\s+/g, ' ').toLowerCase();\n      let prefix = null;\n      for (const a of document.querySelectorAll('a[href]')) {\n        if (!candidate(a)) continue;\n        const text = (a.textContent || '').trim().replace(/\\s+/g, ' ').toLowerCase();\n        if (!text) continue;\n        if (text === t) return a.href;\n        // scan-manga-style: the series link's text starts with the title\n        // (\"Ao no Hako Vol. 13 • Ch. 109 - 15 Janvier\").\n        if (!prefix && (text.startsWith(t) || (text.length >= 6 && t.startsWith(text)))) {\n          prefix = a.href;\n        }\n      }\n      if (prefix) return prefix;\n    }\n    // Slug match: sites routinely put the series under a different path than\n    // its chapters (\"/chapter/erk-chapter-110/\" vs \"/manga/erk/\"), so no URL\n    // surgery can derive one from the other — but they share the slug.\n    const slug = location.pathname\n      .replace(/\\/$/, '').split('/').pop()\n      .replace(/[-_.](chapter|chapitre|chap|ch|episode)[-_. ]?[\\d.].*$/i, '')\n      .toLowerCase();\n    if (slug.length >= 5) {\n      for (const a of document.querySelectorAll('a[href]')) {\n        if (!candidate(a)) continue;\n        if (a.pathname.toLowerCase().includes(slug)) return a.href;\n      }\n    }\n\n    const crumbs = [...document.querySelectorAll(\n      '[class*=\"breadcrumb\" i] a[href], [itemtype*=\"Breadcrumb\"] a[href]'\n    )].filter(candidate);\n    return crumbs.length ? crumbs[crumbs.length - 1].href : null;\n  }\n\n  // Chapter navigation for the reader: the chapter list (dropdown or links)\n  // plus prev/next targets, straight from the page's own DOM — the same data\n  // MangaPin's per-site specs extract, discovered heuristically.\n  const CHAPTERISH = /(chapter|chapitre|chap|ch|episode)[-_/ .]?\\d/i;\n  // NaN is \"this link does not name a chapter\", and every caller below already\n  // treats it that way — which is what keeps MangaDex's `/chapter/<uuid>` links\n  // out of the chapter wheel instead of filling it with numbers read out of a\n  // UUID. CHAPTERISH above stays deliberately loose; this is the gate.\n  const chapNum = (s) => {\n    const found = chapterNumber(s);\n    return found === null ? NaN : parseFloat(found);\n  };\n\n  // Words a site puts on a link that moves you one chapter, in place of the\n  // chapter's name. Fine on the page, where the link sits next to the chapter\n  // you are reading; useless in a dropdown, where \"Next\" is not an answer to\n  // \"which chapters are there\".\n  const NAV_WORD = /^(next|prev|previous|suivant(e)?|pr[eé]c[eé]dent(e)?|chapter|chapitre|chap|ch|episode|read|lire)$/i;\n\n  /**\n   * Whether a label is made of nothing but those words and punctuation. Word by\n   * word rather than as one pattern, because the order is per-language: English\n   * puts the direction last (\"Next chapter\"), French first (\"Chapitre suivant\"),\n   * and a pattern written for one silently lets the other through. A label with\n   * no letters at all — \"«\", \"→\" — is nav too: every() is true of nothing.\n   */\n  const isNavLabel = (s) =>\n    s.split(/[^\\p{L}]+/u).filter(Boolean).every((w) => NAV_WORD.test(w));\n\n  /**\n   * What to write in the chapter list for one entry. The number comes from the\n   * URL, which every site agrees on; the site's own text is kept only when it\n   * says something the number does not.\n   */\n  const optionLabel = (text, n) => {\n    const label = String(text || '').trim().slice(0, 60);\n    if (/\\d/.test(label)) return label;      // already names its chapter\n    if (Number.isNaN(n)) return label;       // nothing better to offer\n    if (isNavLabel(label)) return `Ch. ${n}`;\n    return `Ch. ${n} — ${label}`;            // a real name: \"Prologue\"\n  };\n\n  function chapterNav() {\n    let options = [];\n    // Which chapter this page is, needed twice below: to put it back into a\n    // list that does not contain it, and to find its neighbours.\n    const cur = chapNum(location.pathname + location.search);\n    // 1. A <select> whose options carry chapter URLs (many readers have one).\n    for (const sel of document.querySelectorAll('select')) {\n      const opts = [...sel.options].filter((o) => {\n        const v = o.value || '';\n        return (v.startsWith('http') || v.startsWith('/')) &&\n          (CHAPTERISH.test(v) || CHAPTERISH.test(o.textContent));\n      });\n      if (opts.length >= 3 && opts.length > options.length) {\n        options = opts.map((o) => {\n          const n = chapNum(o.value) || chapNum(o.textContent);\n          return {\n            label: optionLabel(o.textContent, n),\n            url: new URL(o.value, location.href).href,\n            n,\n          };\n        });\n      }\n    }\n    // 2. Otherwise chapter links sharing this page's URL shape — and its series.\n    // Every chapter-ish link on the page used to qualify, which is fine on a\n    // reader that links nothing but its own chapters and wrong everywhere else:\n    // MangaNato lists other series down the side, so the wheel for One Piece\n    // 1139 read 1141, 1140, 1139, then 165, 150, 89, 63, 48, 28, 13, 9.\n    //\n    // seriesKey() is the reducer the library already matches URLs with, so a\n    // site whose slugs it collapses cannot disagree with itself here. When it\n    // cannot find a slug it hands back the whole URL; filtering on that would\n    // match nothing at all, so an unkeyed page stays unfiltered as before.\n    if (options.length < 3) {\n      const key = window.PanelFlowMatch?.seriesKey?.(location.href);\n      const wanted = typeof key === 'string' && key.includes('|') ? key : null;\n      const seen = new Set();\n      const fromLinks = [];\n      for (const a of document.querySelectorAll('a[href]')) {\n        if (a.host !== location.host || !CHAPTERISH.test(a.pathname + a.search)) continue;\n        if (seen.has(a.href)) continue;\n        seen.add(a.href);\n        if (wanted && window.PanelFlowMatch.seriesKey(a.href) !== wanted) continue;\n        const n = chapNum(a.pathname + a.search);\n        if (Number.isNaN(n)) continue;\n        // The label matters most here: on a lot of sites the only chapter-ish\n        // links on a chapter page are its own prev/next arrows, so this branch\n        // is where a list of \"Next / Prev\" comes from.\n        fromLinks.push({ label: optionLabel(a.textContent, n), url: a.href, n });\n      }\n      if (fromLinks.length >= 2) options = fromLinks;\n    }\n    // 3. Numeric-ID selects (scan-manga style): the selected option's value is\n    // embedded in the chapter URL — swap it, plus the chapter number in the\n    // slug, to build every chapter's URL. Verified against the live site.\n    if (options.length < 3) {\n      const curNum = chapNum(location.pathname);\n      for (const sel of document.querySelectorAll('select')) {\n        const cur = sel.selectedOptions[0];\n        if (!cur || sel.options.length < 3 || !/^\\d+$/.test(cur.value)) continue;\n        if (!location.href.includes(cur.value)) continue;\n        options = [...sel.options]\n          .filter((o) => /^\\d+$/.test(o.value))\n          .map((o) => {\n            const n = parseFloat(o.textContent); // labels like \"110 - Interview\"\n            let url = location.href.replace(cur.value, o.value);\n            if (!Number.isNaN(curNum) && !Number.isNaN(n)) {\n              url = url.replace(\n                new RegExp('((?:chapter|chapitre|chap|ch|episode)[-_ ]?)' +\n                  String(curNum).replace('.', '\\\\.'), 'i'),\n                '$1' + n);\n            }\n            return { label: optionLabel(o.textContent, n), url, n };\n          });\n        break;\n      }\n    }\n    // When the list was built from this page's own prev/next links, the one\n    // chapter missing from it is the one you are reading — and a select whose\n    // value is absent displays whatever comes first, so the dropdown would\n    // claim you were on the next chapter.\n    if (!Number.isNaN(cur) && !options.some((o) => o.n === cur || o.url === location.href)) {\n      options.push({ label: `Ch. ${cur}`, url: location.href, n: cur });\n    }\n\n    options.sort((a, b) => (b.n || 0) - (a.n || 0)); // newest first, like the sites\n    if (options.length > 400) options = options.slice(0, 400);\n\n    // prev/next: the engine's own arrows first — they are the one thing these\n    // readers all place in the same element and label differently, sometimes\n    // with nothing but an icon. Then explicit rel/text links, then neighbours in\n    // the list. `el.href` is checked because a rule selector is free to land on\n    // something that is not a link at all.\n    const arrows = siteFor() || {};\n    const findNav = (rel, textRe, named) => {\n      const el = firstMatch(named) || document.querySelector(`a[rel=\"${rel}\"]`) ||\n        [...document.querySelectorAll('a[href]')].find((a) =>\n          a.host === location.host && textRe.test((a.textContent || '').trim()) &&\n          (a.textContent || '').trim().length < 30 && CHAPTERISH.test(a.pathname + a.search));\n      return el && el.href && el.href !== location.href ? el.href : null;\n    };\n    let prevUrl = findNav('prev',\n      /^(<|«|‹|←)?\\s*(prev(ious)?( chapter)?|chapitre )?pr[eé]c[eé]dent|^prev/i, arrows.prevChapter);\n    let nextUrl = findNav('next',\n      /^(next( chapter)?|chapitre suivant|suivant)\\s*(>|»|›|→)?$|^next/i, arrows.nextChapter);\n    if ((!prevUrl || !nextUrl) && !Number.isNaN(cur) && options.length >= 2) {\n      const idx = options.findIndex((o) => o.n === cur || o.url === location.href);\n      if (idx !== -1) {\n        if (!nextUrl && idx > 0) nextUrl = options[idx - 1].url; // newest-first\n        if (!prevUrl && idx < options.length - 1) prevUrl = options[idx + 1].url;\n      }\n    }\n    return { options, prevUrl, nextUrl };\n  }\n\n  function seriesUrlGuess() {\n    // Series page ≈ chapter URL with the trailing chapter segment removed.\n    // Cutting mid-segment leaves the separator behind (\".../Ao-no-Hako-\"),\n    // which 404s, so always trim trailing separators off the result.\n    const trim = (p) => p.replace(/[-_.\\s]+$/, '');\n    const attempts = [\n      // Chapter token buried inside the last segment, e.g. scan-manga's\n      // \".../Ao-no-Hako-Chapitre-109-FR_330666.html\" → \".../Ao-no-Hako\".\n      // Tried first: it is the precise pattern, and the generic one below\n      // would otherwise cut it at \"Chapitre\" and strand the separator.\n      /[-_.](chapter|chapitre|chap|ch|episode)[-_. ]?[\\d.][^/]*\\/?$/i,\n      // Chapter as its own trailing path segment: \".../erk/chapter-109\".\n      /\\/((chapter|chapitre|chap|ch|episode)[-_ ]?[\\d.]+[^/]*)\\/?$/i,\n    ];\n    for (const re of attempts) {\n      const stripped = trim(location.pathname.replace(re, ''));\n      // A bare \"/\" or \"\" means we ate the whole path — not a series URL.\n      if (stripped && stripped !== trim(location.pathname) && stripped !== '/') {\n        return location.origin + stripped;\n      }\n    }\n    return location.href;\n  }\n\n  // Every blob URL minted below. An object URL pins its bytes until it is\n  // revoked, so without this a long session held every page of every chapter it\n  // had ever opened — forty full-size images per chapter, for the life of the\n  // tab. The reader hands the list back when it closes.\n  const minted = new Set();\n  const mint = (blob) => {\n    const url = URL.createObjectURL(blob);\n    minted.add(url);\n    return url;\n  };\n\n  // Only ours: anything else in the list is the site's own URL or a plain http\n  // src, and revoking one of those would break the page underneath.\n  function releaseStable(urls) {\n    for (const url of urls || []) {\n      if (minted.delete(url)) URL.revokeObjectURL(url);\n    }\n  }\n\n  // blob: page URLs die when the site revokes them (scan-manga revokes pages\n  // you scrolled past). Copy the bytes into our own blob URL that we control;\n  // if the original is already dead, rescue the decoded pixels off the <img>.\n  // Where a page's address is when the <img> has not been given one yet. Lazy\n  // loaders park it in a data- attribute and move it to src as you scroll, so\n  // by reading the attribute we get pages the reader would otherwise have to\n  // wait for the user to scroll past — which, inside our own reader, never\n  // happens. Measured on sushiscan.net: 9 pages in src, 11 in data-src.\n  const LAZY_ATTRS = ['data-src', 'data-lazy-src', 'data-original', 'data-url', 'data-lazy'];\n  // A `data:` src too short to be a page of anything. Lazy-loading themes park\n  // a transparent gif in `src` and keep the real address in `data-src`; the few\n  // readers that inline a panel produce thousands of characters of base64.\n  // Under half a kilobyte it is furniture, not a chapter.\n  const SPACER_MAX = 512;\n  const isSpacer = (src) => src.startsWith('data:') && src.length < SPACER_MAX;\n\n  function lazySrc(img) {\n    const src = img.currentSrc || img.src;\n    // The loop below has always known a `data:` attribute is not an address and\n    // this line did not, so on a theme that parks a spacer in `src` and the\n    // address in `data-src`, the spacer won: the reader opened on seventy\n    // copies of a transparent pixel, which is exactly what \"the extension\n    // activates and I cannot read the chapter\" looks like from the outside.\n    if (src && !isSpacer(src)) return src;\n    for (const name of LAZY_ATTRS) {\n      const v = img.getAttribute(name);\n      if (v && !v.startsWith('data:')) return new URL(v, location.href).href;\n    }\n    // srcset without src: take the first candidate, dropping its descriptor.\n    const set = (img.getAttribute('srcset') || '').split(',')[0].trim().split(/\\s+/)[0];\n    if (set) return new URL(set, location.href).href;\n    // Nothing but the spacer, and a spacer is not something to show anybody.\n    // Every gate here reads '' as \"no page behind this element\".\n    return '';\n  }\n\n  async function stableImageSrc(img) {\n    const src = lazySrc(img);\n    if (!src || !src.startsWith('blob:')) return src;\n    try {\n      const blob = await fetch(src).then((r) => r.blob());\n      return mint(blob);\n    } catch {\n      try {\n        const c = document.createElement('canvas');\n        c.width = img.naturalWidth;\n        c.height = img.naturalHeight;\n        c.getContext('2d').drawImage(img, 0, 0);\n        const blob = await new Promise((r) => c.toBlob(r, 'image/png'));\n        if (blob) return mint(blob);\n      } catch { /* fall through */ }\n      return src;\n    }\n  }\n\n  // The strip as it is now, not as it was when detection fired.\n  //\n  // Two things go stale between the two moments. The container can be the wrong\n  // one outright: on natomanga the chapter's 25 <img> have no decoded size for\n  // the first second, so the only cluster of big images on the page is the \"you\n  // may also like\" carousel, and the reader opened on seven covers of other\n  // people's series. And the right container can be half full: a paginated\n  // reader keeps appending, and the snapshot took what had arrived.\n  //\n  // So the search is run again — it is one pass over the images — and its answer\n  // is taken when it finds more than detection did. Then the winning container\n  // is re-read directly: an <img> still loading counts as long as it has an\n  // address to load from, because it is a page on its way, while one that has\n  // finished loading small is an icon and is dropped. Nothing here ever returns\n  // fewer panels than detection found; a page that has moved on under us is a\n  // reason to fall back on the snapshot, not to lose the chapter.\n  function currentStrip(gallery) {\n    const fresh = galleryImages();\n    const best = fresh && fresh.images.length > gallery.images.length ? fresh : gallery;\n    const found = best.container?.querySelectorAll?.('img');\n    if (!found) return best;\n    const images = [...found].filter((img) =>\n      (img.complete && img.naturalWidth ? sizedImage(img) : Boolean(lazySrc(img))));\n    return images.length >= best.images.length ? { container: best.container, images } : best;\n  }\n\n  /** How many panels the page is offering right now. Nothing to count is 0. */\n  function panelCount() {\n    return detection?.gallery ? currentStrip(detection.gallery).images.length : 0;\n  }\n\n  /** Opens the reader. Resolves false when there was nothing to open with. */\n  async function openReader() {\n    if (!detection) return false;\n    if (detection.novel) {\n      window.PanelFlowReader.openText(\n        detection.novel.paragraphs, seriesMeta(), detection.domainRule || {});\n      return true;\n    }\n    // Panels that came from the site's API, not from the page: there is no\n    // strip on screen to hand over, and nothing to re-measure — the list is\n    // already the whole chapter, in order.\n    if (detection.pages) {\n      window.PanelFlowReader.open(detection.pages, seriesMeta(), detection.domainRule || {}, null);\n      return true;\n    }\n    if (!detection.gallery) return false;\n    const strip = currentStrip(detection.gallery);\n    const srcs = (await Promise.all(strip.images.map(stableImageSrc))).filter(Boolean);\n    // A panel still loading has no address to hand over yet, and a reader opened\n    // on what is left is a near-blank page with no way back — the caller that\n    // thought this worked has already taken the pill away. Detection needs the\n    // same count to fire at all, so a page that got here can reach it.\n    if (srcs.length < rules.heuristics.minGalleryImages) return false;\n    window.PanelFlowReader.open(srcs, seriesMeta(), detection.domainRule || {}, strip.container);\n    return true;\n  }\n\n  // --- scan orchestration --------------------------------------------------\n\n  function scheduleScan() {\n    clearTimeout(scanTimer);\n    scanTimer = setTimeout(scan, 600);\n  }\n\n  // A tab opened in the background is not laid out: Chrome never brings its\n  // lazy images into a viewport, so they have no size, and everything here\n  // measures sizes. On natomanga that turned the whole chapter invisible and\n  // left the \"you may also like\" covers as the only cluster of images on the\n  // page — the reader opened on seven covers of other people's series, in the\n  // one habit manga readers have most, middle-clicking a stack of chapters open.\n  // Nothing is lost by waiting: nobody is reading a tab they cannot see, and the\n  // page is measured for the first time at the moment it is first looked at.\n  let waitingForView = false;\n  function scanWhenSeen() {\n    if (waitingForView) return;\n    waitingForView = true;\n    const onShow = () => {\n      if (document.hidden) return;\n      document.removeEventListener('visibilitychange', onShow);\n      waitingForView = false;\n      scheduleScan();\n    };\n    document.addEventListener('visibilitychange', onShow);\n  }\n\n  function scan() {\n    if (document.hidden) return scanWhenSeen();\n    const result = scorePage();\n    // Both gates are veto-only: a page that clears the score still has to look\n    // like a chapter and read like one, or the reader stays out of the way.\n    if (result.gallery &&\n        (rowCount(result.gallery.images) < 3 || !chapterEvidence())) return;\n    if (result.score >= rules.heuristics.scoreThreshold && result.gallery) {\n      detection = result;\n      accept();\n      return;\n    }\n    // No strip: the page may still be a chapter, in prose. The score is built\n    // out of image signals and a novel clears none of them, so structure has to\n    // carry it — and on its own it would fire on any long article. A chapter\n    // number in the URL or real prev/next links is what an article never has;\n    // a chapter number in the title alone is not enough, because \"Chapter 3\" is\n    // a normal thing for a blog post to be called.\n    if (!result.gallery && (urlLooksLikeChapter() || hasChapterNav()) && chapterEvidence()) {\n      const novel = novelContent();\n      if (!novel) return askForPages(result) || trackOnly(result);\n      detection = { ...result, novel };\n      accept();\n    }\n  }\n\n  // A chapter whose panels are not on the page at all. MangaDex shows one at a\n  // time and keeps three <img> around — the one being read and two preloads\n  // with no box yet — so there is no strip to lift, and no amount of waiting\n  // for the DOM produces one. Its rule names the API that lists them instead\n  // (`pageApi`, see shared/panelflow-core.js) and the service worker makes the\n  // call: this side sends the address it is on and never a URL to fetch.\n  //\n  // Once per address, whatever comes back, because scan() runs again on every\n  // mutation of a page like this one.\n  let pagesAsked = null;\n  function askForPages(result) {\n    if (!siteFor()?.pageApi || pagesAsked === location.href) return false;\n    pagesAsked = location.href;\n    chrome.runtime.sendMessage({ type: 'chapterPages', url: location.href }, (resp) => {\n      if (chrome.runtime.lastError || detection) return;\n      const pages = (resp && resp.pages) || [];\n      // Nothing to read: a chapter hosted on the publisher's own site, a rate\n      // limit, an answer whose shape moved. It was still read, so record it.\n      if (pages.length < rules.heuristics.minGalleryImages) {\n        trackOnly(result);\n        return;\n      }\n      detection = { ...result, pages };\n      accept();\n    });\n    return true;\n  }\n\n  // A chapter we can follow but cannot render. Some readers keep two images in\n  // the DOM and swap their src as you page through (scan-manga, mangas-origines\n  // both do this) — there is no strip to lift, so no reader, and until now the\n  // page went by in silence: no library entry, no progress, nothing.\n  //\n  // Reading the chapter still happened, so say so. Only on a domain the rules\n  // already name, because outside that list \"chapter-ish URL, no images\" is\n  // most of the web. No pill — there is nothing behind it — and the observer\n  // stays connected, so if the panels do turn up later this becomes a real\n  // detection on the next pass.\n  let tracked = null;\n  function trackOnly(result) {\n    if (!result.domainRule || tracked === location.href) return;\n    tracked = location.href;\n    chrome.runtime.sendMessage({\n      type: 'pageDetected',\n      domain: location.hostname,\n      url: location.href,\n      meta: seriesMeta(result.domainRule),\n    });\n  }\n\n  const urlLooksLikeChapter = () => {\n    const path = location.pathname + location.search;\n    return rules.heuristics.urlPatterns.some((p) => {\n      try { return new RegExp(p, 'i').test(path); } catch { return false; }\n    });\n  };\n\n  /** Everything that happens once a page is settled as a chapter. */\n  function accept() {\n    // Nothing left to watch for. The callback already ignored mutations once\n    // a detection stuck, but the observer itself kept running for the life of\n    // the tab — and the pages this fires on are infinite-scrolling readers\n    // that mutate on every panel.\n    observer.disconnect();\n    showPill();\n    // Ship the page's series meta along: Cloudflare-walled sites can only be\n    // scraped from here, where the user's real session is (MangaPin's model),\n    // so this is how covers and latest chapters stay fresh.\n    chrome.runtime.sendMessage({\n      type: 'pageDetected',\n      domain: location.hostname,\n      url: location.href,\n      meta: seriesMeta(),\n    });\n    maybeAutoOpen();\n  }\n\n  // Auto-show reader, plus a one-shot reopen after the reader navigated\n  // chapters in the same tab. The per-site override wins over the global\n  // default; absent, the site follows the default.\n  let autoOpened = false;\n  function maybeAutoOpen() {\n    if (autoOpened) return;\n    const keys = ['autoShowDefault', 'autoShowSites', 'settings', 'reopenReaderFor'];\n    chrome.storage.local.get(keys, (v) => {\n      if (autoOpened || !detection) return;\n      const reopen = v.reopenReaderFor === location.href;\n      if (reopen) chrome.storage.local.remove('reopenReaderFor');\n      const site = (v.autoShowSites || {})[location.hostname];\n      const auto = site !== undefined\n        ? site\n        : (v.autoShowDefault ?? !!v.settings?.autoOpenReader);\n      if (reopen || auto) autoOpenNow();\n    });\n  }\n\n  // Detection settles as soon as three panels have a size, which on a paginated\n  // reader is well before they have a src worth reading — measured on natomanga,\n  // where auto-open beat the panels every time and left neither reader nor pill.\n  // So: try, and if the panels are not there yet come back a few times rather\n  // than once. Bounded, because a page that never fills in is a page where the\n  // pill has to stay reachable instead of a timer running for the life of the\n  // tab. The whole run is ~5s, and the flag is only set on the try that worked.\n  //\n  // \"Ready\" is not a count, it is a strip that has stopped growing: opening on\n  // the first three panels that show up gives a four-page chapter that looks\n  // complete and is not. So each try counts the panels and, while that number is\n  // still climbing, hands the turn to the next try instead of opening. The\n  // baseline is what detection saw, so a page that was already whole opens at\n  // once and pays nothing for this. The last try opens on whatever it has: a\n  // strip that never settles is still better read than not read.\n  const AUTO_OPEN_TRIES = 6;\n  const AUTO_OPEN_WAIT = 900;\n  function autoOpenNow(attempt = 0, before = detection?.gallery?.images?.length || 0) {\n    if (autoOpened || !detection) return;\n    const last = attempt + 1 >= AUTO_OPEN_TRIES;\n    const now = panelCount();\n    const again = () => setTimeout(() => autoOpenNow(attempt + 1, now), AUTO_OPEN_WAIT);\n    if (!last && now > before) {\n      again();\n      return;\n    }\n    // Taken back off only on success; a page that ends up not opening keeps it.\n    const pill = document.getElementById('panelflow-pill');\n    openReader().then((ok) => {\n      if (ok) {\n        autoOpened = true;\n        pill?.remove();\n        return;\n      }\n      if (!last) again();\n    });\n  }\n\n  // Lazy-loaded images and SPA navigations: rescan on DOM changes (debounced)\n  // until a detection sticks, and every time the address changes under us.\n  const observer = new MutationObserver(() => { if (!detection) scheduleScan(); });\n  const watchDom = () => observer.observe(document.body, { childList: true, subtree: true });\n  watchDom();\n\n  // An SPA navigation is a new page with no new document: the detection is\n  // stale, the pill points at the chapter before last, the reader is full of\n  // the previous chapter's panels, and the observer was disconnected by the\n  // accept() of the page that just went away.\n  //\n  // `popstate` alone only covers the Back button. MangaDex — and every other\n  // reader built as a single-page app — moves to the next chapter by calling\n  // `history.pushState`, which fires nothing. The usual answer, patching\n  // `history.pushState`, cannot work here: a content script runs in its own\n  // JavaScript realm, so the patch is invisible to the page's own copy of\n  // history. The permission that would work, `webNavigation`, spends \"read\n  // your browsing history\" on the install screen — too much to ask of someone\n  // installing a zip a friend sent them.\n  //\n  // So watch the address itself. One string comparison a second catches every\n  // way a page can replace itself, including the ones nobody has invented yet.\n  // The hash is left out: `#page-4` is an anchor inside the same chapter, and\n  // readers move it as you scroll. Our own chapter navigation reloads the\n  // document (see `reopenReaderFor`), so it never comes through here.\n  const addressHere = () => location.pathname + location.search;\n  let address = addressHere();\n  function addressChanged() {\n    if (addressHere() === address) return;\n    address = addressHere();\n    detection = null;\n    tracked = null;\n    site = null;\n    autoOpened = false;\n    document.getElementById('panelflow-pill')?.remove();\n    if (window.PanelFlowReader?.isOpen?.()) window.PanelFlowReader.close();\n    watchDom();\n    scheduleScan();\n  }\n  addEventListener('popstate', addressChanged);\n  setInterval(addressChanged, 1000);\n\n  // A chapter page often only shows its own number; the series page lists\n  // every chapter. Fetching it from here rides the user's real session, so\n  // Cloudflare-walled sites answer normally (the server gets challenged).\n  // Returns null when the URL does not resolve — the caller uses that to reject\n  // a guessed series URL. Some sites (scan-manga) expose series pages under a\n  // path unrelated to their chapter URLs, so no amount of string surgery can\n  // derive one; storing an unverified guess is what produces dead library\n  // entries. A chapter URL that works beats a series URL that 404s.\n  async function fetchSeriesInfo(url) {\n    try {\n      const resp = await fetch(url, { credentials: 'include' });\n      if (!resp.ok) return null;\n      const doc = new DOMParser().parseFromString(await resp.text(), 'text/html');\n      // Soft 404s answer 200 with an error page; a real series page lists\n      // chapters, so require at least one before trusting the URL.\n      const latest = latestChapterInDom(doc);\n      if (latest === null && /erreur|error|404|not found/i.test(doc.title || '')) return null;\n      return {\n        latest,\n        // The series page is where the cover, status and genres actually live —\n        // a chapter page has none of them.\n        cover: coverGuess(doc),\n        status: statusGuess(doc),\n        language: languageGuess(doc),\n        // The fetched page's own name, so the season and language links it\n        // lists as tags are recognised as being about it. `title` above belongs\n        // to the chapter page this was called from and is not in scope here.\n        genres: genresInDom(doc, (doc.querySelector('meta[property=\"og:title\"]')?.content\n          || doc.title || '').replace(/\\s*[-–|].*$/, '').trim()),\n      };\n    } catch { return null; }\n  }\n\n  // Popup \"add this page\" + reader toggle (Alt+R command, popup button).\n  chrome.runtime.onMessage.addListener((msg, _sender, sendResponse) => {\n    // The popup labels its reader button from this, so it must answer even on\n    // pages where nothing was detected.\n    if (msg.type === 'readerState') {\n      sendResponse({\n        detected: !!detection,\n        open: !!window.PanelFlowReader?.isOpen?.(),\n      });\n      return; // sync response\n    }\n    if (msg.type === 'toggleReader') {\n      if (window.PanelFlowReader?.isOpen?.()) {\n        window.PanelFlowReader.close();\n        sendResponse({ ok: true, open: false });\n      } else if (detection) {\n        // Same rule as the pill's own click: it is the open that earns its\n        // removal. The response stays synchronous — the popup is asking what it\n        // just did to the reader, and it cannot wait on the panels.\n        openReader().then((ok) => {\n          if (ok) document.getElementById('panelflow-pill')?.remove();\n        });\n        sendResponse({ ok: true, open: true });\n      } else {\n        sendResponse({ ok: false, error: 'No chapter detected on this page.' });\n      }\n      return; // sync response\n    }\n    if (msg.type === 'openLibraryModal') {\n      (async () => {\n        sendResponse(await window.PanelFlowLibraryModal.open(await enrichedMeta()));\n      })();\n      return true; // async response\n    }\n    if (msg.type !== 'getSeriesMeta') return;\n    (async () => {\n      sendResponse({ meta: await enrichedMeta() });\n    })();\n    return true; // async response\n  });\n\n  // Series meta plus whatever the series page adds (real latest chapter, cover)\n  // — and a sourceUrl that has been checked to actually resolve.\n  async function enrichedMeta() {\n    const meta = seriesMeta();\n    if (meta.sourceUrl && meta.sourceUrl !== location.href) {\n      const extra = await fetchSeriesInfo(meta.sourceUrl);\n      if (extra) {\n        const cur = parseFloat(meta.lastKnownChapter);\n        const real = parseFloat(extra.latest);\n        if (!Number.isNaN(real) && (Number.isNaN(cur) || real > cur)) {\n          meta.lastKnownChapter = String(real);\n        }\n        // A chapter page's \"cover\" is just its first panel — the series page's\n        // is the real one, so it wins rather than only filling a gap.\n        if (extra.cover) meta.coverUrl = extra.cover;\n        if (extra.status) meta.seriesStatus = extra.status;\n        if (!meta.language && extra.language) meta.language = extra.language;\n        // And its genres, for the same reason and with the same precedence: a\n        // chapter page has none of its own, so whatever was scraped there came\n        // from the site's furniture.\n        if (extra.genres?.length) meta.genres = extra.genres;\n      } else {\n        // The guess does not resolve. Pin the chapter URL instead: it is\n        // reachable, so the entry stays clickable and progress still tracks.\n        meta.sourceUrl = location.href;\n        meta.seriesUrlVerified = false;\n      }\n    }\n    // Said out loud so a caller that already has this cannot be made to fetch\n    // the series page a second time — see enrich() in library-modal.js, which\n    // opens the sheet on the cheap meta and asks for this one behind it.\n    meta.enriched = true;\n    return meta;\n  }\n\n  // Expose for reader.js: series meta, chapter navigation, blob rescue and the\n  // matching release — whoever mints has to be the one who frees.\n  //\n  // lazySrc and sizedImage are here so the reader's harvest asks the same two\n  // questions this file does — where the address is, and whether the thing is\n  // a page — instead of keeping its own copy of the answers. It kept one, and\n  // the copy did not know about the spacer gif: every panel behind one was\n  // measured at 1x1 and dropped on its way into an open reader.\n  /**\n   * Look at the page again, from nothing.\n   *\n   * `accept()` disconnects the observer, deliberately — the pages this fires on\n   * are infinite-scrolling readers that mutate on every panel, and watching them\n   * for the life of the tab is a cost with no answer at the end of it. The\n   * consequence is that a verdict, once taken, is final: if the strip had not\n   * arrived yet, the page was judged prose and stays prose, and the reader opens\n   * a manga chapter as text.\n   *\n   * In Chrome that barely happens — content scripts run at `document_idle`,\n   * after the theme has built its strip. A WebView is not Chrome, and the shell\n   * around one cannot always promise the same moment. So it may ask for one more\n   * look, once, a couple of seconds later.\n   *\n   * Refused while the reader is open: the reader *is* the answer, and taking the\n   * page apart underneath somebody who is reading it is worse than any verdict.\n   * Returns whether the rescan was started, so a caller can tell the difference.\n   */\n  function rescan() {\n    if (window.PanelFlowReader?.isOpen?.()) return false;\n    detection = null;\n    tracked = null;\n    autoOpened = false;\n    // Both are keyed to \"we already did this for this address\"; a fresh look has\n    // to be allowed to ask the same questions again.\n    pagesAsked = null;\n    document.getElementById('panelflow-pill')?.remove();\n    watchDom();\n    scheduleScan();\n    return true;\n  }\n\n  window.__panelflowDetect = {\n    seriesMeta, enrichedMeta, chapterNav, stableImageSrc, releaseStable, lazySrc, sizedImage,\n    rescan,\n    get detection() { return detection; },\n  };\n})();\n\n}catch(e){if(window.PanelFlowFailed)window.PanelFlowFailed(\"detect.js\",e);else console.warn('panelflow: detect.js failed',e)}\ntry{\n// PanelFlow \"Add to library\" sheet.\n// Runs on every page (not just inside the reader) because the popup's\n// \"Add to library\" opens it too. Everything lives in a closed shadow root:\n// scan sites ship aggressive global CSS, and a leaked `* { box-sizing }` or\n// `button { all: unset }` would wreck the form.\n(() => {\n  'use strict';\n  if (window.top !== window) return;\n  if (window.__panelflowLibModalLoaded) return;\n  window.__panelflowLibModalLoaded = true;\n\n  const FOLDERS = ['reading', 'paused', 'plan', 'completed', 'dropped'];\n  const LANGUAGES = ['English', 'Japanese', 'Korean', 'Chinese (Simplified)', 'French'];\n\n  // Same line as reader.js, and for the same reason: a content script cannot\n  // load extension/send.js, and this modal is where a series is added, edited\n  // and migrated — the three writes whose silent failure costs the reader\n  // something. The reply is passed through untouched.\n  const send = (msg) => new Promise((r) => chrome.runtime.sendMessage(msg, r)).then((resp) => {\n    if (resp && resp.error) {\n      console.warn(`[panelflow] ${(msg && msg.type) || 'unknown'} failed`\n        + `${resp.failedAt ? ' in ' + resp.failedAt : ''}`\n        + `${resp.ref ? ' ref=' + resp.ref : ''}: ${resp.error}`);\n    }\n    return resp;\n  });\n  // True inside the phone app, where `chrome` is the shim over the native\n  // bridge rather than a real extension runtime.\n  const isShim = () => !!chrome.runtime.__panelflowShim;\n\n  let host = null;\n  let form = null;\n\n  const STYLE = `\n    :host { all: initial; }\n    /* A shadow root blocks the page's selectors but not inheritance: the page's\n       \"* { letter-spacing: 4px !important }\" matches our host element and the\n       inherited value flows in, beating :host in the cascade. Re-assert the\n       inheritable properties sites most often force, on our own elements —\n       there the page's selectors cannot reach. */\n    .backdrop, .backdrop * {\n      letter-spacing: normal; word-spacing: normal; text-transform: none;\n      text-indent: 0; font-style: normal; font-variant: normal;\n      text-shadow: none; white-space: normal; visibility: visible;\n    }\n    * { box-sizing: border-box; font-family: system-ui, -apple-system, Segoe UI, sans-serif; }\n    .backdrop {\n      position: fixed; inset: 0; z-index: 2147483647;\n      display: flex; align-items: center; justify-content: center;\n      background: rgba(0, 0, 0, .6); padding: 16px;\n    }\n    .sheet {\n      width: 100%; max-width: 420px; max-height: 88vh; overflow-y: auto;\n      background: #1c1917; color: #fafaf9; border-radius: 16px;\n      padding: 18px 20px 20px; box-shadow: 0 16px 48px rgba(0,0,0,.5);\n      font-size: 14px; line-height: 1.4;\n    }\n    .head { display: flex; align-items: center; gap: 10px; margin-bottom: 16px; }\n    .head h2 { flex: 1; margin: 0; font-size: 16px; text-align: center; font-weight: 650; }\n    .x {\n      width: 28px; height: 28px; flex: none; padding: 0; cursor: pointer;\n      border: none; border-radius: 50%; background: none; color: #fafaf9; font-size: 17px;\n    }\n    .x:hover { background: #34302d; }\n    .series { display: flex; align-items: center; gap: 12px; margin-bottom: 18px; }\n    .series img {\n      width: 46px; height: 62px; flex: none; object-fit: cover;\n      border-radius: 7px; background: #34302d;\n    }\n    .series .t { font-weight: 600; }\n    .series .d { font-size: 12px; color: #a8a29e; }\n    h3 {\n      margin: 0 0 7px; font-size: 12.5px; font-weight: 600; color: #d6d3d1;\n    }\n    section { margin-bottom: 15px; }\n    .chips { display: flex; flex-wrap: wrap; gap: 7px; }\n    .chip {\n      border: none; border-radius: 999px; cursor: pointer;\n      background: #34302d; color: #e7e5e4;\n      font: 500 12.5px/1 system-ui, sans-serif; padding: 8px 13px;\n    }\n    .chip:hover { background: #443f3b; }\n    .chip[aria-pressed=\"true\"] { background: #c25d33; color: #fff; }\n    .chip .rm { margin-left: 6px; opacity: .8; }\n    input[type=\"text\"], input[type=\"date\"] {\n      background: #34302d; color: #fafaf9; border: 1px solid #443f3b;\n      border-radius: 8px; padding: 7px 10px; font: inherit; width: 100%;\n    }\n    input:focus { outline: none; border-color: #e87f56; }\n    .row { display: flex; gap: 7px; align-items: center; }\n    .save {\n      width: 100%; margin-top: 6px; padding: 13px; cursor: pointer;\n      border: none; border-radius: 10px; background: #b8552c; color: #fff;\n      font: 600 15px/1 system-ui, sans-serif;\n    }\n    .save:hover { background: #c25d33; }\n    .save:disabled { opacity: .6; cursor: default; }\n    .err { color: #f2705f; font-size: 12.5px; margin: 8px 0 0; }\n\n    /* tracker strip */\n    .tk {\n      margin: -6px 0 16px; padding: 10px 12px; border-radius: 11px;\n      background: #262220; border: 1px solid #3a3532;\n    }\n    .tkrow { display: flex; align-items: center; gap: 10px; }\n    .tkrow + .tkrow { margin-top: 9px; }\n    .tkrow.on { color: #fafaf9; }\n    .tktxt { flex: 1; min-width: 0; }\n    .tkname { font-size: 12.5px; font-weight: 600; }\n    .tksum { font-size: 12px; color: #a8a29e; overflow-wrap: anywhere; }\n    .tkas { font-size: 11.5px; color: #8a8582; overflow-wrap: anywhere; }\n    .tkbtn {\n      flex: none; padding: 6px 12px; cursor: pointer; border-radius: 999px;\n      border: 1px solid #443f3b; background: none; color: #e7e5e4;\n      font: 600 12px/1 system-ui, sans-serif;\n    }\n    .tkbtn:hover { background: #34302d; }\n    .tkrow.on .tkbtn { border-color: #b8552c; color: #e87f56; }\n    .tknote { font-size: 12px; color: #a8a29e; }\n    .tknote + .chips { margin-top: 8px; }\n    .tknote.warnish { color: #f0c99a; }\n    .hint { color: #a8a29e; font-size: 12px; margin: 10px 0 0; text-align: center; }\n\n    /* duplicate / migration sheet */\n    .lead { margin: 0 0 16px; color: #d6d3d1; font-size: 13px; }\n    .cmp { display: flex; align-items: stretch; gap: 10px; margin-bottom: 6px; }\n    .side {\n      flex: 1; min-width: 0; background: #262220; border: 1px solid #3a3532;\n      border-radius: 11px; padding: 11px 12px;\n    }\n    .side.to { border-color: #b8552c; }\n    .side .cap {\n      font-size: 10.5px; text-transform: uppercase; letter-spacing: .06em;\n      color: #a8a29e; margin-bottom: 6px;\n    }\n    .side.to .cap { color: #e87f56; }\n    .side .name {\n      font-weight: 600; font-size: 13px; margin-bottom: 3px;\n      overflow-wrap: anywhere;\n    }\n    .side .dom { font-size: 12px; color: #a8a29e; overflow-wrap: anywhere; }\n    .side .at { font-size: 12px; color: #d6d3d1; margin-top: 7px; }\n    .arrow { align-self: center; flex: none; color: #a8a29e; font-size: 17px; }\n    .warn {\n      margin: 10px 0 0; padding: 9px 11px; border-radius: 9px;\n      background: #3a2f22; color: #f0c99a; font-size: 12.5px;\n    }\n    .keeps { margin: 14px 0 4px; padding: 0 0 0 17px; color: #a8a29e; font-size: 12.5px; }\n    .keeps li { margin: 3px 0; }\n    .actions { display: flex; flex-direction: column; gap: 8px; margin-top: 16px; }\n    .ghost {\n      width: 100%; padding: 12px; cursor: pointer; border-radius: 10px;\n      border: 1px solid #443f3b; background: none; color: #e7e5e4;\n      font: 600 14px/1 system-ui, sans-serif;\n    }\n    .ghost:hover { background: #2a2624; }\n    .ghost.quiet { border-color: transparent; color: #a8a29e; font-weight: 500; }\n  `;\n\n  // --- state ----------------------------------------------------------------\n\n  // If you are on a chapter of it, you are reading it — the form should not\n  // ask. The exception is a one-shot: there is no next chapter to come back\n  // for, so it lands in \"plan\" until you mark it finished yourself.\n  const ONESHOT = /^one[-\\s_]?shots?$/i;\n  function defaultFolder(meta) {\n    const tags = [...(meta.genres || []), ...(meta.tags || [])];\n    if (tags.some((tag) => ONESHOT.test(String(tag).trim()))) return 'plan';\n    return 'reading';\n  }\n\n  /**\n   * The entry as the worker stores it.\n   *\n   * Named here because two buttons send it now: Save, and the tracker strip's\n   * \"Add\" — which needs the series to exist locally before anything can be\n   * pushed anywhere, since what reaches AniList is a bookmark on a library row.\n   */\n  function entryPayload(state) {\n    return {\n      title: state.meta.title,\n      coverUrl: state.meta.coverUrl ?? null,\n      sourceDomain: state.meta.sourceDomain,\n      sourceUrl: state.meta.sourceUrl,\n      tags: state.tags,\n      lastKnownChapter: state.meta.lastKnownChapter ?? null,\n      seriesStatus: state.meta.seriesStatus ?? null,\n      // What kind of work this is, from whoever opened the sheet — the episode\n      // page says 'anime', a chapter page says nothing and the core defaults.\n      // Dropped here, an anime would be filed as a manga and its progress sent\n      // to the wrong catalogue on the reader's tracker.\n      medium: state.meta.medium ?? null,\n      folder: state.folder,\n      language: state.language,\n      score: state.score,\n      startDate: state.startDate,\n      // Where the user is right now. Adding a series from chapter 2 has to\n      // record that, or the web app greets a brand-new entry with\n      // \"Not started\" even though it was added mid-read.\n      chapterUrl: state.meta.chapterUrl ?? null,\n      chapterLabel: state.meta.chapterLabel ?? null,\n    };\n  }\n\n  function initialState(meta, existing) {\n    return {\n      folder: existing?.folder || defaultFolder(meta),\n      // The page declares its own language; only fall back to asking when it\n      // does not, and never override what the user already chose.\n      language: existing ? (existing.language ?? null) : (meta.language ?? null),\n      score: existing?.score ?? null,\n      startDate: existing?.startDate ?? todayISO(),\n      // Prefill from the page's genre links the first time only; an existing\n      // entry's curated tags must not be overwritten by whatever the site lists.\n      tags: existing ? (existing.tags || []).slice() : (meta.genres || []).slice(0, 8),\n      meta,\n      existing,\n      // What the reader's own AniList / MyAnimeList already holds for this\n      // series, once the answer arrives — see askTrackers(). The sheet opens\n      // before it does and works without it.\n      tracker: null,\n      // The tracker strip's \"Add\" while it is happening: which service, what to\n      // say about it, and the candidates to choose from when the service did\n      // not recognise the title. Kept on the state because every chip click\n      // rebuilds the sheet from scratch.\n      addTo: null,\n      appliedFrom: null,\n      before: null,\n      // Set by every chip and every keystroke. A prefill that lands after the\n      // reader has started choosing does not touch the form; it offers.\n      dirty: false,\n    };\n  }\n\n  // The five built-in folders, named here for the tracker line below. Custom\n  // shelves keep the name their owner typed — see folderName() in the popup for\n  // the same rule stated at more length.\n  const folderName = (id) => t('folder_' + id) || id;\n  const TRACKER_NAME = { anilist: 'AniList', mal: 'MyAnimeList', kitsu: 'Kitsu' };\n  const trackerName = (s) => TRACKER_NAME[s] || s;\n\n  /** \"Reading · 880 ch. · 8/10\" — what the tracker holds, in one line. */\n  function trackerSummary(entry) {\n    const bits = [folderName(entry.folder)];\n    if (entry.chaptersRead) bits.push(t('chaptersShort', [String(entry.chaptersRead)]));\n    if (entry.score != null) bits.push(`${entry.score}/10`);\n    if (entry.startDate) bits.push(t('sinceDate', [String(entry.startDate)]));\n    return bits.join(' · ');\n  }\n\n  /**\n   * Copy a tracker entry into the form.\n   *\n   * Progress is deliberately not copied. The tracker knows how far the reader\n   * had got; the page knows where they are *now*, and it is the page they are\n   * looking at — filling the form with the older number and saving it would\n   * push a reader who is on chapter 883 back to 880. The count is shown in the\n   * line above instead, which is the honest place for it.\n   */\n  function applyTracker(state, entry) {\n    state.before = state.before\n      ?? { folder: state.folder, score: state.score, startDate: state.startDate };\n    state.folder = entry.folder || state.folder;\n    if (entry.score != null) state.score = entry.score;\n    if (entry.startDate) state.startDate = entry.startDate;\n    state.appliedFrom = entry.service;\n  }\n\n  /** Put back what the form said before a prefill was applied. */\n  function undoTracker(state) {\n    if (state.before) Object.assign(state, state.before);\n    state.before = null;\n    state.appliedFrom = null;\n  }\n\n  const todayISO = () => new Date().toISOString().slice(0, 10);\n  const shiftISO = (days) => {\n    const d = new Date();\n    d.setDate(d.getDate() + days);\n    return d.toISOString().slice(0, 10);\n  };\n\n  // --- rendering ------------------------------------------------------------\n\n  function chip(label, pressed, onClick, removable) {\n    const b = document.createElement('button');\n    b.type = 'button';\n    b.className = 'chip';\n    b.setAttribute('aria-pressed', String(!!pressed));\n    b.textContent = label;\n    if (removable) {\n      const x = document.createElement('span');\n      x.className = 'rm';\n      x.textContent = '✕';\n      b.appendChild(x);\n    }\n    b.addEventListener('click', onClick);\n    return b;\n  }\n\n  /** The shell every view shares: backdrop, sheet, close button, title. */\n  function shell(root, title, close) {\n    root.innerHTML = '';\n    const style = document.createElement('style');\n    style.textContent = STYLE;\n    root.appendChild(style);\n\n    const backdrop = document.createElement('div');\n    backdrop.className = 'backdrop';\n    const sheet = document.createElement('div');\n    sheet.className = 'sheet';\n    backdrop.appendChild(sheet);\n    // Click-through on the backdrop only; clicks inside must not close.\n    backdrop.addEventListener('click', (e) => { if (e.target === backdrop) close(); });\n    root.appendChild(backdrop);\n\n    const head = document.createElement('div');\n    head.className = 'head';\n    const x = document.createElement('button');\n    x.className = 'x';\n    x.textContent = '✕';\n    x.addEventListener('click', close);\n    const h2 = document.createElement('h2');\n    h2.textContent = title;\n    head.append(x, h2);\n    sheet.appendChild(head);\n    return sheet;\n  }\n\n  // The series being added is already in the library under a different site.\n  // Adding it again would leave two entries for one book, each with its own\n  // half-finished bookmark — so ask first, and offer to move the entry over\n  // rather than start a second one.\n  function renderDuplicate(root, state, close) {\n    const { meta, match } = state;\n    const entry = match.entry;\n    const sheet = shell(root, t('modalAlreadyInLibrary'), close);\n\n    const lead = document.createElement('p');\n    lead.className = 'lead';\n    lead.textContent = match.confidence === 'same-title'\n      ? t('modalSameTitleLead')\n      : t('modalLikelyLead');\n    sheet.appendChild(lead);\n\n    const cmp = document.createElement('div');\n    cmp.className = 'cmp';\n    cmp.append(\n      sideCard(t('modalInYourLibrary'), entry.title, entry.sourceDomain,\n        state.progressLabel || entry.lastKnownChapter, false),\n      Object.assign(document.createElement('div'), { className: 'arrow', textContent: '→' }),\n      sideCard(t('modalThisPage'), meta.title, meta.sourceDomain,\n        meta.chapterLabel || meta.lastKnownChapter, true),\n    );\n    sheet.appendChild(cmp);\n\n    // A fuzzy title match is a guess. Say so, because accepting it merges two\n    // entries and there is no undo.\n    if (match.confidence === 'likely') {\n      const warn = document.createElement('p');\n      warn.className = 'warn';\n      warn.textContent = t('modalCloseTitlesWarning');\n      sheet.appendChild(warn);\n    }\n\n    const keeps = document.createElement('ul');\n    keeps.className = 'keeps';\n    for (const line of [t('modalKeepsProgress'), t('modalKeepsDate'), t('modalKeepsOldSite')]) {\n      const li = document.createElement('li');\n      li.textContent = line;\n      keeps.appendChild(li);\n    }\n    sheet.appendChild(keeps);\n\n    const err = document.createElement('p');\n    err.className = 'err';\n    err.hidden = true;\n\n    const migrate = document.createElement('button');\n    migrate.className = 'save';\n    migrate.textContent = `Migrate to ${meta.sourceDomain || 'this site'}`;\n    migrate.addEventListener('click', async () => {\n      migrate.disabled = true;\n      migrate.textContent = t('modalMigrating');\n      const resp = await send({ type: 'migrateEntry', id: entry.id, target: {\n        sourceUrl: meta.sourceUrl,\n        sourceDomain: meta.sourceDomain,\n        title: meta.title,\n        coverUrl: meta.coverUrl ?? null,\n        lastKnownChapter: meta.lastKnownChapter ?? null,\n        chapterUrl: meta.chapterUrl ?? null,\n        chapterLabel: meta.chapterLabel ?? null,\n      }});\n      if (resp?.error || !resp?.ok) {\n        err.hidden = false;\n        err.textContent = resp?.error || t('modalMigrationFailed');\n        migrate.disabled = false;\n        migrate.textContent = `Migrate to ${meta.sourceDomain || 'this site'}`;\n        return;\n      }\n      // Land in the normal edit view on the entry that just moved, so the\n      // migration is visible and still adjustable.\n      Object.assign(state, initialState(meta, resp.entry),\n        { view: 'form', match: null, signedIn: state.signedIn });\n      render(root, state, close);\n      // initialState cleared what the trackers had said, and this is a form the\n      // reader has not answered yet either. Ask again rather than leave the\n      // strip missing for the rest of the sheet's life.\n      askTrackers(root, state, close);\n    });\n\n    const separate = document.createElement('button');\n    separate.className = 'ghost';\n    separate.textContent = t('modalAddSeparately');\n    separate.addEventListener('click', () => {\n      // The guess was wrong, or they deliberately want both. Never ask again\n      // for this page within this modal.\n      state.view = 'form';\n      state.match = null;\n      render(root, state, close);\n    });\n\n    const cancel = document.createElement('button');\n    cancel.className = 'ghost quiet';\n    cancel.textContent = t('actionCancel');\n    cancel.addEventListener('click', close);\n\n    const actions = document.createElement('div');\n    actions.className = 'actions';\n    actions.append(migrate, separate, cancel);\n    sheet.append(actions, err);\n\n    function sideCard(cap, title, domain, at, isTo) {\n      const el = document.createElement('div');\n      el.className = isTo ? 'side to' : 'side';\n      const c = document.createElement('div');\n      c.className = 'cap';\n      c.textContent = cap;\n      const n = document.createElement('div');\n      n.className = 'name';\n      n.textContent = title || t('untitled');\n      const d = document.createElement('div');\n      d.className = 'dom';\n      d.textContent = domain || '';\n      el.append(c, n, d);\n      if (at) {\n        const a = document.createElement('div');\n        a.className = 'at';\n        a.textContent = at;\n        el.appendChild(a);\n      }\n      return el;\n    }\n  }\n\n  function render(root, state, close) {\n    if (state.view === 'duplicate' && state.match) return renderDuplicate(root, state, close);\n\n    // Every chip click rebuilds the sheet from scratch, which throws away the\n    // scroll offset and the focus with it. Carried across the redraw below.\n    const prevScroll = root.querySelector('.sheet')?.scrollTop ?? 0;\n    const sheet = shell(root, state.existing ? t('modalEditEntry') : t('popupAddToLibrary'), close);\n    // Two ways to redraw. `redraw` is what a chip, a date or a tag calls, and\n    // it marks the form as touched: from then on a tracker answer that arrives\n    // late may offer its values but must not install them behind the reader's\n    // back. `repaint` is for everything the sheet does to itself.\n    const repaint = () => render(root, state, close);\n    const redraw = () => { state.dirty = true; repaint(); };\n\n    // series identity\n    const series = document.createElement('div');\n    series.className = 'series';\n    const img = document.createElement('img');\n    // Empty rather than the file name: an <img> with an alt renders the alt in\n    // place of a picture that will not load, and the placeholder behind it is\n    // a better answer than the series' title written twice.\n    img.alt = '';\n    if (state.meta.coverUrl) {\n      img.src = state.meta.coverUrl;\n      // Scan-site covers are hotlink-protected often enough that this is the\n      // ordinary case rather than the accident: what the reader saw was the\n      // browser's broken-image icon where the cover should be.\n      img.addEventListener('error', () => img.removeAttribute('src'), { once: true });\n    }\n    const info = document.createElement('div');\n    // Named for what it is, not for its class: `t` is the translation function\n    // in this file now, and a local of that name shadowing it turns the line\n    // below into a call on a div.\n    const titleEl = document.createElement('div');\n    titleEl.className = 't';\n    titleEl.textContent = state.meta.title || t('untitled');\n    const d = document.createElement('div');\n    d.className = 'd';\n    d.textContent = state.meta.sourceDomain || '';\n    info.append(titleEl, d);\n    series.append(img, info);\n    sheet.appendChild(series);\n\n    // what AniList / MyAnimeList already say about this series\n    const tk = trackerBlock();\n    if (tk) sheet.appendChild(tk);\n\n    // folder\n    sheet.appendChild(group(t('fieldFolder'), FOLDERS.map((f) =>\n      chip(folderName(f), state.folder === f, () => {\n        state.folder = f;\n        redraw();\n      }))));\n\n    // language\n    // A language detected from the page may not be in the fixed list.\n    const langs = state.language && !LANGUAGES.includes(state.language)\n      ? [state.language, ...LANGUAGES]\n      : LANGUAGES;\n    const langChips = [\n      chip(t('chipNone'), state.language === null, () => { state.language = null; redraw(); }),\n      ...langs.map((l) =>\n        chip(l, state.language === l, () => { state.language = l; redraw(); })),\n    ];\n    sheet.appendChild(group(t('modalTranslatedLanguage'), langChips));\n\n    // current progress (read-only: it comes from the page)\n    if (state.meta.chapterLabel) {\n      sheet.appendChild(group(t('modalCurrentProgress'), [chip(state.meta.chapterLabel, true, () => {})]));\n    }\n\n    // start date\n    const dateInput = document.createElement('input');\n    dateInput.type = 'date';\n    dateInput.value = state.startDate || '';\n    dateInput.addEventListener('change', () => {\n      state.startDate = dateInput.value || null;\n      redraw();\n    });\n    const dateChips = document.createElement('div');\n    dateChips.className = 'chips';\n    dateChips.append(\n      chip(t('chipNone'), state.startDate === null, () => { state.startDate = null; redraw(); }),\n      chip(t('dayToday'), state.startDate === todayISO(), () => { state.startDate = todayISO(); redraw(); }),\n      chip(t('dayYesterday'), state.startDate === shiftISO(-1), () => { state.startDate = shiftISO(-1); redraw(); }),\n    );\n    const dateSection = document.createElement('section');\n    const dateTitle = document.createElement('h3');\n    dateTitle.textContent = t('fieldStartDate');\n    dateSection.append(dateTitle, dateChips, spacer(), dateInput);\n    sheet.appendChild(dateSection);\n\n    // score\n    const scoreChips = [chip(t('chipNone'), state.score === null, () => { state.score = null; redraw(); })];\n    for (let n = 1; n <= 10; n++) {\n      scoreChips.push(chip(String(n), state.score === n, () => { state.score = n; redraw(); }));\n    }\n    sheet.appendChild(group(t('fieldScore'), scoreChips));\n\n    // tags\n    const tagChips = state.tags.map((tag) =>\n      chip(tag, true, () => {\n        state.tags = state.tags.filter((x) => x !== tag);\n        redraw();\n      }, true));\n    const tagInput = document.createElement('input');\n    tagInput.type = 'text';\n    tagInput.placeholder = t('modalAddTagHint');\n    tagInput.addEventListener('keydown', (e) => {\n      if (e.key !== 'Enter') return;\n      e.preventDefault();\n      const v = tagInput.value.trim();\n      if (v && !state.tags.includes(v)) {\n        state.tags.push(v);\n        state.focus = 'tags';\n        redraw();\n      }\n    });\n    const tagSection = document.createElement('section');\n    const tagTitle = document.createElement('h3');\n    tagTitle.textContent = t('fieldTags');\n    const tagWrap = document.createElement('div');\n    tagWrap.className = 'chips';\n    tagWrap.append(...tagChips);\n    tagSection.append(tagTitle, tagWrap, spacer(), tagInput);\n    sheet.appendChild(tagSection);\n\n    // save\n    const save = document.createElement('button');\n    save.className = 'save';\n    save.textContent = t('actionSave');\n    const err = document.createElement('p');\n    err.className = 'err';\n    err.hidden = true;\n    save.addEventListener('click', async () => {\n      save.disabled = true;\n      save.textContent = t('statusSaving');\n      const resp = await send({ type: 'addToLibrary', entry: entryPayload(state) });\n      if (resp?.error) {\n        err.hidden = false;\n        err.textContent = resp.error;\n        save.disabled = false;\n        save.textContent = t('actionSave');\n        return;\n      }\n      close();\n    });\n    sheet.append(save, err);\n\n    // Saving always works locally, but without an account nothing reaches the\n    // web app — and \"I saved it and it isn't there\" is otherwise a total\n    // mystery from here.\n    if (!state.signedIn) {\n      const hint = document.createElement('p');\n      hint.className = 'hint';\n      hint.textContent = t('modalLocalOnlyHint');\n      sheet.appendChild(hint);\n    }\n\n    // Put the reader back where they were: picking a score sits near the bottom\n    // of the form and used to bounce the sheet to the top, and typing a second\n    // tag meant clicking the field again because Enter redrew it away.\n    sheet.scrollTop = prevScroll;\n    if (state.focus === 'tags') {\n      state.focus = null;\n      tagInput.focus();\n    }\n\n    function group(title, chipEls) {\n      const s = document.createElement('section');\n      const h = document.createElement('h3');\n      h.textContent = title;\n      const c = document.createElement('div');\n      c.className = 'chips';\n      c.append(...chipEls);\n      s.append(h, c);\n      return s;\n    }\n    function spacer() {\n      const s = document.createElement('div');\n      s.style.height = '7px';\n      return s;\n    }\n\n    /**\n     * The tracker strip: what the reader's own accounts hold for this series,\n     * or a way to connect one.\n     *\n     * Nothing is drawn until the answer arrives — the sheet must open at the\n     * speed of the page, not at the speed of AniList — and nothing is drawn at\n     * all without an account, because connecting a tracker goes through the\n     * PanelFlow account that stores the token.\n     */\n    function trackerBlock() {\n      if (!state.signedIn || !state.tracker) return null;\n      const { entries = [], connected = [], errors = [] } = state.tracker;\n      const box = document.createElement('section');\n      box.className = 'tk';\n\n      for (const entry of entries) {\n        const row = document.createElement('div');\n        row.className = 'tkrow';\n        const txt = document.createElement('div');\n        txt.className = 'tktxt';\n        const name = document.createElement('div');\n        name.className = 'tkname';\n        name.textContent = trackerName(entry.service);\n        const sum = document.createElement('div');\n        sum.className = 'tksum';\n        sum.textContent = trackerSummary(entry);\n        // The title the tracker matched, when it is not the one on the page.\n        // A prefill from the wrong series is the failure worth catching, and\n        // the only way to catch it is to be told which series was read.\n        if (entry.remoteTitle && entry.remoteTitle !== state.meta.title) {\n          const as = document.createElement('div');\n          as.className = 'tkas';\n          as.textContent = t('modalTrackerMatchedAs', [entry.remoteTitle]);\n          txt.append(name, sum, as);\n        } else {\n          txt.append(name, sum);\n        }\n        row.appendChild(txt);\n\n        const act = document.createElement('button');\n        act.type = 'button';\n        act.className = 'tkbtn';\n        if (state.appliedFrom === entry.service) {\n          act.textContent = t('actionUndo');\n          act.addEventListener('click', () => { undoTracker(state); repaint(); });\n          row.classList.add('on');\n        } else {\n          act.textContent = t('actionUse');\n          act.addEventListener('click', () => {\n            applyTracker(state, entry);\n            // Applying is the reader choosing. Anything that arrives later has\n            // to ask, exactly as it would after a chip.\n            state.dirty = true;\n            repaint();\n          });\n        }\n        row.appendChild(act);\n        box.appendChild(row);\n      }\n\n      // Connected, asked, and it is not on the list.\n      //\n      // This used to be one sentence and a full stop, which said the lookup had\n      // happened and left the reader with nowhere to go: their AniList had\n      // never heard of the series they were reading and PanelFlow, which knew\n      // both facts, offered no way to say so. It is a row with a button now.\n      const failed = new Set(errors.map((e) => e.service));\n      for (const service of connected) {\n        if (entries.some((e) => e.service === service) || failed.has(service)) continue;\n        box.appendChild(addRow(service));\n      }\n\n      for (const e of errors) {\n        const line = document.createElement('div');\n        line.className = 'tknote warnish';\n        line.textContent = t('modalTrackerUnreachable', [trackerName(e.service), e.error]);\n        box.appendChild(line);\n      }\n\n      // Nothing connected: the offer. Both services open their authorisation\n      // page in a new tab; this sheet is a content script and cannot open one\n      // itself, so the worker does it.\n      //\n      // The same file runs inside the phone app's in-app browser, where the\n      // worker is a WebView with no tabs to open and no tracker screen behind\n      // it. Offering a button there would be offering a dead end, so it says\n      // where the connecting is actually done instead.\n      if (!connected.length && !isShim()) {\n        const note = document.createElement('div');\n        note.className = 'tknote';\n        note.textContent = t('modalConnectTrackerLead');\n        const row = document.createElement('div');\n        row.className = 'chips';\n        const err2 = document.createElement('div');\n        err2.className = 'tknote warnish';\n        err2.hidden = true;\n        for (const service of ['anilist', 'mal']) {\n          row.appendChild(chip(t('modalTrackerConnect', [trackerName(service)]), false, async (ev) => {\n            const btn = ev.currentTarget;\n            btn.disabled = true;\n            const resp = await send({ type: 'trackerConnectTab', service });\n            btn.disabled = false;\n            if (resp?.error) {\n              err2.hidden = false;\n              err2.textContent = `${trackerName(service)}: ${resp.error}`;\n              return;\n            }\n            // The tab is open but nobody has authorised anything yet — asking\n            // now would answer \"not connected\" and be wrong within the minute.\n            // The reader comes back to this window when they are done, and\n            // that is the moment to ask again.\n            window.addEventListener('focus', () => {\n              if (form === state && host) askTrackers(root, state, close);\n            }, { once: true });\n          }));\n        }\n        box.append(note, row, err2);\n      } else if (!connected.length && isShim()) {\n        const note = document.createElement('div');\n        note.className = 'tknote';\n        note.textContent = t('modalConnectTrackerHint');\n        box.appendChild(note);\n      }\n\n      return box.childNodes.length ? box : null;\n\n      /**\n       * One service that has never heard of this series, and the button that\n       * tells it.\n       *\n       * What the button does is save the entry and then send the bookmark: the\n       * push lives on the server, on the progress route, and it is a library\n       * row and a chapter label that it works from. So \"add to AniList\" is\n       * exactly \"add to the library, then say where I am\" — which is also why\n       * it sends the chapter number the reader is actually on rather than\n       * starting them at zero.\n       */\n      function addRow(service) {\n        const live = state.addTo?.service === service ? state.addTo : null;\n        const row = document.createElement('div');\n        row.className = 'tkrow';\n        const txt = document.createElement('div');\n        txt.className = 'tktxt';\n        const name = document.createElement('div');\n        name.className = 'tkname';\n        name.textContent = trackerName(service);\n        const sum = document.createElement('div');\n        sum.className = 'tksum';\n        sum.textContent = live?.note || t('modalTrackerNotListed', [trackerName(service)]);\n        txt.append(name, sum);\n        row.appendChild(txt);\n\n        if (!live?.done) {\n          const act = document.createElement('button');\n          act.type = 'button';\n          act.className = 'tkbtn';\n          act.textContent = live?.busy ? t('modalTrackerAdding') : t('modalTrackerAdd');\n          act.disabled = !!live?.busy;\n          act.addEventListener('click', () => addToTracker(service));\n          row.appendChild(act);\n        } else {\n          row.classList.add('on');\n        }\n\n        // The service searched its catalogue and did not find this title under\n        // the name the site uses for it. Its own best guesses are the only\n        // thing that can settle that, and the reader is the only one allowed\n        // to choose between them — picking one here would write a chapter\n        // count onto a stranger's series.\n        if (live?.hits?.length) {\n          const wrap = document.createElement('div');\n          wrap.className = 'chips';\n          for (const hit of live.hits) {\n            wrap.appendChild(chip(hit.title, false, () => linkAndPush(service, hit)));\n          }\n          const box2 = document.createElement('div');\n          box2.append(row, wrap);\n          return box2;\n        }\n        return row;\n      }\n\n      /** Save the series, then tell the service where the reader is. */\n      async function addToTracker(service) {\n        state.addTo = { service, busy: true, note: t('modalTrackerAdding') };\n        repaint();\n        const saved = await send({ type: 'addToLibrary', entry: entryPayload(state) });\n        if (form !== state || !host) return;\n        if (saved?.error || !saved?.entry) {\n          state.addTo = { service, note: saved?.error || t('modalTrackerFailed',\n            [trackerName(service), 'library']) };\n          return repaint();\n        }\n        state.existing = saved.entry;\n        await pushAndReport(service, saved.entry);\n      }\n\n      /** The reader picked the series themselves; link it and send again. */\n      async function linkAndPush(service, hit) {\n        state.addTo = { service, busy: true, note: t('modalTrackerAdding') };\n        repaint();\n        const linked = await send({\n          type: 'trackerLink',\n          service,\n          libraryId: state.existing?.remoteId,\n          remoteId: hit.id,\n          remoteTitle: hit.title,\n          state: 'linked',\n        });\n        if (form !== state || !host) return;\n        if (linked?.error) {\n          state.addTo = { service, note: t('modalTrackerFailed',\n            [trackerName(service), linked.error]) };\n          return repaint();\n        }\n        await pushAndReport(service, state.existing);\n      }\n\n      /**\n       * Send the bookmark and say, in one line, what the service did with it.\n       *\n       * Every outcome gets a sentence, including the two that are not failures:\n       * a tracker already further along is the forward-only rule working, and a\n       * title the catalogue does not recognise is a question rather than an\n       * error.\n       */\n      async function pushAndReport(service, entry) {\n        const resp = await send({ type: 'trackerPushOne', sourceUrl: entry?.sourceUrl });\n        if (form !== state || !host) return;\n        const name = trackerName(service);\n        const r = (resp?.trackers || []).find((x) => x.service === service);\n        // A refusal with no reason attached is still a refusal, and saying so\n        // beats a sentence that trails off after the dash.\n        const fail = (why) => {\n          state.addTo = {\n            service,\n            note: t('modalTrackerFailed', [name, why || t('modalTrackerNoAnswer')]),\n          };\n          repaint();\n        };\n        if (resp?.error) return fail(resp.error);\n        if (!r) return fail(null);\n        if (r.error) return fail(r.error);\n        if (r.skipped === 'unmatched' || r.skipped === 'no-title') {\n          const found = await send({ type: 'trackerSearch', service, q: state.meta.title });\n          if (form !== state || !host) return;\n          const hits = (found?.hits || []).slice(0, 5);\n          state.addTo = {\n            service,\n            note: hits.length\n              ? t('modalTrackerPickSeries', [name])\n              : t('modalTrackerNoHits', [name, state.meta.title]),\n            hits,\n          };\n          return repaint();\n        }\n        if (r.skipped === 'not-further') {\n          state.addTo = { service, done: true, note: t('modalTrackerAhead', [name]) };\n          return repaint();\n        }\n        state.addTo = {\n          service,\n          done: true,\n          note: r.chapter != null\n            ? t('modalTrackerAdded', [name, String(r.chapter)])\n            : t('modalTrackerAddedPlain', [name]),\n        };\n        return repaint();\n      }\n    }\n  }\n\n  /**\n   * Fill in what a chapter page does not know about the series.\n   *\n   * A chapter page has no cover — its og:image is a panel, or the site's logo,\n   * or nothing — and no genres: the only genre links on it belong to the site's\n   * own menu. The series page has both, and detect.js already fetches it for\n   * the popup's \"add this page\". The sheet used to be opened from the reader\n   * with the raw chapter-page meta instead, which is why it showed a broken\n   * image and offered Kingdom the tags \"Romance\" and \"Adulte\".\n   *\n   * Behind the sheet rather than in front of it, exactly like the tracker strip\n   * below: this crosses the network to the site, and the reader pressed a\n   * button on a page they are looking at. What comes back is metadata, so it\n   * fills gaps and corrects the cover; the tags are only replaced while the\n   * form is still untouched and new, on the same rule as a tracker prefill.\n   */\n  async function enrichMeta(root, state, close) {\n    if (state.meta.enriched) return;\n    // Promise.resolve around it: on a surface where detect.js is not running\n    // there is no promise to hang a .catch on, and the sheet still has to open.\n    const better = await Promise.resolve(\n      window.__panelflowDetect?.enrichedMeta?.()).catch(() => null);\n    if (!better || form !== state || !host) return;\n    const wasSeeded = !state.dirty && !state.existing;\n    state.meta = { ...state.meta, ...better };\n    if (wasSeeded && better.genres?.length) state.tags = better.genres.slice(0, 8);\n    render(root, state, close);\n  }\n\n  /**\n   * Ask the backend what the reader's trackers hold for this title, then decide\n   * whether to fill the form or merely offer to.\n   *\n   * Auto-filling is limited to a brand-new, untouched entry. Editing an entry\n   * that already exists means the values in front of the reader are their own\n   * saved ones, and a remote list is not entitled to overwrite those without\n   * being asked; a form the reader has started answering is theirs for the same\n   * reason. In both cases the strip still appears with a \"Use\" button.\n   */\n  async function askTrackers(root, state, close) {\n    // The tokens hang off the PanelFlow account, so without one there is\n    // nothing to ask and nothing to offer.\n    if (!state.signedIn) return;\n    const resp = await send({ type: 'trackerEntry', title: state.meta.title });\n    // The sheet may have been closed, or reopened on another series, while the\n    // request was in flight.\n    if (form !== state || !host) return;\n    state.tracker = resp || { entries: [], connected: [] };\n    const first = (state.tracker.entries || [])[0];\n    if (first && !state.dirty && !state.existing) applyTracker(state, first);\n    render(root, state, close);\n  }\n\n  // --- public API -----------------------------------------------------------\n\n  function close() {\n    host?.remove();\n    host = null;\n    form = null;\n    document.removeEventListener('keydown', onKey, true);\n  }\n\n  function onKey(e) {\n    if (e.key === 'Escape' && host) { e.stopPropagation(); close(); }\n  }\n\n  async function open(meta) {\n    if (!meta?.title) return { ok: false, error: t('modalNoTitle') };\n    close();\n    const [similar, account, stored] = await Promise.all([\n      send({ type: 'findSimilar', meta }),\n      send({ type: 'getAccount' }),\n      send({ type: 'getProgressAll' }),\n    ]);\n\n    const best = (similar?.matches || [])[0];\n    // The same page, or the same series on the same site: saving updates that\n    // entry either way (background.js merges on seriesKey), so show the edit\n    // form rather than pretending this is a new addition.\n    const onThisSite = best && (best.confidence === 'same-page' || best.confidence === 'same-site');\n    const existing = onThisSite ? best.entry : undefined;\n    // A title match on a *different* site is the one case worth interrupting\n    // for: adding it would file the same book twice.\n    const duplicate = !onThisSite && best ? best : null;\n\n    host = document.createElement('div');\n    host.id = 'panelflow-libmodal';\n    // Closed: nothing on the page can reach in and restyle or read the form.\n    const root = host.attachShadow({ mode: 'closed' });\n    document.documentElement.appendChild(host);\n    form = initialState(meta, existing);\n    form.signedIn = !!account?.authUser;\n    form.match = duplicate;\n    form.view = duplicate ? 'duplicate' : 'form';\n    form.progressLabel = duplicate\n      ? stored?.progress?.[duplicate.entry.sourceUrl]?.chapterLabel ?? null\n      : null;\n    render(root, form, close);\n    document.addEventListener('keydown', onKey, true);\n    // Neither of the two below is awaited, and for the same reason.\n    enrichMeta(root, form, close);\n    // Deliberately not awaited: this crosses the network to AniList and MAL,\n    // and the reader pressed a button on a page they are looking at. The sheet\n    // is complete and usable without it; the strip appears when it appears.\n    askTrackers(root, form, close);\n    return { ok: true };\n  }\n\n  window.PanelFlowLibraryModal = { open, close, isOpen: () => !!host };\n})();\n\n}catch(e){if(window.PanelFlowFailed)window.PanelFlowFailed(\"library-modal.js\",e);else console.warn('panelflow: library-modal.js failed',e)}\ntry{\n// PanelFlow Reader Mode overlay.\n// Modes: vertical scroll, paged LTR, paged RTL, double-page spread.\n// Key differentiator: free-form pinch-zoom/pan that NEVER snaps back —\n// boundaries are elastic (resisted) but the view settles at the bound,\n// not at the origin.\n// Shortcuts: Esc close · arrows navigate · S preferences · B break 1st page.\n(() => {\n  'use strict';\n  if (window.__panelflowReaderLoaded) return;\n  window.__panelflowReaderLoaded = true;\n\n  /**\n   * Whether this page is inside one of PanelFlow's own phone shells.\n   *\n   * Not \"is this a touch screen\": a laptop with a touch screen is still a\n   * laptop with a keyboard, and the two things this answer changes — the help\n   * text and the hide-controls button — are about what the reader is holding.\n   * The three shells install exactly one of these two globals, and a page in\n   * Chrome has neither.\n   */\n  const inShell = () => !!(window.PanelFlowNative\n    || (window.webkit && window.webkit.messageHandlers\n      && window.webkit.messageHandlers.panelflow));\n\n  const PRELOAD_AHEAD = 3;\n  const MIN_VISIBLE_FRACTION = 0.15; // part of the page that must stay on screen\n\n  const DEFAULT_PREFS = {\n    brightness: 100, contrast: 100, gap: 0, stripWidth: 100,\n    autoNext: false, autoplaySpeed: 80, progressSize: 3,\n    tapZones: 'sides', invertTap: false,\n    // On by default, and the default is the whole argument: a page of artwork\n    // is looked at rather than read, and paper-white margins around it at night\n    // are a lamp pointed at the reader. Off hands the choice to the system —\n    // this runs on a scan site's origin and cannot see the settings page's.\n    readerDark: true,\n    // Off by default: a chapter list with holes in it is confusing until you\n    // know why, and the reader who wants this is the one on chapter 400 of a\n    // list of 900 — they will find the switch.\n    hideRead: false,\n    // Novel mode. Stored as whole numbers because every control here is a\n    // range input: the line height is a percentage, applied as 1.65.\n    fontSize: 18, lineHeight: 165, textWidth: 680,\n  };\n\n  // What a series is allowed to remember for itself, on top of the settings\n  // above. Mihon's hierarchy: global defaults, overridden per series, and the\n  // override is asked for rather than assumed.\n  //\n  // These three and no others, because these three are what actually differ\n  // between two series read on the same evening — which way the pages go, and\n  // how wide the strip or the text column is. Brightness, tap zones and\n  // autoplay describe the reader and the room they are in, not the book, and a\n  // reader who dims the screen for the night does not want it back at full\n  // brightness because the next series remembers otherwise. `mode` is listed\n  // apart because it is not in readerPrefs: it has always had storage of its own.\n  const SERIES_KEYS = ['stripWidth', 'textWidth'];\n  // A record is three numbers and a timestamp. The cap is not about bytes, it\n  // is about a store nobody ever prunes: four hundred series read over a year\n  // would otherwise carry four hundred rows of \"and this one is right to left\"\n  // for ever, including for series the reader dropped in March.\n  const SERIES_LIMIT = 200;\n\n  // How much of the screen width, on each edge, turns the page. The rest of it\n  // toggles the controls. `edges` is for readers who keep tapping the middle of\n  // a panel to look at it and turning the page by accident; `off` is for a mouse\n  // and the arrow keys, where a stray click should never move anything.\n  const TAP_LAYOUTS = { sides: 0.33, edges: 0.18, off: 0 };\n\n  // Shown for a moment when the reader opens and whenever the mode changes: the\n  // direction is the one thing you must know before the first tap, and getting\n  // it wrong means reading a chapter backwards before noticing.\n  //\n  // Read through t() at use, not once at load: this script is injected into a\n  // page that may outlive a locale change, and a frozen table would keep\n  // announcing the direction in the language the tab was opened in.\n  const modeToast = (mode) => ({\n    vertical: 'modeToastVertical',\n    ltr: 'modeToastLtr',\n    spread: 'modeToastSpread',\n  }[mode]);\n\n  const state = {\n    root: null, images: [], meta: null, rule: {}, nav: null, container: null,\n    mode: 'vertical', page: 0, chromeVisible: true,\n    zoom: 1, panX: 0, panY: 0,\n    breakFirst: false, prefs: { ...DEFAULT_PREFS },\n    // The settings as they are stored, kept apart from `prefs` so that a series\n    // override never leaks back into them: `prefs` is what this chapter reads\n    // by, `globalPrefs` is what every other series will read by tomorrow.\n    globalPrefs: { ...DEFAULT_PREFS }, seriesPrefs: null, seriesAll: {},\n    playing: false, playRaf: 0, playLastTs: 0,\n    harvestObserver: null, harvestTimer: 0,\n    // Novel mode: prose instead of pages. There is no strip to page through, so\n    // position is a scroll ratio and \"pages\" are screenfuls of text.\n    novel: false, paragraphs: [], screens: 1, scrollRatio: 0,\n    // Chapters of this series already read. Null until the worker answers,\n    // which is not the same as \"none are read\" — the list is drawn whole in the\n    // meantime rather than flickering from full to filtered.\n    readChapters: null,\n    // The chapter wheel: every chapter of the series, the row it opens on, and\n    // the frame the scroll handler is waiting for. The last two are what the\n    // scroll handler is allowed to touch — the rows as an array, and which of\n    // them currently carries the highlight — because looking either up in the\n    // DOM on a series with a thousand chapters is what stopped the wheel from\n    // scrolling at all.\n    chapters: [], wheelIndex: 0, wheelRaf: 0,\n    wheelRows: null, wheelOn: -1,\n  };\n\n  const $ = (sel) => state.root.querySelector(sel);\n\n  /**\n   * Ask the worker something, as a promise.\n   *\n   * A content script cannot load extension/send.js, so the one line it adds is\n   * repeated here: the worker names which handler died (`failedAt`), and this\n   * is the only place in this file that sees every answer. Without it a failed\n   * download or a lost bookmark shows a ⚠ on a button and says nothing to the\n   * page console, which is the one console anybody actually has open on a scan\n   * site. The reply is passed through untouched.\n   */\n  const send = (msg) => new Promise((r) => chrome.runtime.sendMessage(msg, r)).then((resp) => {\n    if (resp && resp.error) {\n      console.warn(`[panelflow] ${(msg && msg.type) || 'unknown'} failed`\n        + `${resp.failedAt ? ' in ' + resp.failedAt : ''}`\n        + `${resp.ref ? ' ref=' + resp.ref : ''}: ${resp.error}`);\n    }\n    return resp;\n  });\n\n  /** How many units the position is counted in — page images, or screenfuls. */\n  const pageTotal = () => (state.novel ? state.screens : state.images.length);\n\n  // The one question the modes get asked all over this file, so it is asked in\n  // one place. There used to be two — pairing and direction — because two of\n  // the five modes were right to left. Those two are gone: which side moves\n  // forward is now `invertTap`, one preference instead of a property smuggled\n  // into a mode name.\n  /**\n   * The three modes that exist, and what a fourth one becomes.\n   *\n   * A phone that read a chapter in `rtl` last month still has that word in\n   * storage, and another client on the account may still write it. Anything\n   * unrecognised reads as vertical — which is the mode that cannot be wrong,\n   * because a strip has no page order to get backwards.\n   */\n  const MODES = ['vertical', 'ltr', 'spread'];\n  const knownMode = (mode) => (MODES.includes(mode) ? mode : 'vertical');\n\n  const isSpread = () => state.mode === 'spread';\n\n  async function open(images, meta, rule, container, paragraphs = null) {\n    if (state.root) close();\n    Object.assign(state, {\n      images: images.slice(), meta, rule, container: container || null,\n      paragraphs: paragraphs ? paragraphs.slice() : [], novel: !!paragraphs,\n      screens: 1, scrollRatio: 0, readChapters: null,\n      chapters: [], wheelIndex: 0, wheelRows: null, wheelOn: -1,\n      // True, not false: restoreProgress can drop the reader straight at the\n      // bottom of a strip they left there, and that arrival must not count as\n      // reaching the end — with \"auto next chapter\" on it would walk the whole\n      // series in one go without a page being read. Only a crossing counts.\n      atEnd: true,\n      page: 0, zoom: 1, panX: 0, panY: 0,\n      breakFirst: false, playing: false,\n      nav: window.__panelflowDetect?.chapterNav?.() || null,\n    });\n    // After the state above, which callers read straight back, and before\n    // anything is built: every control in here is a symbol with a title on it,\n    // and a chosen language is read from storage — so building ahead of that\n    // read would label the whole reader in the browser's language instead.\n    await PanelFlowI18n.ready;\n    chrome.storage.local.get(['readerMode', 'readerPrefs', 'readerHelpSeen', 'readerSeries'], (v) => {\n      state.seriesAll = v.readerSeries || {};\n      state.seriesPrefs = state.seriesAll[state.meta.sourceUrl] || null;\n      // Text has no reading direction and nothing to page through, so the mode\n      // picker does not apply to it — and everything downstream that asks about\n      // the mode (\"can it autoplay\", \"do arrows scroll\") wants the strip answer.\n      //\n      // The series' own answer comes first when it has one. That is the whole\n      // of the override: a webtoon and a tankōbon read right to left have no\n      // business sharing a mode, and the reader who switched for one of them\n      // should not have to switch back on opening the other.\n      state.mode = state.novel ? 'vertical'\n        : knownMode(state.seriesPrefs?.mode || v.readerMode);\n      state.globalPrefs = { ...DEFAULT_PREFS, ...(v.readerPrefs || {}) };\n      state.prefs = { ...state.globalPrefs, ...seriesPick(state.seriesPrefs) };\n      build();\n      // Once, on the first chapter ever opened. Everything in the reader is a\n      // tap or a key with no label on it, and a reader who never finds them\n      // gets a worse version of the site they came from. Before render(), which\n      // is what raises the direction toast: the list already says the direction,\n      // and stacking a toast under a modal explaining it reads as a bug.\n      if (!v.readerHelpSeen) {\n        showHelp(true);\n        chrome.storage.local.set({ readerHelpSeen: true });\n      }\n      render();\n      restoreProgress();\n      // A novel is already whole: there is no lazy strip to walk the page for,\n      // and scrolling the document underneath would only fight the reader.\n      if (!state.novel) harvestLazyPages();\n      clock.banked = 0;\n      clock.day = null;\n      clockStart();\n      document.addEventListener('visibilitychange', onVisibility);\n      // Rotating the phone reflows the prose, so the chapter that was eight\n      // screens is now twelve and the scrubber is scaled to a length that no\n      // longer exists. Images do not have the problem: there are still n of them.\n      addEventListener('resize', measureScreens);\n      // Following the chapter's own \"next\" link leaves the page without ever\n      // closing the reader, and that is the most common way a chapter ends.\n      addEventListener('pagehide', bankRead);\n    });\n  }\n\n  function close() {\n    // Flushed, not scheduled. saveProgress is debounced by 800 ms and bails on\n    // a closed reader, so the queued call always fired after state.root was\n    // gone and did nothing — closing the reader was the one moment your\n    // position was guaranteed *not* to be written down.\n    saveProgress.flush();\n    // Same reason, and before state.root goes: the read has to be banked while\n    // there is still a chapter to attribute it to.\n    bankRead();\n    stopAutoplay();\n    stopHarvest();\n    document.removeEventListener('visibilitychange', onVisibility);\n    removeEventListener('resize', measureScreens);\n    removeEventListener('pagehide', bankRead);\n    document.removeEventListener('keydown', onKey, true);\n    document.removeEventListener('pointerdown', onWheelAway, true);\n    document.removeEventListener('fullscreenchange', syncFullscreenIcon);\n    // Removing the element while it owns the full screen leaves the tab stuck.\n    if (document.fullscreenElement === state.root) document.exitFullscreen().catch(() => {});\n    state.root?.remove();\n    state.root = null;\n    document.documentElement.classList.remove('panelflow-noscroll');\n\n    // Free the blob URLs the detector minted for this chapter — they pin the\n    // full image bytes until revoked. On a delay, not immediately: a CBZ\n    // download started a second before closing is still reading them. The array\n    // is captured here because open() calls close() and only then puts a fresh\n    // one on state — by the time the timer fires this is the old chapter's.\n    const stale = state.images;\n    const release = window.__panelflowDetect?.releaseStable;\n    if (release) setTimeout(() => release(stale), 60000);\n  }\n\n  const isOpen = () => !!state.root;\n\n  // --- DOM -----------------------------------------------------------------\n\n  function build() {\n    document.documentElement.classList.add('panelflow-noscroll');\n    const root = document.createElement('div');\n    root.id = 'panelflow-reader';\n    // Written with t() interpolated rather than data-i18n attributes placed\n    // afterwards: this markup is built once, in one string, and a second pass\n    // over it would only be a slower way of arriving at the same result. The\n    // keys resolve from bundled locale files, never from the page around them.\n    root.innerHTML = `\n      <div class=\"pf-topbar pf-chrome\">\n        <button class=\"pf-btn\" data-act=\"close\" title=\"${t('readerClose')}\">✕</button>\n        <span class=\"pf-title\"></span>\n        <button class=\"pf-btn pf-chapnav\" data-act=\"prevch\" title=\"${t('readerPrevChapter')}\">⏮</button>\n        <div class=\"pf-chapwrap\" hidden>\n          <button class=\"pf-btn pf-chapbtn\" data-act=\"chapters\" title=\"${t('readerChaptersTitle')}\"\n                  aria-haspopup=\"listbox\" aria-expanded=\"false\">${t('readerChapters')} ▾</button>\n          <div class=\"pf-wheel\" role=\"listbox\" tabindex=\"-1\" hidden></div>\n        </div>\n        <button class=\"pf-btn pf-chapnav\" data-act=\"nextch\" title=\"${t('readerNextChapter')}\">⏭</button>\n      </div>\n      <div class=\"pf-stage\"></div>\n      <div class=\"pf-side pf-chrome\">\n        <button class=\"pf-btn\" data-act=\"library\" title=\"${t('popupAddToLibrary')}\">🔖</button>\n        <button class=\"pf-btn\" data-act=\"download\" title=\"${t('readerDownloadCbz')}\">⬇</button>\n        <button class=\"pf-btn\" data-act=\"offline\" title=\"${t('readerSaveOffline')}\">📥</button>\n        <button class=\"pf-btn\" data-act=\"prefs\" title=\"${t('readerPrefs')}\">⚙</button>\n        <button class=\"pf-btn pf-resetzoom\" data-act=\"resetzoom\" title=\"${t('readerResetZoom')}\" hidden>⊙</button>\n        <button class=\"pf-btn\" data-act=\"fullscreen\" title=\"${t('readerFullscreen')}\">⛶</button>\n        <button class=\"pf-btn\" data-act=\"help\" title=\"${t('readerHelpTitle')}\">?</button>\n        <!-- Not on a phone. Tapping the middle of the page already hides these\n             controls and brings them back (see `onTapZones`), so the button is\n             a second way to do it that looks like the only way — somebody who\n             pressed it and did not know about the tap had no way back in. -->\n        ${inShell() ? '' : `<button class=\"pf-btn\" data-act=\"hide\" title=\"${t('readerHideControls')}\">⇱</button>`}\n      </div>\n      <div class=\"pf-prefs pf-chrome\" hidden>\n        <label class=\"pf-check pf-seriesrow\"><input class=\"pf-seriespref\" type=\"checkbox\"> ${t('readerSeriesPrefs')}</label>\n        <label class=\"pf-only-strip\">${t('readerReadingMode')}\n          <select class=\"pf-mode\">\n            <option value=\"vertical\">${t('modeShortVertical')}</option>\n            <option value=\"ltr\">${t('modeShortLtr')}</option>\n            <option value=\"spread\">${t('modeShortSpread')}</option>\n          </select>\n        </label>\n        <label>${t('readerBrightness')} <input data-pref=\"brightness\" type=\"range\" min=\"30\" max=\"130\" step=\"5\"></label>\n        <label>${t('readerContrast')} <input data-pref=\"contrast\" type=\"range\" min=\"50\" max=\"150\" step=\"5\"></label>\n        <label class=\"pf-only-strip\">${t('readerGapSize')} <input data-pref=\"gap\" type=\"range\" min=\"0\" max=\"40\" step=\"2\"></label>\n        <label class=\"pf-only-strip\">${t('readerStripWidth')} <input data-pref=\"stripWidth\" type=\"range\" min=\"40\" max=\"100\" step=\"5\"></label>\n        <label class=\"pf-only-novel\">${t('readerTextSize')} <input data-pref=\"fontSize\" type=\"range\" min=\"13\" max=\"30\" step=\"1\"></label>\n        <label class=\"pf-only-novel\">${t('readerLineSpacing')} <input data-pref=\"lineHeight\" type=\"range\" min=\"120\" max=\"220\" step=\"5\"></label>\n        <label class=\"pf-only-novel\">${t('readerTextWidth')} <input data-pref=\"textWidth\" type=\"range\" min=\"360\" max=\"900\" step=\"20\"></label>\n        <label>${t('readerPlaySpeed')} <input data-pref=\"autoplaySpeed\" type=\"range\" min=\"20\" max=\"300\" step=\"10\"></label>\n        <label>${t('readerProgressSize')} <input data-pref=\"progressSize\" type=\"range\" min=\"0\" max=\"10\" step=\"1\"></label>\n        <label class=\"pf-only-strip\">${t('readerTapZones')}\n          <select class=\"pf-select\" data-pref=\"tapZones\">\n            <option value=\"sides\">${t('readerTapSides')}</option>\n            <option value=\"edges\">${t('readerTapEdges')}</option>\n            <option value=\"off\">${t('readerTapOff')}</option>\n          </select>\n        </label>\n        <label class=\"pf-check pf-only-strip\"><input data-pref=\"invertTap\" type=\"checkbox\"> ${t('readerSwapTap')}</label>\n        <label class=\"pf-check\"><input data-pref=\"autoNext\" type=\"checkbox\"> ${t('readerAutoNext')}</label>\n        <label class=\"pf-check\"><input data-pref=\"hideRead\" type=\"checkbox\"> ${t('readerHideRead')}</label>\n        <!-- Fifteen sliders and six checkboxes deep, there has to be a way out\n             that is not \"remember what it used to be\". Resets this reader's own\n             answers, not the series override beside them: undoing a global\n             default should not also forget that one webtoon reads vertically. -->\n        <button class=\"pf-btn pf-resetprefs\" data-act=\"resetprefs\">${t('readerResetDefaults')}</button>\n      </div>\n      <div class=\"pf-zones\" hidden></div>\n      <div class=\"pf-toast\" hidden></div>\n      <div class=\"pf-help\" hidden>\n        <h3>${t('readerHelpHead')}</h3>\n        <!-- The tour is not the same tour on a phone. Four of these seven\n             lines are about a keyboard and a mouse wheel, and reading them on a\n             touch screen is being told about a machine you are not holding.\n             What replaces them is the one gesture the phone has and the desktop\n             does not need explained: tap the middle. -->\n        <ul>\n          <li>${t('readerHelpTap')}</li>\n          <li>${t('readerHelpPinch')}</li>\n          <li>${t('readerHelpDoubleTap')}</li>\n          ${inShell()\n            ? `<li>${t('readerHelpToggleChrome')}</li>`\n            : `<li>${t('readerHelpArrows')}</li>\n          <li>${t('readerHelpWheel')}</li>\n          <li>${t('readerHelpKeys1')}</li>\n          <li>${t('readerHelpKeys2')}</li>`}\n        </ul>\n        <p class=\"pf-help-note\">${t('readerHelpNote')}</p>\n        <button class=\"pf-btn\" data-act=\"help-ok\">${t('actionGotIt')}</button>\n      </div>\n      <div class=\"pf-end\" hidden role=\"group\" aria-label=\"${t('readerEndTitle')}\">\n        <h3 class=\"pf-end-title\"></h3>\n        <p class=\"pf-end-left\"></p>\n        <div class=\"pf-end-acts\">\n          <button class=\"pf-btn pf-end-go\" data-act=\"end-next\" hidden></button>\n          <button class=\"pf-btn\" data-act=\"end-read\"></button>\n          <button class=\"pf-btn\" data-act=\"end-stay\">${t('readerEndStay')}</button>\n        </div>\n      </div>\n      <div class=\"pf-bottombar pf-chrome\">\n        <span class=\"pf-counter\"></span>\n        <input class=\"pf-scrub\" type=\"range\" min=\"1\" value=\"1\" title=\"${t('readerCurrentPage')}\">\n        <button class=\"pf-btn pf-break\" data-act=\"break\" title=\"${t('readerBreakFirst')}\">⤸</button>\n        <button class=\"pf-btn pf-play\" data-act=\"play\" title=\"${t('readerAutoPlay')}\">▶</button>\n      </div>\n      <div class=\"pf-progress\"><div class=\"pf-progress-fill\"></div></div>`;\n    // Nothing that happens inside the reader is the page's business. Aggregator\n    // sites hang \"open an ad\" handlers off the document, and the reader is a\n    // div in their document — so pressing our own close button was firing the\n    // site's pop-under handler as well as ours. Stopped here, in the bubble\n    // phase, so the reader's own listeners (on these elements, and in capture\n    // on the document) all still run.\n    for (const type of ['pointerdown', 'pointerup', 'mousedown', 'mouseup', 'click',\n      'dblclick', 'auxclick', 'touchstart', 'touchend', 'contextmenu']) {\n      root.addEventListener(type, (e) => e.stopPropagation());\n    }\n    document.documentElement.appendChild(root);\n    state.root = root;\n\n    $('.pf-title').textContent = state.meta.title;\n    $('.pf-mode').value = state.mode;\n    $('.pf-mode').addEventListener('change', (e) => {\n      state.mode = e.target.value;\n      // Same fork as every other overridable setting: this series' record when\n      // it has one, the global default otherwise.\n      if (state.seriesPrefs) { state.seriesPrefs.mode = state.mode; saveSeriesPrefs(); }\n      else chrome.storage.local.set({ readerMode: state.mode });\n      stopAutoplay();\n      render();\n    });\n    const lock = $('.pf-seriespref');\n    lock.checked = !!state.seriesPrefs;\n    // Nothing to key a record on, so nothing to offer: a chapter reached without\n    // a series behind it would tick the box and forget by the next page.\n    if (!state.meta.sourceUrl) $('.pf-seriesrow').hidden = true;\n    lock.addEventListener('change', () => toggleSeriesPrefs(lock.checked));\n    root.querySelector('[data-act=\"end-next\"]').addEventListener('click', () => {\n      const url = nextChapterUrl();\n      if (url) gotoChapter(url);\n    });\n    root.querySelector('[data-act=\"end-read\"]').addEventListener('click', markChapterRead);\n    root.querySelector('[data-act=\"end-stay\"]').addEventListener('click', () => showEnd(false));\n    root.querySelector('[data-act=\"close\"]').addEventListener('click', close);\n    root.querySelector('[data-act=\"library\"]').addEventListener('click', addToLibrary);\n    root.querySelector('[data-act=\"prefs\"]').addEventListener('click', togglePrefs);\n    root.querySelector('[data-act=\"break\"]').addEventListener('click', toggleBreak);\n    root.querySelector('[data-act=\"play\"]').addEventListener('click', toggleAutoplay);\n    root.querySelector('[data-act=\"download\"]').addEventListener('click', downloadChapter);\n    root.querySelector('[data-act=\"offline\"]').addEventListener('click', toggleOffline);\n    root.querySelector('[data-act=\"fullscreen\"]').addEventListener('click', toggleFullscreen);\n    // Absent on a phone, on purpose — see the markup. `?.` and not a branch,\n    // because \"this control does not exist here\" is not a failure to report.\n    root.querySelector('[data-act=\"hide\"]')?.addEventListener('click', () => setChrome(false));\n    root.querySelector('[data-act=\"help\"]').addEventListener('click', () => showHelp($('.pf-help').hidden));\n    root.querySelector('[data-act=\"help-ok\"]').addEventListener('click', () => showHelp(false));\n    root.querySelector('[data-act=\"resetprefs\"]').addEventListener('click', resetPrefs);\n    root.querySelector('[data-act=\"resetzoom\"]').addEventListener('click', () => {\n      resetTransform();\n      applyTransform();\n    });\n    // Leaving full screen by Esc/F11 rather than our button must still update\n    // the icon, so track the document's state instead of our own flag.\n    document.addEventListener('fullscreenchange', syncFullscreenIcon);\n    buildChapterNav();\n    buildPrefsPanel();\n    syncPrefsRows();\n    // Whether this chapter is already on the device is a question for the\n    // worker, so the button starts in its unsaved state and corrects itself.\n    refreshOffline();\n\n    const scrub = $('.pf-scrub');\n    scrub.max = pageTotal();\n    scrub.addEventListener('input', () => {\n      const n = parseInt(scrub.value, 10) - 1;\n      if (state.mode === 'vertical') {\n        const stage = $('.pf-stage');\n        stage.scrollTop = (n / Math.max(1, pageTotal() - 1)) *\n          (stage.scrollHeight - stage.clientHeight);\n      } else {\n        showPage(n);\n      }\n    });\n\n    document.addEventListener('keydown', onKey, true);\n  }\n\n  function buildChapterNav() {\n    const nav = state.nav;\n    if (!nav?.prevUrl) $('[data-act=\"prevch\"]').hidden = true;\n    if (!nav?.nextUrl) $('[data-act=\"nextch\"]').hidden = true;\n    $('[data-act=\"prevch\"]').addEventListener('click', () => nav?.prevUrl && gotoChapter(nav.prevUrl));\n    $('[data-act=\"nextch\"]').addEventListener('click', () => nav?.nextUrl && gotoChapter(nav.nextUrl));\n\n    $('.pf-chapbtn').textContent = `${state.meta.chapterLabel || t('readerChapters')} ▾`;\n    $('.pf-chapbtn').addEventListener('click', () => openWheel($('.pf-wheel').hidden));\n    const wheel = $('.pf-wheel');\n    wheel.addEventListener('click', (e) => {\n      const row = e.target.closest('.pf-wrow');\n      if (row?.dataset.url) { e.stopPropagation(); pickChapter(row.dataset.url); }\n    });\n    // The highlight follows the middle of the wheel, not the pointer: what you\n    // scrolled to the centre is what Enter picks.\n    wheel.addEventListener('scroll', () => {\n      if (state.wheelRaf) return;\n      state.wheelRaf = requestAnimationFrame(() => {\n        state.wheelRaf = 0;\n        // The reader can close between the scroll and the frame it asked for.\n        if (state.root) markCentre(centreIndex());\n      });\n    }, { passive: true });\n    loadChapters();\n  }\n\n  /**\n   * The two things the worker knows about this series that the page does not:\n   * which chapters have already been read, and how many there are. Asked for\n   * together because they are drawn together — one round trip, one repaint.\n   */\n  async function loadChapters() {\n    const [read, range] = await Promise.all([\n      send({ type: 'getReadChapters', sourceUrl: state.meta.sourceUrl }),\n      send({\n        type: 'chapterList',\n        sourceUrl: state.meta.sourceUrl,\n        chapterUrl: state.meta.chapterUrl || location.href,\n        chapterLabel: state.meta.chapterLabel,\n      }),\n    ]);\n    // The reader may have been closed while the worker was answering, and the\n    // answer belongs to the chapter that asked for it.\n    if (!state.root) return;\n    state.readChapters = new Set(read?.chapters || []);\n    state.chapters = mergeChapters(state.nav?.options || [], range?.chapters || []);\n    // Nothing to pick from: no list, no site links either. The prev/next\n    // buttons, if the page offered any, are the whole of the navigation.\n    $('.pf-chapwrap').hidden = state.chapters.length < 2;\n    fillWheel();\n  }\n\n  /**\n   * The site's own chapter list, topped up with the chapters it did not link.\n   *\n   * A site that lists everything is left as it is. A site that lists three —\n   * previous, current, next — gets the rest of the series filled in from the\n   * worker's derived range. Where both have a chapter the site's own link wins:\n   * it is the address the site publishes, and a derived one is only ever a very\n   * good guess.\n   */\n  function mergeChapters(options, derived) {\n    const numOf = (label) => window.PanelFlowMatch?.chapterNumber(label) ?? null;\n    const byNum = new Map();\n    const unnumbered = [];\n    for (const { label, url } of options) {\n      const n = numOf(label);\n      if (n === null) unnumbered.push({ n: null, label, url });\n      else if (!byNum.has(n)) byNum.set(n, { n, label, url });\n    }\n    for (const row of derived) if (!byNum.has(row.n)) byNum.set(row.n, row);\n    // Newest first, whatever order the page listed them in.\n    return [...byNum.values()].sort((a, b) => b.n - a.n).concat(unnumbered);\n  }\n\n  /** Whether a row is the chapter on screen. */\n  const isHere = (url) => url === location.href || url === state.meta.chapterUrl;\n\n  /**\n   * How tall one row is, asked of the row rather than assumed from the CSS.\n   * Asked every time on purpose: a reader who zooms the page mid-chapter gets\n   * taller rows without the wheel being rebuilt, and a remembered 32 would\n   * centre the wheel on the wrong chapter for the rest of the evening.\n   */\n  const rowHeight = () => $('.pf-wheel .pf-wrow')?.offsetHeight || 32;\n\n  /**\n   * The wheel's rows, in order, kept rather than asked for again. `fillWheel`\n   * is the only thing that changes them, and it is the only thing that clears\n   * this.\n   */\n  const wheelRows = () => (state.wheelRows ||= [...$('.pf-wheel').children]);\n\n  /**\n   * The wheel, rebuilt rather than filtered in place: the reader's stylesheet\n   * is injected into someone else's page and must not carry a bare `[hidden]`\n   * rule, so rows that should not be there simply are not created.\n   */\n  function fillWheel() {\n    const wheel = $('.pf-wheel');\n    wheel.textContent = '';\n    state.wheelIndex = 0;\n    state.wheelRows = null;\n    state.wheelOn = -1;\n    let dropped = 0;\n    let i = 0;\n    for (const { label, url } of state.chapters) {\n      // The chapter being read stays, read or not: a wheel whose current row is\n      // missing opens somewhere else entirely and looks like it jumped. Either\n      // url counts, because they are not always the same string — the page's\n      // list may carry a trailing slash or a #anchor the address bar does not.\n      const here = isHere(url);\n      if (state.prefs.hideRead && !here && state.readChapters?.has(url)) { dropped++; continue; }\n      const row = document.createElement('div');\n      row.className = 'pf-wrow';\n      row.setAttribute('role', 'option');\n      row.dataset.url = url;\n      row.textContent = label;\n      // Read, or pointedly not. Both are said in colour because the only thing\n      // anyone comes to a 1200-row wheel to find is the line between the two,\n      // and a wheel where every row is the same grey makes you read labels to\n      // find it. Nothing is claimed before the history has answered: until then\n      // readChapters is null and every row keeps the neutral colour, rather\n      // than flashing a wheel of \"unread\" at someone who has read all of it.\n      if (state.readChapters) {\n        row.classList.add(state.readChapters.has(url) ? 'pf-read' : 'pf-unread');\n      }\n      if (here) { row.classList.add('pf-here'); state.wheelIndex = i; }\n      wheel.appendChild(row);\n      i++;\n    }\n    if (dropped) {\n      // Why the wheel is short, said in the wheel itself. No url, so it cannot\n      // be picked and navigated to.\n      const note = document.createElement('div');\n      note.className = 'pf-wrow pf-wnote';\n      // One key per plural form rather than a suffix glued on: languages do not\n      // agree on where the plural lives, or on there being only two of them.\n      note.textContent = t(dropped === 1 ? 'readerHiddenOne' : 'readerHiddenMany', [String(dropped)]);\n      wheel.appendChild(note);\n    }\n    // Taken once, now that the rows exist, rather than on every scroll frame.\n    state.wheelRows = [...wheel.children];\n    if (!wheel.hidden) centreOn(state.wheelIndex);\n  }\n\n  /** The row currently in the middle of the wheel. */\n  function centreIndex() {\n    const max = wheelRows().length - 1;\n    return Math.max(0, Math.min(max, Math.round($('.pf-wheel').scrollTop / rowHeight())));\n  }\n\n  /**\n   * Put row `i` in the middle. The wheel is padded by half its own height at\n   * both ends, so a row's scroll position is simply its index times its height\n   * — which is what makes the first and last chapter reachable at the centre.\n   */\n  function centreOn(i, smooth = false) {\n    const wheel = $('.pf-wheel');\n    const top = i * rowHeight();\n    if (smooth) wheel.scrollTo({ top, behavior: 'smooth' });\n    else wheel.scrollTop = top;\n    markCentre(i);\n  }\n\n  /**\n   * Move the highlight rather than repaint it. This used to toggle `pf-on` on\n   * every row on every scroll frame: on a series with a thousand chapters that\n   * is a thousand class writes and a thousand style invalidations to change\n   * one of them, sixty times a second — which is why the wheel appeared to\n   * freeze on long-running mangas and never on a fifty-chapter webtoon.\n   */\n  function markCentre(i) {\n    if (state.wheelOn === i) return;\n    const rows = wheelRows();\n    rows[state.wheelOn]?.classList.remove('pf-on');\n    rows[i]?.classList.add('pf-on');\n    state.wheelOn = i;\n  }\n\n  /**\n   * A press anywhere but the wheel closes it — including on the page behind the\n   * reader, which is why this is on the document and in the capture phase. One\n   * function, added and removed rather than made fresh each time, so an evening\n   * of opening and closing the wheel does not leave a stack of them behind.\n   */\n  function onWheelAway(e) {\n    if (!state.root) return document.removeEventListener('pointerdown', onWheelAway, true);\n    if (e.target.closest('.pf-chapwrap')) return;\n    openWheel(false);\n  }\n\n  function openWheel(show) {\n    $('.pf-wheel').hidden = !show;\n    $('.pf-chapbtn').setAttribute('aria-expanded', String(!!show));\n    document.removeEventListener('pointerdown', onWheelAway, true);\n    if (!show) return;\n    // Offsets are all zero while the element is hidden, so the scroll position\n    // can only be set once it is on screen.\n    centreOn(state.wheelIndex);\n    document.addEventListener('pointerdown', onWheelAway, true);\n  }\n\n  /**\n   * Keys while the wheel is open, which take precedence over the reader's own:\n   * up and down are how you search a wheel, and turning the page underneath\n   * instead would be the opposite of what was asked for.\n   */\n  function onWheelKey(e) {\n    const rows = wheelRows();\n    if (!rows.length) return false;\n    const i = centreIndex();\n    const go = (n) => centreOn(Math.max(0, Math.min(rows.length - 1, n)), true);\n    switch (e.key) {\n      case 'ArrowDown': go(i + 1); break;\n      case 'ArrowUp': go(i - 1); break;\n      case 'PageDown': go(i + 5); break;\n      case 'PageUp': go(i - 5); break;\n      case 'Home': go(0); break;\n      case 'End': go(rows.length - 1); break;\n      case 'Enter': pickChapter(rows[i]?.dataset.url); break;\n      case 'Escape': openWheel(false); break;\n      default: return false;\n    }\n    e.preventDefault();\n    e.stopPropagation();\n    return true;\n  }\n\n  /** A row of the wheel, chosen. Landing on the chapter already open is not a\n   *  navigation — reloading the page to stay where you are loses your place. */\n  function pickChapter(url) {\n    if (!url) return;\n    if (isHere(url)) return openWheel(false);\n    gotoChapter(url);\n  }\n\n  function gotoChapter(url) {\n    if (!url || url === location.href) return;\n    saveProgress.flush?.();\n    // Remember to reopen the reader on the next page (same-tab navigation).\n    chrome.storage.local.set({ reopenReaderFor: url }, () => { location.href = url; });\n  }\n\n  // --- what one series remembers for itself ---------------------------------\n\n  /** The overridable half of a series record, and never anything else. */\n  function seriesPick(rec) {\n    const out = {};\n    if (!rec) return out;\n    for (const key of SERIES_KEYS) if (key in rec) out[key] = rec[key];\n    return out;\n  }\n\n  /** The mode and the widths as they stand, in the shape a record is stored in. */\n  function seriesSnapshot() {\n    const rec = { mode: state.mode };\n    for (const key of SERIES_KEYS) rec[key] = state.prefs[key];\n    return rec;\n  }\n\n  /**\n   * Write this series' record, or remove it, and prune the oldest.\n   *\n   * Keyed on sourceUrl and not on the chapter: the whole point is that it\n   * survives to the next chapter. A page the reader arrived at without a series\n   * behind it has nothing to key on and simply does not get to remember —\n   * silently, because there is no decision to report.\n   */\n  function saveSeriesPrefs() {\n    const key = state.meta?.sourceUrl;\n    if (!key) return;\n    const all = state.seriesAll || {};\n    if (state.seriesPrefs) all[key] = { ...state.seriesPrefs, at: Date.now() };\n    else delete all[key];\n    const keys = Object.keys(all);\n    if (keys.length > SERIES_LIMIT) {\n      keys.sort((a, b) => (all[a].at || 0) - (all[b].at || 0));\n      for (const k of keys.slice(0, keys.length - SERIES_LIMIT)) delete all[k];\n    }\n    state.seriesAll = all;\n    chrome.storage.local.set({ readerSeries: all });\n  }\n\n  /**\n   * The switch itself. On writes what is on screen now; off removes the record\n   * and puts this chapter back on the global settings *at once* — a switch\n   * whose effect only shows next time is a switch you cannot tell you pressed.\n   */\n  function toggleSeriesPrefs(on) {\n    if (on) {\n      state.seriesPrefs = seriesSnapshot();\n      saveSeriesPrefs();\n      return flash(t('readerSeriesOn'));\n    }\n    state.seriesPrefs = null;\n    saveSeriesPrefs();\n    chrome.storage.local.get(['readerMode', 'readerPrefs'], (v) => {\n      // The reader can be closed while storage is answering, and everything\n      // below reaches into a DOM that would no longer be there.\n      if (!state.root) return;\n      state.globalPrefs = { ...DEFAULT_PREFS, ...(v.readerPrefs || {}) };\n      state.prefs = { ...state.globalPrefs };\n      syncPrefsInputs();\n      applyPrefs();\n      const mode = state.novel ? 'vertical' : knownMode(v.readerMode);\n      if (mode !== state.mode) {\n        state.mode = mode;\n        $('.pf-mode').value = mode;\n        stopAutoplay();\n        render();\n      }\n      flash(t('readerSeriesOff'));\n    });\n  }\n\n  /** Push `state.prefs` back onto the controls. Their kinds, again, from below. */\n  function syncPrefsInputs() {\n    for (const input of state.root.querySelectorAll('[data-pref]')) {\n      const key = input.dataset.pref;\n      if (input.type === 'checkbox') input.checked = !!state.prefs[key];\n      else input.value = state.prefs[key];\n    }\n  }\n\n  function buildPrefsPanel() {\n    for (const input of state.root.querySelectorAll('[data-pref]')) {\n      const key = input.dataset.pref;\n      // Three kinds of control, and the value has to come back the way it went\n      // in: parseInt on a select's value is NaN, which storage keeps happily and\n      // the next open reads back as a broken preference.\n      const kind = input.tagName === 'SELECT' ? 'select' : input.type;\n      if (kind === 'checkbox') input.checked = !!state.prefs[key];\n      else input.value = state.prefs[key];\n      input.addEventListener('input', () => {\n        state.prefs[key] = kind === 'checkbox' ? input.checked\n          : kind === 'select' ? input.value\n          : parseInt(input.value, 10);\n        // Where it lands. A width kept for a webtoon must not become the width\n        // every tankōbon opens at afterwards, and the settings are written from\n        // `globalPrefs` rather than from `prefs` for exactly that reason: `prefs`\n        // has the override mixed into it, and storing it would launder the\n        // override into the defaults on the next brightness drag.\n        if (state.seriesPrefs && SERIES_KEYS.includes(key)) {\n          state.seriesPrefs[key] = state.prefs[key];\n          saveSeriesPrefs();\n        } else {\n          state.globalPrefs[key] = state.prefs[key];\n          chrome.storage.local.set({ readerPrefs: state.globalPrefs });\n        }\n        applyPrefs();\n        // Tap zones are invisible by definition, so changing them shows them.\n        if (key === 'tapZones' || key === 'invertTap') showZoneHint();\n        // Not in applyPrefs: that runs on every slider drag, and rebuilding the\n        // chapter list under an open select is not something to do 60 times a\n        // second for a brightness change.\n        if (key === 'hideRead') fillWheel();\n      });\n    }\n  }\n\n  function applyPrefs() {\n    const stage = $('.pf-stage');\n    stage.style.filter = `brightness(${state.prefs.brightness}%) contrast(${state.prefs.contrast}%)`;\n    stage.style.setProperty('--pf-gap', state.prefs.gap + 'px');\n    stage.style.setProperty('--pf-width', state.prefs.stripWidth + '%');\n    stage.style.setProperty('--pf-font', state.prefs.fontSize + 'px');\n    stage.style.setProperty('--pf-lh', state.prefs.lineHeight / 100);\n    stage.style.setProperty('--pf-textw', state.prefs.textWidth + 'px');\n    // 0 hides the bar entirely — some readers want nothing over the artwork.\n    state.root.style.setProperty('--pf-progress-h', state.prefs.progressSize + 'px');\n    // Off means \"let the system decide\", which is all reader.css can be told\n    // from here: it is on a scan site's origin and cannot read the settings.\n    state.root.classList.toggle('pf-follow-system', !state.prefs.readerDark);\n    // Text reflows when any of the three above change, so what was one screen\n    // is now two and the position the reader is about to save is stale.\n    if (state.novel) measureScreens();\n  }\n\n  /** Controls that only mean something for one kind of chapter. */\n  function syncPrefsRows() {\n    const drop = state.novel ? 'pf-only-strip' : 'pf-only-novel';\n    for (const row of state.root.querySelectorAll('.pf-only-strip, .pf-only-novel')) {\n      row.hidden = row.classList.contains(drop);\n    }\n    // A .cbz of a text chapter is nothing; the text itself is a plain file.\n    const dl = state.root.querySelector('[data-act=\"download\"]');\n    dl.title = state.novel ? t('readerDownloadTxt') : t('readerDownloadCbz');\n  }\n\n  // --- full screen -----------------------------------------------------------\n\n  function toggleFullscreen() {\n    if (document.fullscreenElement) document.exitFullscreen().catch(() => {});\n    else state.root.requestFullscreen?.().catch(() => {});\n  }\n\n  function syncFullscreenIcon() {\n    const btn = state.root?.querySelector('[data-act=\"fullscreen\"]');\n    if (!btn) return;\n    const on = !!document.fullscreenElement;\n    btn.textContent = on ? '⛶' : '⛶';\n    btn.classList.toggle('pf-on', on);\n  }\n\n  // --- progress bar ----------------------------------------------------------\n  // Deliberately outside .pf-chrome: it stays visible with the controls hidden,\n  // so the reader always knows how far along the chapter it is.\n\n  function updateProgress(ratio) {\n    const fill = state.root?.querySelector('.pf-progress-fill');\n    if (!fill) return;\n    if (ratio === undefined) {\n      ratio = state.images.length > 1\n        ? state.page / (state.images.length - 1)\n        : 1;\n    }\n    fill.style.width = `${clamp(ratio, 0, 1) * 100}%`;\n  }\n\n  /**\n   * Every slider and switch in this panel, back to what it shipped as.\n   *\n   * The mode is deliberately not touched. It is the one setting the reader\n   * chose *about this series* — the panel above puts it under a \"remember for\n   * this series\" checkbox — and losing it to a button labelled \"reading\n   * settings\" would be losing something the button did not name.\n   */\n  function resetPrefs() {\n    state.globalPrefs = { ...DEFAULT_PREFS };\n    state.prefs = { ...state.globalPrefs, ...seriesPick(state.seriesPrefs) };\n    chrome.storage.local.set({ readerPrefs: state.globalPrefs });\n    syncPrefsInputs();\n    applyPrefs();\n    render();\n    flash(t('readerResetDefaults'));\n  }\n\n  function togglePrefs() {\n    const p = $('.pf-prefs');\n    p.hidden = !p.hidden;\n  }\n\n  function toggleBreak() {\n    state.breakFirst = !state.breakFirst;\n    $('.pf-break').classList.toggle('pf-on', state.breakFirst);\n    if (isSpread()) showPage(state.page);\n  }\n\n  function setChrome(visible) {\n    state.chromeVisible = visible;\n    state.root.classList.toggle('pf-chrome-hidden', !visible);\n    if (!visible) $('.pf-prefs').hidden = true;\n  }\n\n  // --- transient notices ----------------------------------------------------\n\n  let toastTimer = 0;\n\n  function flash(text, ms = 1600) {\n    const el = state.root?.querySelector('.pf-toast');\n    if (!el) return;\n    el.textContent = text;\n    el.hidden = false;\n    // The class drives the fade, and it only animates from an opacity the\n    // browser has already computed — hence the forced reflow between the two.\n    // A requestAnimationFrame would read better and never run in a background\n    // tab, which is exactly where a chapter opened in a second tab lives.\n    void el.offsetWidth;\n    el.classList.add('pf-on');\n    clearTimeout(toastTimer);\n    toastTimer = setTimeout(() => {\n      el.classList.remove('pf-on');\n      setTimeout(() => { if (state.root) el.hidden = true; }, 250);\n    }, ms);\n  }\n\n  /**\n   * The same toast with a way out of it. Used where the message is about\n   * something the reader can fix, which a line of text they cannot click is\n   * not — and where the fix lives on a page only the worker can open.\n   */\n  function flashAction(text, label, run, ms = 10000) {\n    const el = state.root?.querySelector('.pf-toast');\n    if (!el) return;\n    // After flash(), not before: it writes the text, and writing textContent\n    // takes any button already there with it.\n    flash(text, ms);\n    const btn = document.createElement('button');\n    btn.className = 'pf-toastbtn';\n    btn.textContent = label;\n    btn.addEventListener('click', () => {\n      clearTimeout(toastTimer);\n      el.classList.remove('pf-on');\n      el.hidden = true;\n      run();\n    });\n    el.appendChild(btn);\n  }\n\n  /** The fraction of the width, on each side, that turns the page. */\n  function tapTurnWidth() {\n    const layout = TAP_LAYOUTS[state.prefs.tapZones];\n    return layout === undefined ? TAP_LAYOUTS.sides : layout;\n  }\n\n  /**\n   * True when tapping the right-hand side moves forward.\n   *\n   * Right is forward, unless the reader says otherwise. There used to be a\n   * second answer to this — the mode: two of the five were right-to-left and\n   * flipped it — and those two modes are gone. What they were for is this\n   * preference, which is the same answer said once instead of twice: somebody\n   * who reads manga and expects the right edge to advance sets it and it holds\n   * in every mode.\n   */\n  function tapForwardRight() {\n    return !state.prefs.invertTap;\n  }\n\n  let zoneTimer = 0;\n\n  function showZoneHint(ms = 1400) {\n    const box = state.root?.querySelector('.pf-zones');\n    if (!box) return;\n    const turn = tapTurnWidth();\n    const fwd = tapForwardRight();\n    const zone = (left, width, label) => {\n      const el = document.createElement('div');\n      el.className = 'pf-zone';\n      el.style.left = `${left * 100}%`;\n      el.style.width = `${width * 100}%`;\n      el.textContent = label;\n      return el;\n    };\n    box.textContent = '';\n    if (turn) {\n      // The arrows stay outside the message: they point at the edge of the\n      // screen the zone is on, which is a fact about the layout and not about\n      // the language.\n      box.appendChild(zone(0, turn, fwd ? `← ${t('zoneBack')}` : `${t('zoneNext')} →`));\n      box.appendChild(zone(turn, 1 - turn * 2, t('zoneControls')));\n      box.appendChild(zone(1 - turn, turn, fwd ? `${t('zoneNext')} →` : `← ${t('zoneBack')}`));\n    } else {\n      box.appendChild(zone(0, 1, t('zoneControlsKeys')));\n    }\n    box.hidden = false;\n    void box.offsetWidth;   // same reason as flash(): fade from a known opacity\n    box.classList.add('pf-on');\n    clearTimeout(zoneTimer);\n    zoneTimer = setTimeout(() => {\n      box.classList.remove('pf-on');\n      setTimeout(() => { if (state.root) box.hidden = true; }, 250);\n    }, ms);\n  }\n\n  function showHelp(show) {\n    const help = state.root?.querySelector('.pf-help');\n    if (!help) return;\n    help.hidden = !show;\n    // The controls are what the list talks about; hiding them under it makes\n    // half of it unverifiable.\n    if (show) setChrome(true);\n  }\n\n  // --- rendering -----------------------------------------------------------\n\n  function render() {\n    // A fresh element, not an emptied one. Both mode renderers attach their\n    // handlers as closures — the pan/pinch pair and the scroll pair are new\n    // functions every call, so addEventListener cannot deduplicate them.\n    // Re-rendering onto the same node stacked a second copy of each: after two\n    // mode switches a one-finger drag moved the page twice as far as the\n    // finger, and every scroll saved progress twice.\n    const stage = document.createElement('div');\n    $('.pf-stage').replaceWith(stage);\n    resetTransform();\n    if (state.novel) renderNovel(stage);\n    else if (state.mode === 'vertical') renderVertical(stage);\n    else renderPaged(stage);\n    applyPrefs();\n    $('.pf-play').hidden = state.mode !== 'vertical';\n    $('.pf-break').hidden = !isSpread();\n    // The scrubber ran backwards in the two right-to-left modes. There are\n    // none, so it never does.\n    $('.pf-scrub').classList.remove('pf-rtl');\n    updateCounter();\n    preload();\n    // render() runs on open and on every mode change, which is exactly when the\n    // direction is news. The help list already says it, so do not say it twice.\n    if ($('.pf-help').hidden) {\n      flash(state.novel ? t('modeToastNovel') : (modeToast(state.mode) ? t(modeToast(state.mode)) : ''));\n    }\n  }\n\n  function renderVertical(stage) {\n    stage.className = 'pf-stage pf-vertical';\n    for (const src of state.images) {\n      const img = document.createElement('img');\n      img.loading = 'lazy';\n      img.src = src;\n      stage.appendChild(img);\n    }\n    attachStripScroll(stage);\n    // Keep the reading position across mode switches.\n    if (state.page > 0) {\n      scrollToRatio(stage, state.page / Math.max(1, state.images.length - 1));\n    }\n  }\n\n  // Prose. The page's own markup never enters the overlay — every paragraph is\n  // set as text, so a chapter body carrying scripts, styles, iframes or an ad\n  // slot arrives here as the words it was supposed to be.\n  function renderNovel(stage) {\n    stage.className = 'pf-stage pf-vertical pf-novel';\n    const article = document.createElement('article');\n    article.className = 'pf-text';\n    for (const para of state.paragraphs) {\n      const p = document.createElement('p');\n      p.textContent = para;\n      article.appendChild(p);\n    }\n    stage.appendChild(article);\n    measureScreens();\n    attachStripScroll(stage);\n    if (state.scrollRatio > 0) scrollToRatio(stage, state.scrollRatio);\n  }\n\n  // A novel has no pages, so a screenful is the unit: it is what the scrubber\n  // steps through and what \"how far in\" is counted in.\n  function measureScreens() {\n    const stage = state.root?.querySelector('.pf-stage');\n    if (!stage || !state.novel) return;\n    state.screens = Math.max(1, Math.round(stage.scrollHeight / Math.max(1, stage.clientHeight)));\n    const scrub = state.root.querySelector('.pf-scrub');\n    if (scrub) scrub.max = state.screens;\n  }\n\n  function attachStripScroll(stage) {\n    const ratio = () =>\n      stage.scrollTop / Math.max(1, stage.scrollHeight - stage.clientHeight);\n    // The bar tracks the scroll live; the (costlier) counter and progress save\n    // stay debounced.\n    stage.addEventListener('scroll', () => {\n      state.scrollRatio = ratio();\n      updateProgress(state.scrollRatio);\n    }, { passive: true });\n    stage.addEventListener('scroll', debounce(() => {\n      state.scrollRatio = ratio();\n      state.page = Math.round(state.scrollRatio * (pageTotal() - 1));\n      updateCounter();\n      updateProgress(state.scrollRatio);\n      saveProgress();\n      // The bottom of a long strip is how a webtoon chapter ends, and it was\n      // the one ending the reader had no answer for: `endOfChapter` was reached\n      // from `step()` only, so \"auto next chapter\" never fired in the mode most\n      // people read in. A crossing and not a state — sitting at the bottom must\n      // not re-fire on every scroll event the rubber band produces.\n      const room = stage.scrollHeight - stage.clientHeight;\n      const atEnd = room > 0 && room - stage.scrollTop < 4;\n      if (atEnd && !state.atEnd) endOfChapter();\n      if (!atEnd && state.atEnd) showEnd(false);\n      state.atEnd = atEnd;\n    }, 500));\n    stage.addEventListener('click', (e) => {\n      // Selecting a line of prose ends in a click on it, and hiding the\n      // controls under someone who was copying a quote is not what they asked.\n      if (String(getSelection?.() || '')) return;\n      if (e.target === stage || e.target.tagName === 'IMG' || e.target.closest('.pf-text')) {\n        setChrome(!state.chromeVisible);\n      }\n    });\n    // Any manual interaction pauses autoplay.\n    stage.addEventListener('wheel', stopAutoplay, { passive: true });\n    stage.addEventListener('pointerdown', stopAutoplay);\n  }\n\n  // Images size in asynchronously, so a strip that has not laid out yet has no\n  // height to scroll into and every seek silently lands on page 1. Retry until\n  // it has one, and stop if the reader closed or re-rendered underneath.\n  function scrollToRatio(stage, ratio) {\n    let tries = 0;\n    const attempt = () => {\n      if (!state.root || $('.pf-stage') !== stage) return;\n      const max = stage.scrollHeight - stage.clientHeight;\n      if (max > 0) stage.scrollTop = ratio * max;\n      else if (++tries < 20) setTimeout(attempt, 150);\n    };\n    setTimeout(attempt, 50);\n  }\n\n  function renderPaged(stage) {\n    stage.className = 'pf-stage pf-paged';\n    const wrap = document.createElement('div');\n    wrap.className = 'pf-zoomwrap';\n    stage.appendChild(wrap);\n    attachZoomPan(stage, wrap);\n    stage.addEventListener('click', onTapZones);\n    showPage(state.page, wrap);\n  }\n\n  // Spread pairing honours \"break 1st page\": cover pages stand alone so the\n  // following spreads align the way the book was printed (MangaPin's B key).\n  function spreadIndices(n) {\n    if (!state.breakFirst) return [n, n + 1];\n    if (n === 0) return [0];\n    return [n, n + 1];\n  }\n\n  function pageStart(n) {\n    // Align to spread boundaries so stepping lands on pair starts.\n    if (!isSpread()) return n;\n    if (!state.breakFirst) return n - (n % 2);\n    return n === 0 ? 0 : n - ((n - 1) % 2);\n  }\n\n  function showPage(n, wrap = $('.pf-zoomwrap')) {\n    // Any move within the chapter means the reader is not at the end of it any\n    // more, whether they got here by a tap, a key or the scrubber.\n    showEnd(false);\n    const spread = isSpread();\n    n = clamp(pageStart(n), 0, state.images.length - 1);\n    state.page = n;\n    wrap.innerHTML = '';\n    const indices = (spread ? spreadIndices(n) : [n]).filter((i) => i < state.images.length);\n    // Left to right, always: the mode that drew a spread backwards is gone.\n    const ordered = indices;\n    for (const i of ordered) {\n      const img = document.createElement('img');\n      img.src = state.images[i];\n      wrap.appendChild(img);\n    }\n    // Deliberately DO NOT reset zoom/pan here: the user's framing survives\n    // page turns (the anti-snap-back differentiator). Zoom persists; only\n    // an explicit double-tap resets it.\n    applyTransform();\n    updateCounter();\n    preload();\n    saveProgress();\n  }\n\n  function step(delta) {\n    const stride = isSpread() ? spreadIndices(state.page).length : 1;\n    const target = state.page + delta * stride;\n    if (target > state.images.length - 1) return endOfChapter();\n    showPage(target);\n  }\n\n  function next() { step(1); }\n  function prev() { step(-1); }\n\n  /**\n   * The next chapter, preferring the site's own link.\n   *\n   * The merged list is the fallback and not the first answer: a derived URL is\n   * a very good guess, the site's link is the address the site publishes. The\n   * list runs newest first, so the chapter after this one is the row *before* it.\n   */\n  function nextChapterUrl() {\n    if (state.nav?.nextUrl) return state.nav.nextUrl;\n    const i = hereIndex();\n    return i > 0 ? state.chapters[i - 1]?.url || null : null;\n  }\n\n  /** Where this chapter sits in the merged list, or -1 if it is not in it. */\n  const hereIndex = () => state.chapters.findIndex((c) => isHere(c.url));\n\n  /**\n   * Reaching the end.\n   *\n   * \"Auto next chapter\" still wins, and wins first: someone who asked to be\n   * carried into the next chapter did not ask to be stopped by a panel on the\n   * way. Everyone else gets the panel, which exists because the alternative is\n   * what PanelFlow did until now — hand the reader back to the scan site at the\n   * exact moment they were deciding whether to read another one.\n   */\n  function endOfChapter() {\n    const next = nextChapterUrl();\n    if (state.prefs.autoNext && next) return gotoChapter(next);\n    showEnd(true);\n  }\n\n  /**\n   * The panel. Navigation and nothing else — no rating prompt, no tracker\n   * nudge, no \"turn on notifications\". It appears at the one moment the reader\n   * is most willing to say yes to something, and that is precisely why it is\n   * not allowed to ask for anything.\n   *\n   * Not part of `.pf-chrome`: like the help list, it has to survive the\n   * controls being hidden, because hiding them is how most people read.\n   */\n  function showEnd(show) {\n    const panel = state.root?.querySelector('.pf-end');\n    if (!panel) return;\n    if (!show) {\n      panel.classList.remove('pf-on');\n      panel.hidden = true;\n      return;\n    }\n    const url = state.meta.chapterUrl || location.href;\n    panel.querySelector('.pf-end-title').textContent =\n      t('readerEndOf', [state.meta.chapterLabel || t('readerEndThis')]);\n    panel.querySelector('.pf-end-left').textContent = chaptersLeftText();\n\n    const go = panel.querySelector('[data-act=\"end-next\"]');\n    const next = nextChapterUrl();\n    go.hidden = !next;\n    go.textContent = t('readerEndNext');\n\n    // Already in the history — because it was read, or because this button was\n    // pressed once already. Either way there is nothing left to claim.\n    const mark = panel.querySelector('[data-act=\"end-read\"]');\n    const done = !!state.readChapters?.has(url);\n    mark.disabled = done;\n    mark.textContent = t(done ? 'readerEndMarked' : 'readerEndMarkRead');\n\n    panel.hidden = false;\n    // The fade lives on a class rather than on [hidden]: an element that is\n    // display:none one frame and opaque the next has nothing to fade from.\n    requestAnimationFrame(() => state.root && panel.classList.add('pf-on'));\n  }\n\n  /**\n   * How much of this series is left, in words.\n   *\n   * \"In this list\" and not \"in this series\", because the list is what is known:\n   * it stops at the last chapter the library has seen, and claiming a total the\n   * reader could disprove by visiting the site would be worse than saying less.\n   */\n  function chaptersLeftText() {\n    const i = hereIndex();\n    if (i === -1 || state.chapters.length < 2) return '';\n    if (i === 0) return t('readerEndCaughtUp');\n    return t(i === 1 ? 'readerEndLeftOne' : 'readerEndLeftMany', [String(i)]);\n  }\n\n  /**\n   * \"I have read this\", said by hand.\n   *\n   * Written as a history row, because that is already what \"read\" means here —\n   * the wheel greys a row when the history has one for it, and a second flag\n   * meaning the same thing would be a second answer to drift from the first.\n   * Pages and no seconds: the chapter may have been skimmed in four, and\n   * banking four seconds of reading time would put a lie in the statistics.\n   */\n  function markChapterRead() {\n    const url = state.meta.chapterUrl || location.href;\n    chrome.runtime.sendMessage({ type: 'recordRead', read: {\n      sourceUrl: state.meta.sourceUrl,\n      chapterUrl: url,\n      chapterLabel: state.meta.chapterLabel,\n      pages: pageTotal(),\n      seconds: 0,\n      day: clock.day || localDay(),\n    }});\n    // And the position, so the shelf agrees with the wheel: a chapter marked\n    // read that still resumes on page 3 is two answers to one question.\n    state.page = Math.max(0, pageTotal() - 1);\n    state.scrollRatio = 1;\n    saveProgress();\n    saveProgress.flush?.();\n    state.readChapters?.add(url);\n    fillWheel();\n    showEnd(true);\n  }\n\n  function onTapZones(e) {\n    if (e.target.closest('.pf-chrome')) return;\n    if (e.target.closest('.pf-help')) return;\n    if (e.target.closest('.pf-end')) return;\n    if (suppressTapUntil > Date.now()) return; // ignore tap that ended a pan\n    const turn = tapTurnWidth();\n    const x = e.clientX / innerWidth;\n    // The mode decides which side is forward; the preference only swaps it, so\n    // a manga reader who prefers \"right = next\" keeps it across both directions.\n    const fwd = tapForwardRight();\n    if (turn && x < turn) return fwd ? prev() : next();\n    if (turn && x > 1 - turn) return fwd ? next() : prev();\n    setChrome(!state.chromeVisible);\n  }\n\n  /**\n   * Whether this key is being typed into something, rather than pressed at the\n   * reader. The shortcuts below cancel the keys they claim — `s`, `b`, `f`,\n   * `h`, `c`, `0` and `?` among them — and a cancelled keydown never becomes a\n   * character, so a tag typed into the library sheet came out missing half its\n   * letters.\n   *\n   * The sheet is a closed shadow root, which is why its host is what gets\n   * checked: from this document every key pressed anywhere inside it is\n   * reported on `#panelflow-libmodal` and the field itself is unreachable.\n   */\n  function typingInto(el) {\n    if (!el || el.nodeType !== 1) return false;\n    if (el.id === 'panelflow-libmodal') return true;\n    if (el.isContentEditable) return true;\n    return el.tagName === 'INPUT' || el.tagName === 'TEXTAREA' || el.tagName === 'SELECT';\n  }\n\n  function onKey(e) {\n    if (!state.root) return;\n    if (typingInto(e.target)) {\n      // Escape is the one key a field does not own: it is how you leave the\n      // panel the field is in, and the reader's own panels have no other way\n      // out from the keyboard. The library sheet is the exception — it has its\n      // own Escape, and closing the reader out from under it would take the\n      // half-typed tag with it.\n      if (e.key !== 'Escape' || e.target.id === 'panelflow-libmodal') return;\n    }\n    // The wheel is on top and gets first refusal: while it is open, up and down\n    // search the chapter list rather than turning pages behind it.\n    if (!$('.pf-wheel').hidden && onWheelKey(e)) return;\n    if (e.key === 'c' || e.key === 'C') {\n      e.preventDefault();\n      if (!$('.pf-chapwrap').hidden) return openWheel($('.pf-wheel').hidden);\n    }\n    if (e.key === 'Escape') {\n      e.preventDefault();\n      // Esc dismisses what is on top first. Closing the reader out from under\n      // someone who only wanted the help list gone loses their place.\n      if (!$('.pf-help').hidden) return showHelp(false);\n      if (!$('.pf-end').hidden) return showEnd(false);\n      return close();\n    }\n    if (e.key === '?') { e.preventDefault(); return showHelp($('.pf-help').hidden); }\n    if (e.key === 's' || e.key === 'S') { e.preventDefault(); return togglePrefs(); }\n    if (e.key === 'b' || e.key === 'B') { e.preventDefault(); return toggleBreak(); }\n    if (e.key === 'f' || e.key === 'F') { e.preventDefault(); return toggleFullscreen(); }\n    if (e.key === 'h' || e.key === 'H') { e.preventDefault(); return setChrome(!state.chromeVisible); }\n    if (e.key === '0') { e.preventDefault(); resetTransform(); return applyTransform(); }\n    if (state.mode === 'vertical') {\n      const stage = $('.pf-stage');\n      if (e.key === 'ArrowDown' || e.key === ' ') {\n        e.preventDefault(); stopAutoplay();\n        stage.scrollBy({ top: innerHeight * 0.8, behavior: 'smooth' });\n      }\n      if (e.key === 'ArrowUp') {\n        e.preventDefault(); stopAutoplay();\n        stage.scrollBy({ top: -innerHeight * 0.8, behavior: 'smooth' });\n      }\n      return;\n    }\n    if (e.key === 'ArrowRight') { e.preventDefault(); next(); }\n    if (e.key === 'ArrowLeft') { e.preventDefault(); prev(); }\n    if (e.key === ' ') { e.preventDefault(); next(); }\n  }\n\n  function updateCounter() {\n    // \"Page 3 of 14\" means nothing for prose, where the pages are screenfuls of\n    // whatever text size happens to be set. How far in you are does mean\n    // something, and it is the number a novel reader looks for.\n    $('.pf-counter').textContent =\n      (state.novel\n        ? `${Math.round(state.scrollRatio * 100)}%`\n        : `${state.page + 1} / ${state.images.length}`) +\n      (state.meta.chapterLabel ? ` — ${state.meta.chapterLabel}` : '');\n    const scrub = $('.pf-scrub');\n    if (scrub) scrub.value = state.page + 1;\n    updateProgress();\n  }\n\n  function preload() {\n    for (let i = state.page + 1; i <= state.page + PRELOAD_AHEAD && i < state.images.length; i++) {\n      const im = new Image();\n      im.src = state.images[i];\n    }\n  }\n\n  // --- lazy page harvesting ------------------------------------------------\n  // Most readers load pages as you scroll, so at open time only the first few\n  // exist. Scroll the (hidden) page behind the overlay to make it load\n  // everything, and append each new image to the reader live. This is how a\n  // generic reader gets the full chapter without MangaPin's per-site specs.\n\n  function harvestLazyPages() {\n    const container = state.container;\n    if (!container || !document.contains(container)) return;\n    const seen = new Set(state.images);\n    // The detector's two answers, not a second copy of them.\n    //\n    // This read `src` and measured what the browser had decoded from it. On a\n    // theme that parks a transparent gif in `src` and keeps the address in\n    // data-src — sushiscan among them — that measured the gif: 1x1, under\n    // every threshold, so each panel arriving behind a spacer was dropped in\n    // silence. detect.js was fixed for exactly this and the reader was not,\n    // which is the same chapter coming up short by another door. The fallbacks\n    // are the old readings, for a reader running without the detector beside\n    // it; on all four clients it is loaded first.\n    const detect = () => window.__panelflowDetect;\n    const address = (img) => detect()?.lazySrc?.(img) ?? (img.currentSrc || img.src);\n    const isPanel = (img) => (detect()?.sizedImage\n      ? detect().sizedImage(img)\n      : (img.naturalWidth || img.width) >= 300 && (img.naturalHeight || img.height) >= 200);\n    const track = (img) => {\n      const add = async () => {\n        const src = address(img);\n        if (!src || seen.has(src)) return;\n        if (!isPanel(img)) return;\n        seen.add(src);\n        // Snapshot blob: URLs before the site revokes them (scan-manga does).\n        const stab = window.__panelflowDetect?.stableImageSrc;\n        const finalSrc = stab ? await stab(img) : src;\n        if (!state.root) return; // reader closed while snapshotting\n        state.images.push(finalSrc);\n        onImagesGrown(finalSrc);\n      };\n      if (img.complete && img.naturalWidth) add();\n      else img.addEventListener('load', add, { once: true });\n    };\n    state.harvestObserver = new MutationObserver((muts) => {\n      for (const m of muts) {\n        if (m.type === 'attributes' && m.target.tagName === 'IMG') track(m.target);\n        for (const n of m.addedNodes || []) {\n          if (n.tagName === 'IMG') track(n);\n          else if (n.querySelectorAll) n.querySelectorAll('img').forEach(track);\n        }\n      }\n    });\n    state.harvestObserver.observe(container, {\n      childList: true, subtree: true, attributes: true, attributeFilter: ['src'],\n    });\n\n    // Drive the page's lazy loading: scroll the document under the overlay.\n    //\n    // It stops when it stops producing, wherever it has got to — not only at\n    // the bottom. A page that already holds the whole chapter has nothing left\n    // to give, and a sushiscan volume is 188 panels, every one of them in the\n    // DOM before the reader opens: the old rule still walked the document to\n    // the end of a quarter of a million pixels, nearly two minutes of the page\n    // moving under the overlay for no new panel and the scroll lock off the\n    // whole time. Twelve ticks is ~5s and ~10,000px with nothing arriving,\n    // which is well past any lazy loader's threshold.\n    document.documentElement.classList.remove('panelflow-noscroll');\n    const IDLE_TICKS = 12;\n    const startY = scrollY;\n    let y = startY, idleTicks = 0, bottomTicks = 0, lastCount = state.images.length;\n    state.harvestTimer = setInterval(() => {\n      if (!state.root) return stopHarvest();\n      y += innerHeight * 0.9;\n      window.scrollTo(0, y);\n      const doc = document.documentElement;\n      if (y >= doc.scrollHeight - innerHeight) bottomTicks++;\n      if (state.images.length > lastCount) { lastCount = state.images.length; idleTicks = 0; }\n      else idleTicks++;\n      if (idleTicks > IDLE_TICKS || bottomTicks > 5) stopHarvest(startY);\n    }, 400);\n    state.harvestRestoreY = startY;\n  }\n\n  function stopHarvest(restoreY = state.harvestRestoreY) {\n    if (state.harvestTimer) { clearInterval(state.harvestTimer); state.harvestTimer = 0; }\n    if (state.harvestObserver) { state.harvestObserver.disconnect(); state.harvestObserver = null; }\n    if (restoreY !== undefined) window.scrollTo(0, restoreY);\n    if (state.root) document.documentElement.classList.add('panelflow-noscroll');\n  }\n\n  function onImagesGrown(src) {\n    if (!state.root) return;\n    $('.pf-scrub').max = state.images.length;\n    if (state.mode === 'vertical') {\n      const img = document.createElement('img');\n      img.loading = 'lazy';\n      img.src = src;\n      $('.pf-stage').appendChild(img);\n    }\n    updateCounter();\n  }\n\n  // --- autoplay (long strip) ----------------------------------------------\n\n  function toggleAutoplay() {\n    state.playing ? stopAutoplay() : startAutoplay();\n  }\n\n  function startAutoplay() {\n    if (state.mode !== 'vertical' || state.playing) return;\n    state.playing = true;\n    $('.pf-play').textContent = '⏸';\n    state.playLastTs = 0;\n    const stage = $('.pf-stage');\n    // Track position as a float: incremental += gets truncated to whole\n    // physical pixels, so slow speeds would never move at all.\n    let pos = stage.scrollTop;\n    const tick = (ts) => {\n      if (!state.playing || !state.root) return;\n      if (state.playLastTs) {\n        pos += (state.prefs.autoplaySpeed * (ts - state.playLastTs)) / 1000;\n        stage.scrollTop = pos;\n        if (stage.scrollTop + stage.clientHeight >= stage.scrollHeight - 2) {\n          stopAutoplay();\n          return endOfChapter();\n        }\n      }\n      state.playLastTs = ts;\n      state.playRaf = requestAnimationFrame(tick);\n    };\n    state.playRaf = requestAnimationFrame(tick);\n  }\n\n  function stopAutoplay() {\n    if (!state.playing) return;\n    state.playing = false;\n    cancelAnimationFrame(state.playRaf);\n    const btn = state.root?.querySelector('.pf-play');\n    if (btn) btn.textContent = '▶';\n  }\n\n  // --- zoom & pan (no snap-back) ------------------------------------------\n\n  let suppressTapUntil = 0;\n\n  function resetTransform() {\n    state.zoom = 1; state.panX = 0; state.panY = 0;\n  }\n\n  function applyTransform() {\n    const wrap = $('.pf-zoomwrap');\n    if (wrap) wrap.style.transform =\n      `translate(${state.panX}px, ${state.panY}px) scale(${state.zoom})`;\n    // The reset button only earns its slot once there is something to reset.\n    const btn = state.root?.querySelector('.pf-resetzoom');\n    if (btn) btn.hidden = state.zoom <= 1;\n  }\n\n  // Clamp so at least MIN_VISIBLE_FRACTION of the content stays in view —\n  // the page can be pushed mostly off-screen but never lost entirely, and it\n  // is never recentered behind the user's back.\n  function clampPan() {\n    const wrap = $('.pf-zoomwrap');\n    if (!wrap) return;\n    const w = wrap.offsetWidth * state.zoom;\n    const h = wrap.offsetHeight * state.zoom;\n    const keepX = Math.max(innerWidth, w) * MIN_VISIBLE_FRACTION;\n    const keepY = Math.max(innerHeight, h) * MIN_VISIBLE_FRACTION;\n    state.panX = clamp(state.panX, -(w - keepX), innerWidth - keepX);\n    state.panY = clamp(state.panY, -(h - keepY), innerHeight - keepY);\n  }\n\n  function attachZoomPan(stage, wrap) {\n    const pointers = new Map();\n    let lastDist = 0, lastMid = null, panning = false, lastTap = 0;\n\n    stage.addEventListener('wheel', (e) => {\n      if (!e.ctrlKey && state.mode === 'vertical') return;\n      e.preventDefault();\n      const factor = e.deltaY < 0 ? 1.1 : 1 / 1.1;\n      zoomAt(e.clientX, e.clientY, factor);\n    }, { passive: false });\n\n    stage.addEventListener('pointerdown', (e) => {\n      pointers.set(e.pointerId, { x: e.clientX, y: e.clientY });\n      if (pointers.size === 1) {\n        // Double tap: zoom into the tapped point, or reset if already zoomed\n        // (MangaPin's \"double tap to zoom\"). The only ways the view recenters.\n        if (Date.now() - lastTap < 300) {\n          if (state.zoom > 1) resetTransform();\n          else { state.zoom = 1; zoomAt(e.clientX, e.clientY, 2.5); suppressTapUntil = Date.now() + 250; }\n          applyTransform();\n        }\n        lastTap = Date.now();\n      }\n      if (pointers.size === 2) {\n        const [a, b] = [...pointers.values()];\n        lastDist = Math.hypot(a.x - b.x, a.y - b.y);\n        lastMid = { x: (a.x + b.x) / 2, y: (a.y + b.y) / 2 };\n      }\n      stage.setPointerCapture(e.pointerId);\n    });\n\n    stage.addEventListener('pointermove', (e) => {\n      const p = pointers.get(e.pointerId);\n      if (!p) return;\n      const dx = e.clientX - p.x, dy = e.clientY - p.y;\n      pointers.set(e.pointerId, { x: e.clientX, y: e.clientY });\n\n      if (pointers.size === 2) {\n        const [a, b] = [...pointers.values()];\n        const dist = Math.hypot(a.x - b.x, a.y - b.y);\n        const mid = { x: (a.x + b.x) / 2, y: (a.y + b.y) / 2 };\n        if (lastDist > 0) zoomAt(mid.x, mid.y, dist / lastDist);\n        state.panX += mid.x - lastMid.x;\n        state.panY += mid.y - lastMid.y;\n        lastDist = dist; lastMid = mid;\n        applyTransform();\n      } else if (pointers.size === 1 && state.zoom > 1) {\n        if (Math.abs(dx) + Math.abs(dy) > 2) panning = true;\n        state.panX += dx; state.panY += dy;\n        applyTransform();\n      }\n    });\n\n    const endPointer = (e) => {\n      pointers.delete(e.pointerId);\n      lastDist = 0;\n      if (pointers.size === 0) {\n        if (panning) suppressTapUntil = Date.now() + 200;\n        panning = false;\n        // Settle to the nearest legal bound — elastic, not a reset.\n        clampPan();\n        wrap.style.transition = 'transform 120ms ease-out';\n        applyTransform();\n        setTimeout(() => { wrap.style.transition = ''; }, 140);\n      }\n    };\n    stage.addEventListener('pointerup', endPointer);\n    stage.addEventListener('pointercancel', endPointer);\n\n    function zoomAt(cx, cy, factor) {\n      const newZoom = clamp(state.zoom * factor, 1, 8);\n      const applied = newZoom / state.zoom;\n      // Keep the focal point stationary while zooming.\n      state.panX = cx - (cx - state.panX) * applied;\n      state.panY = cy - (cy - state.panY) * applied;\n      state.zoom = newZoom;\n      if (state.zoom === 1) { state.panX = 0; state.panY = 0; }\n      applyTransform();\n    }\n  }\n\n  // --- download ------------------------------------------------------------\n\n  // Built here, not in the worker: sites like scan-manga hand pages out as\n  // blob: URLs that only exist in this document, and same-origin fetches\n  // carry the page's cookies/referer for free. Cross-origin CDN images that\n  // CORS won't let us read fall back to the worker (DNR sets their referer).\n  const CRC_TABLE = (() => {\n    const table = new Uint32Array(256);\n    for (let n = 0; n < 256; n++) {\n      let c = n;\n      for (let k = 0; k < 8; k++) c = c & 1 ? 0xedb88320 ^ (c >>> 1) : c >>> 1;\n      table[n] = c >>> 0;\n    }\n    return table;\n  })();\n\n  function crc32(bytes) {\n    let c = 0xffffffff;\n    for (let i = 0; i < bytes.length; i++) c = CRC_TABLE[(c ^ bytes[i]) & 0xff] ^ (c >>> 8);\n    return (c ^ 0xffffffff) >>> 0;\n  }\n\n  function zipStore(files) { // [{name, bytes}] -> Uint8Array (no compression)\n    const chunks = [], central = [];\n    let offset = 0;\n    const le16 = (v) => [v & 255, (v >> 8) & 255];\n    const le32 = (v) => [v & 255, (v >> 8) & 255, (v >> 16) & 255, (v >>> 24) & 255];\n    for (const { name, bytes } of files) {\n      const nameB = new TextEncoder().encode(name);\n      const crc = crc32(bytes);\n      const head = new Uint8Array([\n        ...le32(0x04034b50), ...le16(20), ...le16(0), ...le16(0), ...le16(0), ...le16(0),\n        ...le32(crc), ...le32(bytes.length), ...le32(bytes.length), ...le16(nameB.length), ...le16(0),\n      ]);\n      chunks.push(head, nameB, bytes);\n      central.push(new Uint8Array([\n        ...le32(0x02014b50), ...le16(20), ...le16(20), ...le16(0), ...le16(0), ...le16(0), ...le16(0),\n        ...le32(crc), ...le32(bytes.length), ...le32(bytes.length), ...le16(nameB.length),\n        ...le16(0), ...le16(0), ...le16(0), ...le16(0), ...le32(0), ...le32(offset),\n      ]), nameB);\n      offset += head.length + nameB.length + bytes.length;\n    }\n    const centralStart = offset;\n    let centralSize = 0;\n    for (const c of central) centralSize += c.length;\n    const end = new Uint8Array([\n      ...le32(0x06054b50), ...le16(0), ...le16(0), ...le16(files.length), ...le16(files.length),\n      ...le32(centralSize), ...le32(centralStart), ...le16(0),\n    ]);\n    const all = [...chunks, ...central, end];\n    const out = new Uint8Array(all.reduce((s, c) => s + c.length, 0));\n    let pos = 0;\n    for (const c of all) { out.set(c, pos); pos += c.length; }\n    return out;\n  }\n\n  async function fetchPageBytes(src) {\n    try {\n      const resp = await fetch(src, { credentials: 'include' });\n      if (resp.ok) return new Uint8Array(await resp.arrayBuffer());\n    } catch { /* CORS or network — try the worker */ }\n    const resp = await new Promise((r) =>\n      chrome.runtime.sendMessage({ type: 'fetchImage', url: src, siteUrl: location.href }, r));\n    if (!resp?.b64) return null;\n    const bin = atob(resp.b64);\n    const bytes = new Uint8Array(bin.length);\n    for (let i = 0; i < bin.length; i++) bytes[i] = bin.charCodeAt(i);\n    return bytes;\n  }\n\n  /** `Title - Ch. 5` with everything a file name cannot hold taken out. */\n  function chapterFileName(ext) {\n    const safe = (s) => String(s || '').replace(/[<>:\"/\\\\|?*]+/g, ' ').trim().slice(0, 80);\n    return `${safe(state.meta.title) || 'chapter'}` +\n      `${state.meta.chapterLabel ? ' - ' + safe(state.meta.chapterLabel) : ''}.${ext}`;\n  }\n\n  function saveBlob(blob, name) {\n    const url = URL.createObjectURL(blob);\n    const a = document.createElement('a');\n    a.href = url;\n    a.download = name;\n    a.click();\n    setTimeout(() => URL.revokeObjectURL(url), 60000);\n  }\n\n  // What an image actually is, read from its first bytes rather than guessed\n  // from its URL. Half the pages the detector hands over are blob: URLs, which\n  // carry no extension at all — so the URL can only ever be the fallback, and\n  // both the .cbz and the offline store want the same answer from it.\n  function imageType(bytes, src) {\n    const ext =\n      bytes[0] === 0x89 && bytes[1] === 0x50 ? 'png' :\n      bytes[0] === 0xff && bytes[1] === 0xd8 ? 'jpg' :\n      bytes[0] === 0x52 && bytes[1] === 0x49 ? 'webp' :\n      bytes[0] === 0x47 && bytes[1] === 0x49 ? 'gif' :\n      (String(src).match(/\\.(jpe?g|png|webp|gif|avif)(?=$|[?#])/i) || [, 'jpg'])[1].toLowerCase();\n    return { ext, mime: `image/${ext === 'jpg' ? 'jpeg' : ext}` };\n  }\n\n  /**\n   * The hosts this chapter's pages sit on that PanelFlow may not read from —\n   * the worker's answer, since only it can ask chrome.permissions.\n   *\n   * A worker that does not answer at all is taken as \"no objection\": an old\n   * copy of the extension left running after an update should still try, and\n   * fail the way it always did, rather than refuse to start.\n   */\n  async function blockedImageHosts(images) {\n    const r = await send({ type: 'imageAccess', urls: images }).catch(() => null);\n    return r?.missing || [];\n  }\n\n  /**\n   * Name the hosts and offer the only thing that grants them. The permission\n   * has to be asked for from an extension page, so this cannot do more than\n   * open the page that asks — but the reader now knows what went wrong, which\n   * is the whole of what was missing before.\n   */\n  function askForImageAccess(hosts) {\n    flashAction(\n      t('readerNeedsAccess', [hosts.slice(0, 3).join(', ')]),\n      t('readerGrantAccess'),\n      () => send({ type: 'openOptions' }),\n    );\n  }\n\n  async function downloadChapter() {\n    const btn = state.root.querySelector('[data-act=\"download\"]');\n    // A text chapter is already in hand — there is nothing to fetch, and a\n    // .cbz of it would be an archive of nothing.\n    if (state.novel) {\n      const head = [state.meta.title, state.meta.chapterLabel].filter(Boolean).join(' — ');\n      // CRLF: the file lands in Notepad as often as anywhere else.\n      const body = [head, ...state.paragraphs].join('\\r\\n\\r\\n');\n      saveBlob(new Blob([body], { type: 'text/plain;charset=utf-8' }), chapterFileName('txt'));\n      btn.textContent = '✓';\n      setTimeout(() => { if (state.root) btn.textContent = '⬇'; }, 3000);\n      return;\n    }\n    btn.disabled = true;\n    try {\n      const images = state.images.slice();\n      // Before the loop, not during it: forty refusals produce forty identical\n      // failures and a message naming a page number, when the answer has\n      // nothing to do with which page it was.\n      const blocked = await blockedImageHosts(images);\n      if (blocked.length) return askForImageAccess(blocked);\n      const files = [];\n      for (let i = 0; i < images.length; i++) {\n        btn.textContent = `${i + 1}/${images.length}`;\n        const bytes = await fetchPageBytes(images[i]);\n        if (!bytes) continue;\n        const { ext } = imageType(bytes, images[i]);\n        files.push({ name: String(i + 1).padStart(3, '0') + '.' + ext, bytes });\n      }\n      if (!files.length) throw new Error('no pages');\n      saveBlob(\n        new Blob([zipStore(files)], { type: 'application/vnd.comicbook+zip' }),\n        chapterFileName('cbz'),\n      );\n      btn.textContent = '✓';\n    } catch (e) {\n      btn.textContent = '⚠';\n      // A glyph is not a reason. This used to be the whole of what a failed\n      // download said, which left \"the pages could not be fetched\" and \"this\n      // site is broken\" looking exactly alike.\n      flash(t('readerNotDownloaded', [String(e.message)]), 4000);\n    } finally {\n      btn.disabled = false;\n      setTimeout(() => { if (state.root) btn.textContent = '⬇'; }, 3000);\n    }\n  }\n\n  // --- offline ---------------------------------------------------------------\n  // \"Download\" writes a .cbz to the user's disk, which is theirs to keep and\n  // nothing here ever reads again. \"Save offline\" puts the same pages inside\n  // PanelFlow, so the chapter opens with no network. Two different wants, two\n  // buttons.\n  //\n  // The pages cannot be stored from here. A content script runs on the site's\n  // origin, so its IndexedDB is the *site's* — a library kept there would be\n  // invisible to the extension and cleared with that site's data. So the bytes\n  // go to the service worker, which owns the extension's own origin, one page\n  // per message because chrome messaging is JSON and a Blob is not.\n\n  const chunk = (bytes) => {\n    let bin = '';\n    // 0x8000 at a time: `apply` on a 5 MB array overflows the argument stack.\n    for (let p = 0; p < bytes.length; p += 0x8000) {\n      bin += String.fromCharCode.apply(null, bytes.subarray(p, p + 0x8000));\n    }\n    return btoa(bin);\n  };\n\n  /** The saved/not-saved state of the open chapter, on the button. */\n  function markOffline(saved) {\n    const btn = state.root?.querySelector('[data-act=\"offline\"]');\n    if (!btn) return;\n    btn.textContent = saved ? '📗' : '📥';\n    btn.title = saved ? t('readerSavedOffline') : t('readerSaveOffline');\n    btn.dataset.saved = saved ? '1' : '';\n  }\n\n  async function refreshOffline() {\n    const url = state.meta?.chapterUrl;\n    if (!url) return;\n    const r = await send({ type: 'offlineHas', chapterUrl: url });\n    // The answer is about the chapter that asked it. Two chapters opened one\n    // after the other and the first reply lands last, painting 📗 on a chapter\n    // that was never saved.\n    if (state.meta?.chapterUrl === url) markOffline(!!r?.saved);\n  }\n\n  async function toggleOffline() {\n    const btn = state.root.querySelector('[data-act=\"offline\"]');\n    // Pinned before the first await, and checked after every one. Saving forty\n    // pages takes ten seconds and clicking \"next chapter\" takes one, so every\n    // line below can outlive the chapter it started on — and `state.meta` by\n    // then is the chapter now open. Reading it each time round the loop files\n    // page 30 under the next chapter's URL, silently, producing one saved\n    // chapter that is two chapters interleaved.\n    const url = state.meta?.chapterUrl;\n    if (!url) return;\n    const mine = () => state.meta?.chapterUrl === url;\n\n    if (btn.dataset.saved) {\n      await send({ type: 'offlineRemove', chapterUrl: url });\n      if (mine()) markOffline(false);\n      return;\n    }\n    btn.disabled = true;\n    try {\n      const meta = {\n        ...state.meta,\n        kind: state.novel ? 'text' : 'images',\n        // Prose is small enough to travel in the metadata, so a text chapter is\n        // saved by the commit alone — there is nothing to fetch.\n        paragraphs: state.novel ? state.paragraphs.slice() : undefined,\n        bytes: 0,\n      };\n      if (!state.novel) {\n        const images = state.images.slice();\n        // Same question as the download, asked for the same reason — and here\n        // it matters more: a save is all or nothing, so a chapter that cannot\n        // be fetched wastes the whole loop before saying so.\n        const blocked = await blockedImageHosts(images);\n        if (blocked.length) { askForImageAccess(blocked); return; }\n        for (let i = 0; i < images.length; i++) {\n          if (!mine()) return; // moved on — and nothing has been committed\n          btn.textContent = `${i + 1}/${images.length}`;\n          const bytes = await fetchPageBytes(images[i]);\n          // Not `continue`. A chapter that skipped page 12 commits like any\n          // other, shows 📗 like any other, and opens like any other — three\n          // weeks later, on a train, with nothing left to fetch the gap from.\n          // Offline is a promise about the one moment when nothing can be\n          // repaired, so it is the whole chapter or none of it.\n          if (!bytes) throw new Error(`page ${i + 1} could not be fetched`);\n          const r = await send({\n            type: 'offlinePage',\n            chapterUrl: url,\n            index: i,\n            b64: chunk(bytes),\n            mime: imageType(bytes, images[i]).mime,\n          });\n          if (!r?.ok) throw new Error(`page ${i + 1} was rejected`);\n          meta.bytes += bytes.length;\n        }\n      }\n      // Last, and only now: until this lands the chapter does not exist, which\n      // is what keeps a save interrupted halfway from being offered as\n      // readable. Its pages are swept at the next browser start.\n      const done = await send({ type: 'offlineCommit', meta });\n      if (!done?.ok) throw new Error('commit failed');\n      if (mine()) markOffline(true);\n    } catch (e) {\n      btn.textContent = '⚠';\n      // Said out loud, not left as a glyph. A failed save is indistinguishable\n      // from a slow one until it is named, and the one thing worse than not\n      // having the chapter is thinking you do.\n      flash(t('readerNotSaved', [String(e.message)]), 3000);\n      setTimeout(() => { if (state.root && mine()) markOffline(false); }, 3000);\n    } finally {\n      btn.disabled = false;\n    }\n  }\n\n  // --- library & progress --------------------------------------------------\n\n  // Opens the details sheet rather than adding blind, so folder/score/tags are\n  // captured while the series is in front of you.\n  function addToLibrary() {\n    window.PanelFlowLibraryModal?.open(state.meta);\n  }\n\n  const saveProgress = debounce(() => {\n    if (!state.root) return;\n    chrome.runtime.sendMessage({ type: 'saveProgress', progress: {\n      sourceUrl: state.meta.sourceUrl,\n      chapterUrl: state.meta.chapterUrl,\n      chapterLabel: state.meta.chapterLabel,\n      page: state.page,\n      pageCount: pageTotal(),\n      // A novel's position is the scroll itself, not a page derived from it:\n      // rounding to the nearest screenful of a fifteen-screen chapter drops the\n      // reader up to half a screen from where they stopped.\n      scrollPos: state.novel ? state.scrollRatio\n        : state.page / Math.max(1, state.images.length - 1),\n    }});\n  }, 800);\n\n  // --- how long this chapter was actually read -----------------------------\n  //\n  // Wall clock between opening and closing is not reading time: a chapter left\n  // open in a background tab overnight would claim eight hours, and the whole\n  // point of the statistics is that they are not made up. The clock runs only\n  // while this tab is visible and the reader is open, and it is banked on every\n  // pause so a tab closed without an unload event still counts what it saw.\n\n  const clock = { since: 0, banked: 0, day: null };\n\n  function clockStart() {\n    if (clock.since || !state.root || document.hidden) return;\n    clock.since = Date.now();\n    clock.day ??= localDay();\n  }\n\n  function clockPause() {\n    if (!clock.since) return;\n    clock.banked += Math.round((Date.now() - clock.since) / 1000);\n    clock.since = 0;\n  }\n\n  // A chapter read across midnight is banked under the day it started: it is\n  // one sitting, and splitting it would invent a second read out of a clock.\n  const localDay = (ts = Date.now()) => {\n    const d = new Date(ts);\n    const pad = (n) => String(n).padStart(2, '0');\n    return `${d.getFullYear()}-${pad(d.getMonth() + 1)}-${pad(d.getDate())}`;\n  };\n\n  function bankRead() {\n    clockPause();\n    if (clock.banked < 5) return;   // a glance at the wrong chapter is not a read\n    chrome.runtime.sendMessage({ type: 'recordRead', read: {\n      sourceUrl: state.meta.sourceUrl,\n      chapterUrl: state.meta.chapterUrl,\n      chapterLabel: state.meta.chapterLabel,\n      pages: state.page + 1,\n      seconds: clock.banked,\n      day: clock.day,\n    }});\n    clock.banked = 0;\n  }\n\n  const onVisibility = () => (document.hidden ? bankRead() : clockStart());\n\n  function restoreProgress() {\n    chrome.runtime.sendMessage(\n      { type: 'getProgressFor', chapterUrl: state.meta.chapterUrl },\n      (resp) => {\n        if (chrome.runtime.lastError || !resp || !resp.progress) return;\n        const p = resp.progress;\n        if (state.mode === 'vertical') {\n          // This runs while the strip is still empty of laid-out images, so the\n          // scroll has to wait for a height like the mode switch does — it used\n          // to multiply by zero and drop the reader at the top of the chapter,\n          // in the one mode that is the default.\n          const ratio = Number(p.scrollPos);\n          if (!Number.isFinite(ratio) || ratio <= 0) return;\n          state.scrollRatio = ratio;\n          state.page = Math.round(ratio * Math.max(0, pageTotal() - 1));\n          updateCounter();\n          scrollToRatio($('.pf-stage'), ratio);\n        } else if (p.page > 0) {\n          showPage(p.page);\n        }\n      });\n  }\n\n  // --- utils ---------------------------------------------------------------\n\n  function clamp(v, lo, hi) { return Math.min(hi, Math.max(lo, v)); }\n  function debounce(fn, ms) {\n    let timer;\n    const wrapped = (...a) => { clearTimeout(timer); timer = setTimeout(() => fn(...a), ms); };\n    wrapped.flush = () => { clearTimeout(timer); fn(); };\n    return wrapped;\n  }\n\n  // A text chapter is the same reader with paragraphs where the images go, so it\n  // is the same open() — the empty image list is what everything downstream\n  // (page count, download, mode picker) branches on through state.novel.\n  const openText = (paragraphs, meta, rule) => open([], meta, rule, null, paragraphs);\n\n  window.PanelFlowReader = { open, openText, close, isOpen };\n})();\n\n}catch(e){if(window.PanelFlowFailed)window.PanelFlowFailed(\"reader.js\",e);else console.warn('panelflow: reader.js failed',e)}\n\ntry{\n  if(!document.getElementById('panelflow-reader-css')){\n    var s=document.createElement('style');\n    s.id='panelflow-reader-css';\n    s.textContent=\"/* PanelFlow content styles: detection pill + reader overlay */\\n\\n/* The one surface that cannot link shared/theme.css.\\n *\\n * This file is injected into a stranger's page, so it may not declare anything\\n * on `:root` — that is the host's element and its variables are the host's —\\n * and a content script cannot @import. So the palette is repeated here, on our\\n * own two roots, and backend/test/theme.test.js compares every value below\\n * against the matching --dark-* in shared/theme.css. Change one, change both,\\n * or the suite says so.\\n *\\n * They are the dark values on purpose and in both themes: a page of artwork is\\n * looked at, and paper-white margins around it are a lamp pointed at the\\n * reader. The setting that lets someone insist otherwise lives in the options\\n * page, not here.\\n */\\n#panelflow-pill,\\n#panelflow-reader {\\n  --pf-bg: #12100f;\\n  --pf-surface: #1c1917;\\n  --pf-surface-hi: #262220;\\n  --pf-line: #38332f;\\n  --pf-text: #fafaf9;\\n  --pf-muted: #a8a29e;\\n  --pf-accent: #e8613c;\\n  --pf-unread: #e0a15c;\\n  --pf-scrim: rgba(0, 0, 0, .6);\\n}\\n\\n/* The other palette, for the reader who turned \\\"keep the reader dark\\\" off.\\n *\\n * It asks the operating system rather than the settings page, and that is not\\n * laziness: the reader runs on a scan site's origin, where this extension's\\n * localStorage — the whole of shared/theme.js — does not exist. The system is\\n * the only answer both sides can hear. Values below are the --light-* set in\\n * shared/theme.css, checked by backend/test/theme.test.js.\\n *\\n * --pf-unread is absent on purpose: \\\"there is something here you have not\\n * read\\\" is information, and information does not change colour with the room.\\n *\\n * The pill is absent too. It is a chip floating over someone else's page, not\\n * a surface anyone reads on, and it is put there by content/detect.js, which\\n * has no reason to load the reader's preferences to tint one button.\\n */\\n@media (prefers-color-scheme: light) {\\n  #panelflow-reader.pf-follow-system {\\n    --pf-bg: #f7f4ec;\\n    --pf-surface: #ffffff;\\n    --pf-surface-hi: #ede8dc;\\n    --pf-line: #ddd5c6;\\n    --pf-text: #1a1714;\\n    --pf-muted: #6b635c;\\n    --pf-accent: #c64a28;\\n    --pf-scrim: rgba(26, 23, 20, .35);\\n  }\\n}\\n\\n#panelflow-pill {\\n  position: fixed;\\n  right: 16px;\\n  bottom: 16px;\\n  z-index: 2147483646;\\n  padding: 10px 16px;\\n  border: none;\\n  border-radius: 999px;\\n  background: var(--pf-surface);\\n  color: var(--pf-text);\\n  font: 600 14px/1 system-ui, sans-serif;\\n  box-shadow: 0 4px 16px var(--pf-scrim);\\n  cursor: pointer;\\n  opacity: .92;\\n}\\n#panelflow-pill:hover { opacity: 1; transform: scale(1.03); }\\n\\n.panelflow-noscroll { overflow: hidden !important; }\\n\\n#panelflow-reader {\\n  position: fixed;\\n  inset: 0;\\n  z-index: 2147483647;\\n  background: var(--pf-bg);\\n  color: var(--pf-text);\\n  font: 14px system-ui, sans-serif;\\n  display: flex;\\n  flex-direction: column;\\n}\\n\\n#panelflow-reader .pf-chrome {\\n  position: absolute;\\n  left: 0; right: 0;\\n  display: flex;\\n  align-items: center;\\n  gap: 10px;\\n  padding: 10px 14px;\\n  background: color-mix(in srgb, var(--pf-bg) 92%, transparent);\\n  transition: transform .2s ease, opacity .2s ease;\\n  z-index: 2;\\n}\\n#panelflow-reader .pf-topbar { top: 0; }\\n/* Leave room for the progress bar, which is pinned below it. */\\n#panelflow-reader .pf-bottombar {\\n  bottom: 0;\\n  justify-content: space-between;\\n  padding-bottom: calc(10px + var(--pf-progress-h, 3px));\\n}\\n#panelflow-reader.pf-chrome-hidden .pf-topbar { transform: translateY(-100%); opacity: 0; }\\n#panelflow-reader.pf-chrome-hidden .pf-bottombar { transform: translateY(100%); opacity: 0; }\\n\\n#panelflow-reader .pf-title {\\n  flex: 1;\\n  overflow: hidden;\\n  text-overflow: ellipsis;\\n  white-space: nowrap;\\n  font-weight: 600;\\n}\\n#panelflow-reader .pf-btn {\\n  background: var(--pf-surface-hi);\\n  color: var(--pf-text);\\n  border: none;\\n  border-radius: 6px;\\n  padding: 6px 10px;\\n  cursor: pointer;\\n  font: inherit;\\n}\\n#panelflow-reader .pf-btn:hover { background: color-mix(in srgb, var(--pf-surface-hi) 80%, var(--pf-text)); }\\n\\n#panelflow-reader .pf-stage {\\n  position: absolute;\\n  inset: 0;\\n  overflow: hidden;\\n  touch-action: none;\\n  z-index: 1;\\n}\\n#panelflow-reader .pf-stage.pf-vertical {\\n  overflow-y: auto;\\n  touch-action: pan-y;\\n  display: flex;\\n  flex-direction: column;\\n  align-items: center;\\n}\\n#panelflow-reader .pf-vertical img {\\n  display: block;\\n  max-width: min(var(--pf-width, 100%), 900px);\\n  height: auto;\\n  margin: 0 auto var(--pf-gap, 0);\\n}\\n\\n/* Text chapters. Same scrolling stage as the long strip — only the thing being\\n   scrolled changes — with the measure kept narrow and the chrome's height left\\n   clear at both ends so the first and last lines are never under a bar. */\\n#panelflow-reader .pf-novel { justify-content: flex-start; }\\n#panelflow-reader .pf-text {\\n  width: 100%;\\n  /* The host page's box-sizing is whatever it is, and \\\"text width\\\" has to mean\\n     the column you see, not the column minus the padding. */\\n  box-sizing: border-box;\\n  max-width: var(--pf-textw, 680px);\\n  padding: 64px 22px 92px;\\n  font-family: Georgia, \\\"Times New Roman\\\", serif;\\n  font-size: var(--pf-font, 18px);\\n  line-height: var(--pf-lh, 1.65);\\n  color: color-mix(in srgb, var(--pf-text) 90%, var(--pf-muted));\\n  /* The reason someone reads prose in here rather than on the page: copying a\\n     line has to keep working, so nothing may suppress selection. */\\n  user-select: text;\\n}\\n#panelflow-reader .pf-text p { margin: 0 0 1.1em; }\\n#panelflow-reader .pf-text p:last-child { margin-bottom: 0; }\\n\\n#panelflow-reader .pf-paged { display: flex; align-items: center; justify-content: center; }\\n#panelflow-reader .pf-zoomwrap {\\n  display: flex;\\n  align-items: center;\\n  justify-content: center;\\n  transform-origin: 0 0;\\n  will-change: transform;\\n}\\n#panelflow-reader .pf-zoomwrap img {\\n  max-height: 100vh;\\n  max-width: 50vw;\\n  height: auto;\\n  user-select: none;\\n  -webkit-user-drag: none;\\n}\\n#panelflow-reader .pf-zoomwrap img:only-child { max-width: 100vw; }\\n\\n#panelflow-reader .pf-scrub {\\n  flex: 1;\\n  accent-color: var(--pf-accent);\\n  min-width: 80px;\\n}\\n#panelflow-reader .pf-scrub.pf-rtl { direction: rtl; }\\n\\n#panelflow-reader .pf-mode { max-width: 130px; }\\n#panelflow-reader .pf-btn.pf-on { background: var(--pf-accent); }\\n\\n/* The chapter wheel. A <select> is fine for the three chapters a site links\\n   from the page you are on and useless for the two hundred it does not, so the\\n   list is a scroller you spin to the chapter you want: whatever is in the middle\\n   is what is picked. It is padded by half its own height at each end so the\\n   first and last chapters can reach the middle too, and every row is exactly\\n   --pf-row tall so the script can turn a scroll position into an index without\\n   measuring anything. */\\n#panelflow-reader .pf-chapwrap { position: relative; }\\n#panelflow-reader .pf-chapbtn {\\n  max-width: 180px;\\n  overflow: hidden;\\n  text-overflow: ellipsis;\\n  white-space: nowrap;\\n}\\n#panelflow-reader .pf-wheel {\\n  --pf-row: 32px;\\n  --pf-rows: 7;\\n  position: absolute;\\n  top: calc(100% + 6px);\\n  left: 0;\\n  /* The host page's box-sizing is whatever it is, and the height has to mean\\n     the seven rows you see — the padding scrolls with the content, it does not\\n     make the wheel taller. */\\n  box-sizing: border-box;\\n  /* Plus the border, so that the window itself is a whole number of rows: a\\n     couple of pixels either way and the centre band no longer lines up with the\\n     row it is marking. */\\n  height: calc(var(--pf-row) * var(--pf-rows) + 2px);\\n  padding: calc(var(--pf-row) * (var(--pf-rows) - 1) / 2) 0;\\n  min-width: 180px;\\n  overflow-y: auto;\\n  overscroll-behavior: contain;\\n  touch-action: pan-y;\\n  scroll-snap-type: y mandatory;\\n  /* The band across the middle marks the row that will be picked. It is painted\\n     as the wheel's own background rather than as an element, because a scroll\\n     container's background stays put while its contents move — which is exactly\\n     what a wheel's window does. */\\n  background-color: var(--pf-surface);\\n  background-image: linear-gradient(\\n    to bottom,\\n    transparent calc(var(--pf-row) * (var(--pf-rows) - 1) / 2),\\n    color-mix(in srgb, var(--pf-accent) 16%, transparent) calc(var(--pf-row) * (var(--pf-rows) - 1) / 2),\\n    color-mix(in srgb, var(--pf-accent) 16%, transparent) calc(var(--pf-row) * (var(--pf-rows) + 1) / 2),\\n    transparent calc(var(--pf-row) * (var(--pf-rows) + 1) / 2));\\n  background-repeat: no-repeat;\\n  border: 1px solid var(--pf-line);\\n  border-radius: 8px;\\n  box-shadow: 0 10px 28px var(--pf-scrim);\\n  z-index: 3;\\n}\\n#panelflow-reader .pf-wrow {\\n  box-sizing: border-box;\\n  height: var(--pf-row);\\n  line-height: calc(var(--pf-row) - 2px);\\n  padding: 0 14px;\\n  scroll-snap-align: center;\\n  white-space: nowrap;\\n  overflow: hidden;\\n  text-overflow: ellipsis;\\n  color: var(--pf-muted);\\n  cursor: pointer;\\n  transition: color .12s ease;\\n}\\n/* Already read, not yet read, and the one on screen. These come first on\\n   purpose: the row in the middle is the one about to be opened and has to be\\n   the brightest thing in the wheel, whatever else is true of it — a chapter you\\n   have read dimming the centre is how you lose track of where the wheel is\\n   pointing. Read recedes and unread advances, so the boundary between them\\n   falls out of the colours instead of having to be looked for. */\\n#panelflow-reader .pf-wrow.pf-read { color: color-mix(in srgb, var(--pf-muted) 60%, var(--pf-bg)); }\\n#panelflow-reader .pf-wrow.pf-unread { color: var(--pf-unread); }\\n#panelflow-reader .pf-wrow.pf-here { color: var(--pf-accent); }\\n#panelflow-reader .pf-wrow:hover { color: color-mix(in srgb, var(--pf-text) 85%, var(--pf-muted)); }\\n#panelflow-reader .pf-wrow.pf-on { color: var(--pf-text); font-weight: 600; }\\n#panelflow-reader .pf-wrow.pf-on.pf-here { color: color-mix(in srgb, var(--pf-accent) 55%, var(--pf-text)); }\\n#panelflow-reader .pf-wnote {\\n  color: color-mix(in srgb, var(--pf-muted) 60%, var(--pf-bg));\\n  font-size: 11px;\\n  cursor: default;\\n}\\n\\n/* Floating vertical stack on the right edge: secondary actions that should not\\n   compete with the chapter navigation up top. */\\n#panelflow-reader .pf-side {\\n  left: auto;\\n  right: 12px;\\n  top: 50%;\\n  transform: translateY(-50%);\\n  flex-direction: column;\\n  gap: 8px;\\n  padding: 8px;\\n  width: max-content;\\n  border-radius: 999px;\\n  background: color-mix(in srgb, var(--pf-bg) 72%, transparent);\\n  backdrop-filter: blur(6px);\\n}\\n#panelflow-reader .pf-side .pf-btn {\\n  width: 34px;\\n  height: 34px;\\n  padding: 0;\\n  border-radius: 50%;\\n  font-size: 15px;\\n  line-height: 1;\\n}\\n#panelflow-reader .pf-side .pf-btn[hidden] { display: none; }\\n#panelflow-reader.pf-chrome-hidden .pf-side {\\n  transform: translateY(-50%) translateX(140%);\\n  opacity: 0;\\n  pointer-events: none;\\n}\\n\\n/* Reading progress. Sits outside .pf-chrome on purpose, so hiding the controls\\n   keeps it on screen; --pf-progress-h 0 removes it entirely. */\\n#panelflow-reader .pf-progress {\\n  position: absolute;\\n  left: 0; right: 0; bottom: 0;\\n  height: var(--pf-progress-h, 3px);\\n  background: color-mix(in srgb, var(--pf-text) 12%, transparent);\\n  z-index: 3;\\n  pointer-events: none;\\n}\\n#panelflow-reader .pf-progress-fill {\\n  height: 100%;\\n  width: 0;\\n  background: var(--pf-accent);\\n  transition: width .12s linear;\\n}\\n\\n#panelflow-reader .pf-prefs .pf-mode,\\n#panelflow-reader .pf-prefs .pf-select {\\n  background: var(--pf-surface-hi);\\n  color: var(--pf-text);\\n  border: none;\\n  border-radius: 6px;\\n  padding: 5px 8px;\\n  font: inherit;\\n  max-width: 150px;\\n}\\n\\n#panelflow-reader .pf-prefs[hidden] { display: none; }\\n#panelflow-reader .pf-prefs {\\n  top: auto;\\n  bottom: 56px;\\n  left: auto;\\n  right: 56px;\\n  width: 240px;\\n  flex-direction: column;\\n  align-items: stretch;\\n  gap: 6px;\\n  border-radius: 10px;\\n  border: 1px solid var(--pf-line);\\n  box-shadow: 0 8px 24px var(--pf-scrim);\\n  z-index: 3;\\n}\\n#panelflow-reader .pf-prefs label {\\n  display: flex;\\n  align-items: center;\\n  justify-content: space-between;\\n  gap: 10px;\\n  font-size: 12px;\\n  color: color-mix(in srgb, var(--pf-text) 80%, var(--pf-muted));\\n}\\n/* The rows are `display: flex`, which outranks the browser's bare\\n   `[hidden] { display: none }` — without this, syncPrefsRows() sets the\\n   attribute and the strip controls stay on screen inside a text chapter. A\\n   global `[hidden]` rule is what the other stylesheets use, but this file is\\n   injected into someone else's page and must not restyle it. */\\n#panelflow-reader .pf-prefs label[hidden] { display: none; }\\n#panelflow-reader .pf-prefs input[type=\\\"range\\\"] {\\n  width: 130px;\\n  accent-color: var(--pf-accent);\\n}\\n#panelflow-reader .pf-prefs .pf-check { justify-content: flex-start; }\\n#panelflow-reader.pf-chrome-hidden .pf-prefs { display: none; }\\n\\n/* Transient notices: the reading direction on open, and the tap zones when they\\n   are changed. Both sit above the artwork and below nothing, and neither may\\n   ever eat a tap — they appear precisely where you are about to tap next. */\\n#panelflow-reader .pf-toast {\\n  position: absolute;\\n  left: 50%;\\n  bottom: 84px;\\n  transform: translateX(-50%);\\n  z-index: 4;\\n  max-width: min(90vw, 420px);\\n  padding: 9px 16px;\\n  border-radius: 999px;\\n  background: color-mix(in srgb, var(--pf-bg) 92%, transparent);\\n  border: 1px solid var(--pf-line);\\n  font-size: 13px;\\n  text-align: center;\\n  pointer-events: none;\\n  opacity: 0;\\n  transition: opacity .2s ease;\\n}\\n#panelflow-reader .pf-toast.pf-on { opacity: 1; }\\n/* The toast lets clicks through to the page turn underneath it; its button is\\n   the one thing in it that must not. */\\n#panelflow-reader .pf-toastbtn {\\n  pointer-events: auto;\\n  margin-left: 10px;\\n  padding: 3px 10px;\\n  border-radius: 999px;\\n  border: 1px solid var(--pf-line);\\n  background: var(--pf-surface-hi);\\n  color: inherit;\\n  font: inherit;\\n  font-size: 12px;\\n  cursor: pointer;\\n}\\n#panelflow-reader .pf-toastbtn:hover { border-color: var(--pf-accent); }\\n#panelflow-reader .pf-toast[hidden] { display: none; }\\n\\n#panelflow-reader .pf-zones {\\n  position: absolute;\\n  inset: 0;\\n  z-index: 4;\\n  pointer-events: none;\\n  opacity: 0;\\n  transition: opacity .2s ease;\\n}\\n#panelflow-reader .pf-zones.pf-on { opacity: 1; }\\n#panelflow-reader .pf-zones[hidden] { display: none; }\\n#panelflow-reader .pf-zone {\\n  position: absolute;\\n  top: 0;\\n  bottom: 0;\\n  display: flex;\\n  align-items: center;\\n  justify-content: center;\\n  box-sizing: border-box;\\n  border: 1px dashed color-mix(in srgb, var(--pf-text) 25%, transparent);\\n  background: color-mix(in srgb, var(--pf-accent) 16%, transparent);\\n  font-weight: 600;\\n  font-size: 13px;\\n  text-shadow: 0 1px 3px var(--pf-scrim);\\n}\\n#panelflow-reader .pf-zone:nth-child(2) { background: color-mix(in srgb, var(--pf-text) 6%, transparent); }\\n\\n/* The help list. Not part of .pf-chrome: it explains how to bring the controls\\n   back, so it has to survive them being hidden. */\\n#panelflow-reader .pf-help {\\n  position: absolute;\\n  left: 50%;\\n  top: 50%;\\n  transform: translate(-50%, -50%);\\n  z-index: 5;\\n  width: min(92vw, 430px);\\n  max-height: 82vh;\\n  overflow-y: auto;\\n  padding: 16px 18px;\\n  border-radius: 12px;\\n  border: 1px solid var(--pf-line);\\n  background: color-mix(in srgb, var(--pf-bg) 97%, transparent);\\n  box-shadow: 0 12px 40px var(--pf-scrim);\\n}\\n#panelflow-reader .pf-help[hidden] { display: none; }\\n#panelflow-reader .pf-help h3 { margin: 0 0 10px; font-size: 16px; font-weight: 700; }\\n#panelflow-reader .pf-help ul { margin: 0; padding: 0 0 0 18px; list-style: disc; }\\n#panelflow-reader .pf-help li { margin: 0 0 7px; font-size: 13px; line-height: 1.45; color: color-mix(in srgb, var(--pf-text) 80%, var(--pf-muted)); }\\n#panelflow-reader .pf-help li b { color: var(--pf-text); font-weight: 700; }\\n#panelflow-reader .pf-help .pf-help-note { margin: 10px 0 12px; font-size: 12px; color: var(--pf-muted); }\\n\\n/* The switch that makes the panel above belong to this series rather than to\\n   every series. It is set apart by a rule because it changes the meaning of\\n   the controls it sits under, and a checkbox in the middle of a list of\\n   checkboxes reads as one more of them. */\\n#panelflow-reader .pf-prefs .pf-seriesrow {\\n  border-top: 1px solid var(--pf-line);\\n  margin-top: 2px;\\n  padding-top: 8px;\\n  color: var(--pf-text);\\n}\\n#panelflow-reader .pf-prefs .pf-seriesrow[hidden] { display: none; }\\n\\n/* The end of the chapter. Anchored to the bottom because that is where the\\n   reading was: a panel in the middle of the screen makes you look up from the\\n   last page you were on to find out what happened. It rises 6px into place so\\n   it reads as arriving rather than as having always been there — a modal that\\n   fades in on the spot is indistinguishable from one that was already open.\\n   Navigation and nothing else lives in here (redesign.md, §2.3). */\\n#panelflow-reader .pf-end {\\n  position: absolute;\\n  left: 50%;\\n  bottom: 84px;\\n  transform: translate(-50%, 6px);\\n  z-index: 5;\\n  box-sizing: border-box;\\n  width: min(92vw, 380px);\\n  display: flex;\\n  flex-direction: column;\\n  gap: 4px;\\n  padding: 14px 16px;\\n  border-radius: 12px;\\n  border: 1px solid var(--pf-line);\\n  background: color-mix(in srgb, var(--pf-bg) 97%, transparent);\\n  box-shadow: 0 12px 40px var(--pf-scrim);\\n  opacity: 0;\\n  transition: opacity .16s ease, transform .16s ease;\\n}\\n#panelflow-reader .pf-end.pf-on { opacity: 1; transform: translate(-50%, 0); }\\n#panelflow-reader .pf-end[hidden] { display: none; }\\n#panelflow-reader .pf-end-title { margin: 0; font-size: 15px; font-weight: 700; }\\n#panelflow-reader .pf-end-left { margin: 0; font-size: 12px; color: var(--pf-muted); }\\n#panelflow-reader .pf-end-left:empty { display: none; }\\n#panelflow-reader .pf-end-acts {\\n  display: flex;\\n  flex-wrap: wrap;\\n  gap: 8px;\\n  margin-top: 8px;\\n}\\n#panelflow-reader .pf-end-acts .pf-btn { flex: 1 1 auto; min-width: 96px; }\\n#panelflow-reader .pf-end-acts .pf-btn[hidden] { display: none; }\\n#panelflow-reader .pf-end-acts .pf-btn:disabled { opacity: .5; cursor: default; }\\n/* The next chapter is the one thing anybody came to this panel for. */\\n#panelflow-reader .pf-end-go {\\n  background: var(--pf-accent);\\n  color: var(--pf-bg);\\n  border-color: var(--pf-accent);\\n}\\n\\n/* --- reduced motion -------------------------------------------------------\\n   What \\\"reduce\\\" means here, and what it deliberately does not mean.\\n\\n   Movement goes: the chrome stops sliding, the progress bar stops growing, the\\n   end panel stops rising. Fades stay, because in this reader the fade *is* the\\n   information — a toast that appears and disappears with no transition at all\\n   reads as a flicker, and the end panel arriving instantly is indistinguishable\\n   from a panel that was always open.\\n\\n   The chrome keeps its `translateY(-100%)` as a resting position rather than\\n   as an animation. It has to: an invisible bar left in place still swallows\\n   every tap along the top and bottom edges of the screen, which are the two\\n   places this reader turns pages. So the transform applies at once and only the\\n   opacity is timed.\\n\\n   Nothing here hides anything that was visible before. */\\n@media (prefers-reduced-motion: reduce) {\\n  #panelflow-reader .pf-chrome { transition: opacity .2s ease; }\\n  #panelflow-reader .pf-progress-fill { transition-duration: 0.01ms; }\\n  /* Colour marking the row about to open: the colour is the information, the\\n     fade to it is not, and it costs nothing to arrive at once. */\\n  #panelflow-reader .pf-wrow { transition-duration: 0.01ms; }\\n  #panelflow-reader .pf-end {\\n    transform: translate(-50%, 0);\\n    transition: opacity .16s ease;\\n  }\\n}\\n\";\n    (document.head||document.documentElement).appendChild(s);\n  }\n}catch(e){if(window.PanelFlowFailed)window.PanelFlowFailed(\"reader.css\",e);else console.warn('panelflow: reader.css failed',e)}";
